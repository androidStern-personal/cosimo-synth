Paused work

Renderer tuning toward the Serum look is paused while we plan the iOS AUv3 build.

The iOS AUv3 shell work is now paused while we pivot back to wavetable-bank implementation.

What is true right now
- The macOS Ableton Live development plug-in works.
- That plug-in is a desktop-only development shell and should stay that way.
- The detailed AUv3 research, testing strategy, and implementation plan now live in `AUV3_RESEARCH_AND_PLAN.md`.
- The current app ships one generated demo bank at `assets/factory-bank.wav`.
- That bank is built by `build_assets.py` from `bench.py` fixture-generation code through `wtbank.py`.
- The offline bank format already supports multiple tables, but the live synth still hardcodes table selection at oscillator construction time.

What we are doing instead
- Research how Serum wavetable exports can be converted into this repo's bank format.
- Research how to replace the current single-bank asset path with a real factory-bank bundling pipeline.
- Research the DSP, manifest, and UI changes needed to switch among bundled tables at runtime.

What happens next
- Finish the Serum import, bank pipeline, and runtime-switching research.
- Turn that research into a concrete implementation plan before resuming the paused AUv3 shell work.
