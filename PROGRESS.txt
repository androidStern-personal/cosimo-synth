Cosimo web effects rack integration — 2026-08-01

Branch, deployment, rollback
- Integration branch: `codex/web-effects-rack-integration`, based on deployed source `c585580`.
- Exact rack commits were cherry-picked as `730cf06` (`9ebee83`) and `047d705` (`27ddb2c`).
- Approved-workspace implementation checkpoint: `a2a6ddd98d9da885ef2e30a77b6336ccd063cf54`; interaction/CPU stabilization checkpoint: `a063eb84783135030ed467d6d85342f9b5850558`; browser-hardening checkpoint: `89d1945cc12b14595ba461bd2695b0e97bb590a5`; touch-ownership checkpoint: `4c58fb06009a2a28ea9a59a3b6dfbe0bda28c961`; deterministic-build checkpoint: `b89d65fd14e3736a27364209a8592b0820c58cbd`; deployed lossless-font checkpoint: `2142fb84e893f5ba4290a6d3cc7711f265c3b6ce`.
- Sites version 18 is public at `https://cosimo-synth.andrew-martin-stern.chatgpt.site/` with public access and contains exact checkpoint `2142fb84e893f5ba4290a6d3cc7711f265c3b6ce`.
- Sites version 17 (`b89d65f`) is the immediate rollback, version 16 (`4c58fb0`) is the pre-deterministic-build rollback, version 15 (`89d1945`) is the browser-hardening rollback, version 14 (`a063eb8`) is the pre-touch-ownership rollback, version 11 (`8f9fc8e`) is the pre-accordion playback-session rollback, version 5 (`70459f0`) is the pre-redesign rollback, and source `c585580` remains the requested pre-rack rollback.

Product and architecture
- The main Cmajor graph has a real ordered rack: Global Filter, Distortion, OTT, Chorus, Flanger, Phaser, Delay, Reverb.
- `sharedEngine.rackModOut` is connected to `rack.rackModIn`; 36 continuous rack destinations are live. Voice sources use Mean/Max reduction; macro/global sources do not.
- The full React synth UI exposes enable/bypass, grip-only single-commit drag/drop reorder, compact collapsed controls, and full editors for all eight modules.
- Distortion and Chorus reuse their established UI, OTT reuses the lab vocabulary, and Phaser/Delay have mutually correct Free/Sync controls.
- One descriptor table (`ui/shared/rack-parameter-descriptors.ts`) owns parameter ranges, labels, displays, and bindings. Approved rack art and wordmark fonts are vendored with attribution in `CREDITS.md`; instrument glyphs continue to use fontaudio.
- Reorders crossfade processed contribution through dry instead of muting the instrument. Time effects use constant-sum mix law; reverb has early energy and decay dependence; sync divisions are monotonic.
- The web pipeline still generates the real embedded Cmajor WebAssembly directly from `WavetableSynth.cmajorpatch`; no mock, localhost, runtime compiler, or external build service is shipped.

Deliberate clean break
- Canonical browser/adapter state is exactly `rack.v1 = {format, version, order, enabled}` and restores once per DSP session.
- The deprecated duplicate `chorusEnabled` endpoint and compatibility plumbing were removed. There is no Chorus migration or dual representation.
- Unpublished presets, DAW sessions, and automation tied to the old endpoint layout may break. Rationale is in `docs/ADR-015-clean-rack-state-and-structure-commit.md`.

Acceptance evidence
- Rack Cmajor suite: 59/59. Warp regression: 16/16 Cmajor and 5/5 Python. Filter regression: 14/14 Cmajor and 31/31 Python.
- Current desktop UI suite: 93/93. Shared hooks: 21/21. Adapter/state/modulation/descriptor/display/worker matrix: 245/245. Effect preset suite: 112/112.
- Production `npm run sites:build` passes from clean source. The packaged public assets contain no localhost/file references or symlinks.
- Chromium logged-out production proof passed: non-silent notes, eight real rack modules, parameter and modulation audio deltas, gain-safe all-on rack, `rack.v1` reload, no console/application-request errors, zero silent analyser polls, and zero over-budget worklet blocks in the measured segment.
- WebKit 26.4 iPhone-13-profile production proof passed all five applicable cases: click-to-start, touch note down/up, visible depression, sticky keyboard, mobile wavetable/filter cards, the FX accordion, rack audio/state/modulation, and no silent analyser polls. The final public run measured worklet average load 0.250 with zero over-budget blocks.
- Initial Cmajor WASM memory is 917 pages (about 58.7 MiB). Public engine/UI/worker hashes match the clean local `dist` exactly; Cloudflare injects its normal challenge bootstrap into public HTML. All checked public assets return 200 and recent worker error logs are empty.
- `npm run synth:desktop:build` passed and installed the self-contained compiled VST3 at `/Users/winterfell/Library/Audio/Plug-Ins/VST3/CosimoDesktopNative.vst3` with valid ad-hoc signing.
- `npm run synth:desktop:dev` started a fresh Vite server from this repo at `http://127.0.0.1:5174` and launched the standalone app against HMR. A final compiled install pass restored the on-disk Ableton VST3 without stopping the already-running HMR review process.

Physical iPhone acceptance
- The paired iPhone 14 Pro (`00C7F433-8B6A-5CAC-856F-56D7385E12F9`) opened public Sites version 11 in Safari. With the hardware silent switch enabled, the user tapped Start, held an on-screen key, saw the key depress, and confirmed audible physical output. Bluetooth and Safari inspection settings were not changed.

Approved mobile rack workspace — 2026-08-06
- The approved variant-D faceplates, individual wordmark fonts, square rack rows, bare power controls, four-dot grips, one-line quick values, and thick bottom value meters are the shared rack workspace. The current accordion integration uses 48-pixel rows while retaining 44-pixel grip/power targets; all eight rows remain visible at 320/375/390/430-pixel widths.
- Mobile now uses one Voice/FX/Mod accordion with every section header always visible and exactly one expanded. The sticky keyboard stays mounted throughout; modulation amount consumes only the selected-effect column.
- Rack parameter gestures use the shared pointer-capture controller. Hover cannot change a value, cancellation/lost capture/window blur closes the host gesture, and only the dedicated grip can begin reorder. Reorder previews optimistically and sends one complete DSP structure commit on drop. The generated shadow-root build specifically verifies this path.
- The mod bar uses the approved exact circular image assets and exposes only MSEG 1–3, Envelope 1–3, and Macro 1–3. The user explicitly rejected LFOs: the LFO asset family, generator entries, state, routes, and engine work are absent. Looping MSEG is the repeating source. See `docs/ADR-016-mobile-rack-workspace-and-no-lfo.md`.
- Source pages carousel for 280 ms; the selected source remains visible. Source-first and target-first route creation both work, and one right-column bipolar control edits the real selected source/target route intersection.
- Local package and fresh logged-out public proofs are green against the exact Sites artifact in Chromium and WebKit 26.4. WebKit iPhone profile verifies click-to-start, the FX accordion, eight rows, no LFO, touch note down/up, visible held-key depression, non-silent audio, and zero over-budget worklet blocks in the measured segment.
- The 2026-08-07 stabilization pass closed the remaining articulation, keyboard, iOS wavetable-inset, and `CanvasWavetableDisplay.setDrawableInsets` type contracts; `npx tsc --noEmit` is clean.

Mobile rack touch repair — 2026-08-06
- Safari touch moves must not use `PointerEvent.buttons` as the drag-liveness signal. WebKit bug 201770 documents active touch `pointermove` events reporting `buttons === 0`; only mouse movement now uses the zero-button release fallback.
- Rack reorder captures on the stable rack list. Optimistic faceplate reordering can no longer move the capture owner out from under the active finger, and the grip plus descendants explicitly suppress touch panning/callouts.
- Modulation source drop hit-testing uses the source's `Document` or `ShadowRoot`, not the outer document. A touch drag from MSEG 1 onto Distortion Knee now creates the real `rack.distortionKnee` route in the generated Cmajor view.
- Two production-seam touch regressions were red before the fix and green afterward in 2.8 seconds. The current full desktop UI is 91/91; generated Chromium passes 5/5 applicable cases and WebKit passes 5/5 applicable cases with non-silent audio and zero measured worklet overruns.
- Sites version 9 was verified at the public URL twice with real Chromium touch injection: reorder commits without scrolling under simulated WebKit zero-button moves, and MSEG 1 drops onto Distortion Knee. The matching curated public WebKit suite passes all five applicable cases; average measured worklet load was 0.257 with zero over-budget blocks.
- Direct rack gesture automation on the physical phone remains unavailable without changing Bluetooth or Safari inspection settings. The unchanged rack paths remain covered by real Chromium touch injection, the WebKit zero-button compatibility case, and the iPhone-profile WebKit suite; physical Safari now also proves Start, key depression, and audible output.

Mobile Safari audio recovery — 2026-08-06
- A physical-phone report exposed a gap in the previous touchscreen proof: it verified note delivery and non-silent output in a continuously running context, but did not cover Safari suspension/interruption after Start.
- The web host now calls `AudioContext.resume()` before its first async boundary in the Start gesture, connects the Cmajor graph exactly once, and resumes an already-connected non-running context from the next pointer/touch gesture. It no longer treats `started` as proof that Safari is still running.
- The new production-seam WebKit case was red before the repair (keyboard touch left the forced-suspended context silent) and green afterward. It requires the note touch to restore `running`, keep the key visibly pressed, and produce analyser peak above `0.00001` after metrics reset.
- Exact packaged Sites artifact passes all five applicable Chromium and five applicable WebKit cases. WebKit measured all-eight-on gain at -2.80 dB, average worklet load 0.267, zero over-budget blocks, and non-silent recovered touch output. The package has no symlinks or localhost/file references.
- Physical iPhone testing then isolated the remaining silence: Start disappeared and touched keys visibly lit, but output became audible immediately after the user disabled the hardware silent switch. This matches WebKit's ambient-session behavior rather than a DSP or MIDI failure.
- The host now opts an explicitly started synth into `navigator.audioSession.type = "playback"` before resume (and again on interruption recovery). A focused WebKit regression was red at `ambient` and green at `playback`; browsers without the Audio Session API keep their existing behavior.
- Sites version 11 is the public playback-session build. Fresh logged-out public Chromium and WebKit suites pass all five applicable cases; WebKit measured -2.94 dB all-eight-on gain, 0.136 average worklet load, zero over-budget blocks, and no silent held-note polls. Recent production worker error logs are empty.
- Final physical proof passed on iPhone Safari with the hardware silent switch enabled: Start completed, the touched key visibly depressed, and the user confirmed audible output.

Mobile accordion and dual-ring rack controls — 2026-08-06
- Voice, FX, and Mod are one persistent accordion; second-tapping the selected FX mod source deep-links its exact MSEG/Envelope/Macro editor, and Back restores the originating effect/source/target context. Voice uses the approved two-column wavetable/filter layout. The keyboard is 18 notes tall with compact octave controls and no mobile OCT/root labels.
- Continuous rack parameters share the stippled dual-ring knob in `ui/desktop/rack-parameter-knob.tsx`: inner base sector, selected-route outer sector, fixed factory-default marker, pointer capture, stable HUD, movement-cancelled long press, and best-effort host haptic. The route amount slider remains authoritative; direct outer-ring editing is the fast path.
- The long-press/right-click menu edits exact real-unit base and route values, resets base without deleting routes, bypasses routes, changes polarity and voice-source reducer, removes one route, or confirms removal of every route to the target. Exact editing never invents a missing route.
- TDD coverage is 91/91 in the full React browser suite. Chromium touch injection proves rack reorder and modulation drop without scrolling; WebKit iPhone profile proves Start, playback audio session, visible held key, non-silent audio, suspension recovery, and Voice-card visibility. The exact local Sites artifact measured -2.96 dB all-eight-on gain in Chromium and -3.28 dB in WebKit, with zero over-budget worklet blocks in both.
- The final `npm run ios:ui:build`, `npm run sites:build`, fresh desktop HMR launch, and compiled desktop build passed. The installed Ableton VST3 is `/Users/winterfell/Library/Audio/Plug-Ins/VST3/CosimoDesktopNative.vst3`. TypeScript is clean after the 2026-08-07 contract repairs.
- Navigation provenance and interaction consequences are recorded in `docs/ADR-017-mobile-accordion-and-dual-ring-rack-controls.md`; ADR-016 remains authoritative for the exact faceplates, no-LFO rule, touch behavior, and Safari playback session.
- Fresh logged-out production suites pass all five applicable Chromium and all five applicable WebKit cases. Public Chromium measured -2.95 dB all-eight-on gain and 0.132 average worklet load; WebKit measured -2.88 dB and 0.250, with zero over-budget blocks and zero silent note polls in both. Host, generated engine, compiled UI, worker, and curated catalog hashes match local `dist`; recent production worker error logs are empty.

Interaction and mobile CPU stabilization — 2026-08-07
- Hidden accordion visuals no longer retain high-rate Cmajor listeners. Mobile Voice listens only to filter spectrum, FX only to the selected Filter or Distortion visual streams, and Mod only to the MSEG playhead; selecting another effect leaves all three high-rate streams detached. Suspended stream state resets, so a stale MSEG playhead cannot reappear after navigation.
- Closed rack parameter overlays no longer retain an extra parameter listener. The authoritative rack parameter catalog now exposes one frozen allocation-free endpoint index instead of rebuilding flattened arrays during interaction.
- Precision number drags end on local or window pointer-up/cancel, lost capture, window blur, document hiding, unmount, and mouse movement that reports no pressed button. Safari keyboard `touchcancel` now follows the same note-release path as `touchend`, preventing stuck notes when the OS cancels a gesture.
- Legacy `.mjs` wavetable/theme duplicates now re-export the production `.js` modules, eliminating divergent colors, projection math, and tests. The current graphite/cyan theme and inset-aware production projection are the single authority.
- Exact generated artifact verification is green in Chromium and WebKit: non-silent notes, visible touch depression/release, real rack parameter and Macro-to-rack audio deltas, `rack.v1` reload, and zero worklet overruns. Local Chromium measured 0.135 average worklet load and 46.4 MiB JS heap; WebKit measured 0.139 average load. All-eight-on gain was about -2.96 dB in both.
- Regression totals: desktop UI 93/93, shared hooks 21/21, iPhone shell 16/16, shared components 11/11, broad adapter/state/worker matrix 245/245, rack DSP 59/59, filter 31/31 Python plus 14/14 Cmajor, warp 5/5 Python plus 16/16 Cmajor. `npx tsc --noEmit`, iPhone UI, Sites/WebAssembly, and curated Sites packaging builds pass.
- Fresh logged-out Sites version 14 passes all five applicable Chromium and all five applicable WebKit cases. Public Chromium measured -3.03 dB all-eight-on gain, 0.185 average worklet load, 34.1 MiB JS heap, zero over-budget blocks, and zero silent note polls. Public WebKit measured -3.28 dB, 0.131 average load, zero over-budget blocks, and zero silent note polls. Engine, host, UI, worker, and curated catalog are byte-identical to local `dist`; recent production worker error logs are empty.

Browser hardening and payload pass — 2026-08-07
- Production UI minification reduces `patch_gui/desktop/app.js` from 3.62 MB to 2.98 MB. Sites packaging now excludes source maps and build-only source assets while retaining the runtime bank; the measured package dropped from 29.75 MB to 25.35 MB. Native source maps remain checked in for debugging but are never public.
- Browser stored-state durability is fail-open: corrupt, unavailable, private-mode, or quota-blocked storage cannot interrupt an engine write. Boot state restores once and echoed engine state is coalesced instead of writing localStorage twice. The explicit web-host asset manifest includes this new runtime module and is regression-tested to prevent a deployed import 404.
- Pointer lifetimes now survive blur, visibility changes, lost capture, cancellation, and unmount across rack modulation sources, wavetable stage positioning, MSEG morph/rate controls, the ADSR editor, filter graph, filter-range editor, and pending MSEG edits. Cancelled source drags cannot create routes and cancelled MSEG touches cannot add points. Current host callbacks are held behind stable refs so React rerenders cannot terminate a valid drag.
- Acceptance diagnostics no longer tax production audio. Worklet clocks/load math and host-wide parameter/effective-state listeners enable only under `?test`; production blocks take the direct DSP path. The generated-engine suite measured -3.05 dB all-eight gain, 0.163 average/0.375 maximum worklet load, zero overruns, about 50.2 MB JS heap, non-silent audio, real modulation deltas, and durable `rack.v1` reload. A separate production-mode run proves diagnostic block/poll/parameter counts remain zero.
- Safari's valid touch `pointermove` events with `buttons === 0` now remain live in the shared slider controller as well as rack reorder. The native iPhone wavetable stage also has one cancel-aware gesture owner for blur, visibility loss, lost capture, pointer cancellation, and unmount; cancelled horizontal swipes cannot change tables.
- Playback-session selection is an optional Safari hint rather than a start dependency: a throwing or rejected `navigator.audioSession.type` assignment cannot block Web Audio. Browser persistence now writes engine-first, so a runtime-rejected state can never be resurrected from localStorage on reload.
- The production wavetable worker is minified and protected by a parse budget. Its generated size fell from 128.3 KB to 88.8 KB (31%) while all 19 worker cases remained green.
- Current validation: desktop UI 100/100, iPhone React shell 17/17, shared hooks/components 36/36, web packaging 8/8, rack DSP 59/59, filter 14/14 Cmajor plus 31/31 Python, warp 16/16 Cmajor plus 5/5 Python, and 26/26 available native iOS packaging checks. The three simulator-only host checks now skip cleanly when no iPad simulator is installed.
- The exact curated Sites package and public Sites version 15 each pass seven applicable Chromium and seven applicable WebKit cases. The final public Chromium run measured -3.58 dB all-eight gain, 0.155 average/0.375 maximum worklet load, zero overruns, and 27.4 MB JS heap; WebKit measured -2.82 dB, 0.167 average/0.375 maximum load, zero overruns, touch playback/recovery, and mobile Voice/FX visibility. Both had zero silent note polls.
- Public engine, host, UI, and worker hashes are byte-identical to the committed `dist`; no maps, symlinks, localhost, or file URLs ship, and recent production worker error logs are empty. Remote acceptance reads the served curated factory catalog rather than comparing it with the larger local development bank.

Touch ownership and focused-entry hardening — 2026-08-07
- The approved `EffectsRackWorkspace` is now the only rack UI authority. Roughly 900 lines of unused pre-approval rack code were removed from `DesktopPatchView.tsx`, including the obsolete native range inputs that could steal the pointer from rack-card reorder gestures. This deliberately favors one exact product surface over keeping an unrendered fallback implementation in sync.
- Pointer capture remains the primary owner, but every active continuous touch path now has a window fallback when the browser rejects capture or the pointer leaves its control: rack dual-ring knobs, collapsed quick controls, the mod-amount control, precision fields, filter response/range graphs, MSEG morph/rate/editor gestures, shared and native-iPhone wavetable scrubbing, articulation range editing, and articulation audition. Window blur, visibility loss, cancellation, and unmount end each gesture exactly once; Safari touch moves with `buttons === 0` stay live.
- Focused exact-value drafts for precision controls, envelopes, and mod-matrix amounts are no longer overwritten by asynchronous host echoes. Enter commits the initiating event's newest text, closes the editor without bubbling back into reopen behavior, and invalid text falls back to the current authoritative binding.
- Browser acceptance now fails on uncaught page errors, failed requests, and HTTP error responses in addition to console errors. The exact packaged artifact has no symlinks or localhost/file references.
- Current validation: desktop responsive UI 114/114; iPhone shell 18/18; shared hooks 26/26; shared components 13/13; web packaging 8/8; rack DSP 59/59; filter 14/14 Cmajor plus 31/31 Python; warp 16/16 Cmajor plus 5/5 Python; and `npx tsc --noEmit` clean. Exact packaged Chromium passed 7 applicable cases at -3.42 dB all-eight gain, 0.129 average/0.375 maximum worklet load, zero overruns, zero silent held-note polls, and 48.6 MB JS heap. WebKit passed 7 applicable cases at -2.87 dB, 0.134 average/0.375 maximum load, zero overruns, zero silent polls, touch playback/recovery, and mobile Voice/FX visibility.
- Fresh logged-out Sites version 16 passed every remotely applicable Chromium and WebKit case; the two direct synthetic touch-injection seams remain local-artifact-only and were already green against the byte-identical build. Public Chromium measured -3.66 dB all-eight gain, 0.155 average/0.375 maximum worklet load, zero overruns, zero silent held-note polls, and 47.9 MB JS heap. Public WebKit measured -2.69 dB, 0.253 average/0.375 maximum load, zero overruns, zero silent polls, touch playback/recovery, and mobile Voice/FX visibility.
- Public host, generated Cmajor engine, compiled UI, worker, and curated catalog hashes are byte-identical to local `dist`. Public HTML differs only by Cloudflare's normal challenge bootstrap injection. Public access was verified without credentials, all exercised requests stayed below HTTP 400, browser/page/console observers stayed clean, and the latest 30-minute production worker error query returned no events.

Deterministic Tailwind scope and payload pass — 2026-08-07
- Tailwind automatic detection was scanning checked-in generated `patch_gui` bundles, so a clean build could inherit stale utility tokens from its own previous output. Both desktop and iPhone styles now disable automatic detection and explicitly scan only their authored surface plus `ui/shared`; generated bundles, `build/web`, and `dist` are outside the source set.
- A TDD source-scope contract was red before the change and green afterward. Two consecutive complete UI builds now produce identical desktop, iPhone, source-map, and worker blob hashes. The desktop bundle fell from 2,985.74 KB to 2,950.29 KB and the iPhone bundle from 1,178.89 KB to 1,077.18 KB without changing product semantics.
- Validation after the scope change: TypeScript clean; desktop responsive UI 114/114; iPhone/shared matrix 57/57; build/layout contract 23/23; exact Sites build green. Exact packaged Chromium passed seven applicable cases at -3.50 dB all-eight gain, 0.167 average/0.375 maximum worklet load, zero overruns, zero silent polls, and 49.7 MB JS heap. Exact packaged WebKit passed seven applicable cases at -3.56 dB, 0.136 average/0.375 maximum load, zero overruns, zero silent polls, non-silent touch playback, and mobile Voice/FX visibility.
- Fresh logged-out Sites version 17 passed seven applicable Chromium and seven applicable WebKit cases. Public Chromium measured -3.25 dB all-eight gain, 0.187 average/0.375 maximum worklet load, zero overruns, zero silent polls, and 36.1 MB JS heap. Public WebKit measured -3.00 dB, 0.132 average/0.375 maximum load, zero overruns, zero silent polls, non-silent touch playback, and mobile Voice/FX visibility.
- Public host, generated engine, optimized UI, worker, and curated catalog hashes match local `dist`; public HTML differs only by Cloudflare's injected challenge bootstrap. The site is public without credentials, all acceptance requests stayed below HTTP 400, all browser/page/console observers remained clean, and recent production worker error logs are empty. A fresh desktop HMR app remains live at `http://127.0.0.1:5174`; the installed Ableton VST3 was rebuilt afterward in self-contained compiled mode and passes strict code-sign verification.

Lossless rack font payload pass — 2026-08-07
- The eight approved rack wordmark fonts remain self-contained in native and web bundles but now use lossless WOFF2 transport instead of base64 TTF. No glyphs were subsetted, renamed, or redrawn; the original TTF sources remain vendored for provenance.
- FontTools comparison proves every glyph outline, advance, cmap, shaping table, and vertical metric matches the original. Chromium and WebKit render all eight real effect labels pixel-identically at 2x against the deployed TTF bundle.
- Raw rack font payload fell from 820 KB to 324 KB. The deterministic desktop bundle fell from 2,950.29 KB to 2,275.78 KB while the iPhone bundle and worker remained byte-identical. Two consecutive complete UI builds reproduced the same hashes.
- TDD source/asset coverage was red before the change and green afterward. TypeScript, the 24-case build/layout contract, and the complete 114-case responsive/touch browser suite pass.
- Exact packaged Chromium passed seven applicable cases at -3.10 dB all-eight gain, 0.189 average/0.375 maximum worklet load, zero overruns, zero silent held-note polls, and 38.3 MB JS heap. WebKit passed seven applicable cases at -3.29 dB, 0.138 average load, zero overruns, zero silent polls, touch playback/recovery, and mobile Voice/FX visibility.
- Fresh logged-out Sites version 18 passed the same seven applicable cases in each engine. Public Chromium measured -3.20 dB, 0.186 average/0.375 maximum load, zero overruns, zero silent polls, and 29.6 MB JS heap; WebKit measured -2.90 dB, 0.200 average/0.375 maximum load, zero overruns, zero silent polls, non-silent touch playback, and mobile Voice/FX visibility. Browser/page/console/request observers stayed clean.
- Version 18 is `appgprj_6a6d44eefd38819193814b5a11c6a57b~appgver_262d061db7a48191bd44d2207daeeac4`; deployment `appgdep_6a76a4c155648191a5ae0ab803b4cf62`; archive `sha256:1e991bf977f5d3ae361a56c89199587fe93c9a489bd0d72e5d407d7a9a6a1706` (24,678,400 bytes). Public host, engine, UI, worker, and curated catalog hashes match local `dist` exactly; the last 30 minutes of worker error logs are empty.
- Final desktop delivery passed: a fresh HMR server from this worktree is running at `http://127.0.0.1:5174` with the standalone app launched, and the installed `/Users/winterfell/Library/Audio/Plug-Ins/VST3/CosimoDesktopNative.vst3` was rebuilt afterward in self-contained compiled mode and passes strict code-sign verification.

Mobile modulation-routing UX implementation — completed 2026-08-08
- Source/target selection is context, not assignment. Tapping either must not create or imply a modulation route; dropping a source onto a target remains the fast explicit creation gesture.
- For a selected pair with no real route, replace the amount slider with the pair label and a touch-friendly `CREATE MAPPING +` action. That action deliberately creates the route at 0%, then reveals the authoritative amount slider.
- The amount slider may edit only an existing route. If creation is blocked by the 12-route limit, show `ROUTE LIMIT REACHED`, retain no draft amount, and leave the pair unassigned.
- The former auto-creation path and draft amount are gone. Source-first and target-first browser cases now prove selection is inert until drop or `CREATE MAPPING +`, and cap failure retains no amount or route.

Dual-ring knob contract — completed 2026-08-08
- Inner ring is the base value and uses vertical movement (up increases, down decreases). Outer ring is the selected source's modulation contribution and uses horizontal movement (left negative, right positive, zero centered).
- The outer ring depicts the parameter's real modulation travel anchored at the current base value: positive clockwise, negative counterclockwise. A real 0% route retains a source-colored presence marker; a bypassed route remains bypassed when edited and renders visibly dimmed/dashed.
- Lock direction after roughly 6–8px, highlight the active ring, and show a `BASE` or `MOD` HUD in real units. Capture touch/pointer, prevent page scroll, update audio live inside one host gesture, and release on up/cancel/lost capture/blur/visibility loss/unmount.
- Outer-ring states are distinct: no selected source hides it; selected-but-unmapped shows only a faint neutral dotted track and `NOT MAPPED · CREATE MAPPING +`; mapped adopts source color. Horizontal movement while unmapped cannot create a route or retain a draft. Creation is explicit via source drop or `CREATE MAPPING +`, and cap failures remain visibly unassigned.
- Single tap selects the target. Reset and exact entry stay in the long-press menu; do not add double-tap reset. Target-selection border and source color remain independent signals.

Mobile MSEG editor redesign — completed 2026-08-08
- The replaced 393×852 editor was objectively outside its frame: content was 36px wider than the modal, `DONE` was 10.9px beyond the viewport/34.9px beyond the frame, graph/footer were 10.9px beyond the viewport, and `LOOPING` was 14.9px beyond the frame.
- The user rejects the generic three-line `MODULATION SHAPE EDITOR` title and permanent desktop/click tutorial paragraph. Together they consume 216px before the 327px graph; title, help, footer, and gaps occupy more height than the editor itself. The graph must be the dominant instrument surface.
- The footer is not usable: Morph and Time native ranges collapse to 31.4×10px with 18px thumbs, the time value is rendered twice, `TIME IN SECONDS` is needlessly verbose, and the detached Looping control breaks the panel boundary. Header A/B targets are only 40×36px.
- Root cause is a desktop-only fixed modal (`p-6`, non-wrapping header flex, fixed 320px graph, two-column footer) plus shared `RangeField`'s fixed 88px readout column. There is no compact modal branch despite the mobile accordion.
- The modal lacks dialog semantics, inert/focus containment, and initial focus; 84 underlying controls remain tabbable. Curve points have only a 32px hit diameter and segments a 10px hit radius. The destructive tap-to-delete gesture has no visible undo in the modal, and point/segment edits have no live coordinate HUD.
- The compact modal now keeps every control inside 393×852, makes the graph dominant, uses 44px A/B/Undo/Done/Loop targets, enlarges point/segment touch geometry, traps focus, makes the background inert, and supplies coordinate HUD plus one-step Undo. Mobile layout/recovery coverage is green.

Mobile Mod Matrix list/detail redesign — completed 2026-08-08
- The replaced failure rendered the non-wrapping desktop `DesktopModMatrix`/`RouteRow` as a horizontally and vertically scrollable phone spreadsheet. The mobile surface now owns a purpose-built list/detail flow.
- Replace it with one vertically scrolling iPhone-style mapping list. Every row shows the complete relationship without horizontal motion: colored source icon/name → effect/parameter, signed amount, and active/bypassed state. The Mod accordion owns the only vertical page scroll; no nested vertical or horizontal scrollers.
- Tapping a route opens a full-width route-detail editor inside Mod with an always-available Back action. Detail owns source, destination, real-unit bipolar amount plus exact entry, polarity, applicable voice-source Max/Mean reducer, bypass, and delete. Minimum primary target is 44px; swipe may reveal Bypass/Delete but cannot be their only access path.
- All creation entries—top-level `+`, approved `CREATE MAPPING +`, and source drag/drop—feed one canonical route-creation flow. At the current limit, show an explicit count such as `12/12`, disable creation cleanly, and retain no phantom/draft amount.
- Destination selection drills down hierarchically: Voice / Global Filter / FX → effect module → parameter. Never present all roughly 48 destinations as one flat menu.
- Filtering is a primary mobile navigation mechanism. Deep-linking from a source chip auto-filters that source; target context auto-filters that target. A compact sheet supports source, effect/target, active/bypassed, and display sort by source or destination. Active filters render as removable context tokens rather than a permanent field-heavy toolbar.
- Default All Routes is the simple vertical list. Cosimo route order has no sonic meaning, so use filtering/display sorting and omit manual reordering until a semantic need exists. Keep Cosimo's visual language and approved source icons; borrow iOS list/detail/sheet behavior, not generic Settings styling.
- Preserve only today's real route model: source, target, amount, polarity, enabled state, and voice-source Max/Mean reducer. Do not fake Serum controls. Design room under a later `Advanced` section for separately authorized DSP work: per-route response curve with rise/fall smoothing, auxiliary source/inversion/curve, and live final-output visualization.
- Explicitly defer Serum-style preset locking, templates, Apply-and-Delete Macros, and expanded desktop view. Queue a separate measured engine-budget investigation before changing the 12-route ceiling; the current hot path is 16 voices × 12 routes, so capacity cannot be raised as a UI-only choice.
- Amount sliders take horizontal touch only after deliberate contact; normal page motion remains one-dimensional. Research reference is Serum 2's route-list model and official matrix documentation (manual pp. 209–214; What's New p. 15), used for interaction scope rather than copied unsupported features.

Variant-D modulation-source visual restoration — completed 2026-08-08
- The replaced Mod Bar chip was an incomplete translation: selection glow/scale applied to the square button, so iPhone Safari composited a purple slab behind MSEG. It also substituted flat SVG approximations and omitted the approved layered number, underline, and white core glow.
- Authority is `/Users/winterfell/.codex/visualizations/2026/08/01/019fbb5f-e954-7e42-abd4-9f74540a94f2/fx-rack-browser-review-fragment.html` Variant D (notably lines 1188+, 1281–1370, 1724–1729, 1862–1875) plus `assets/modulation-sources/approved-generated/*.png`. Evidence includes `/Users/winterfell/Downloads/IMG_9077.PNG`, `IMG_9078.PNG`, and `/Users/winterfell/.codex/visualizations/2026/08/08/019fdf7a-bd95-7101-b151-14bc64284388/mod-source-prototype-vs-shipped.png`.
- Exact structure: a 44px transparent, borderless, shadowless button with visible overflow contains a 36×36 layered art wrapper, approved raster artwork, and separately positioned number. Do not collapse the composition into one generic icon.
- Apply selection to circular artwork only: white core `drop-shadow(0 0 2px rgba(255,255,255,.88))`, source-colored `drop-shadow(0 0 7px var(--source-color))`, and `scale(1.08)`. Add the separate 2px source-colored underline with roughly 5px glow. The selected button itself remains explicitly transparent with no background or box shadow.
- Restore compact fixed horizontal rhythm: 36px source positions and 20px paddles, not production's 42px art, 26px paddles, or three flexible columns. Restore the generated 256px dark circular faces, gradients, fine rings, and approved 1990s-adjacent energy—no redraw or approximation.
- Preserve family identity: MSEG purple, Envelope lime, Macro orange. Continue the deliberate NO-LFO model; never revive the prototype's cyan LFO slot. If a later-approved palette/order conflicts with the old prototype, stop and surface that specific conflict rather than inventing a hybrid.
- Visual repair must preserve selection, touch drag/drop routing, second-tap deep link, route pairing/limits, 280ms page animation, supported source pages, and accessibility. Git provenance places the divergence at `a2a6ddd`; it was incomplete from initial integration.
- Deterministic DOM/CSS and byte-hash guards now cover asset identity, layered 36/44 sizing, art-level two-stage glow/scale, underline, transparent/no-shadow button, absence of a rectangular plate, family colors, and selected/unselected contrast against the approved prototype assets.

Final explicit-modulation validation — 2026-08-08
- The complete responsive React suite passes 120/120; shared hooks pass 26/26; MSEG renderer plus exact Variant-D asset guards pass 23/23; TypeScript is clean; iPhone and Sites/WebAssembly builds pass.
- Rack DSP passes 59/59, warp passes 16/16 Cmajor plus 5/5 analysis, and filter passes 14/14 Cmajor plus 31/31 stability analysis.
- Exact generated Chromium and WebKit artifacts produce non-silent notes, real rack and modulation audio deltas, durable `rack.v1` reload, and no browser/page/request failures. WebKit iPhone coverage proves click-to-start, playback-session audio, visible key depression/release, suspension recovery, and Voice-card visibility.
- Measured WebKit all-eight gain was -2.87 dB, average worklet load 0.132 with 0.375 maximum, zero over-budget blocks, and zero silent held-note polls. Two phone-sized visual reviews covered FX, unassigned mapping, Mod overview, route detail, and MSEG; the source navigation containment contract passes at both 393px and 320px.
- Consequential product choices are recorded in `docs/ADR-018-explicit-mobile-modulation-workflow.md`.

Mobile FX parameter-state truth correction — 2026-08-08
- Parameter framing is now neutral selection only; the inner ring is neutral base value; only the armed source/current-target pair owns source-colored outer geometry. Drag preview is transient and separate. Neutral exact-count badges include bypassed routes and remain independent of the armed source.
- `ui/shared/rack-route-presentation.ts` is the small shared projection for pair relationship, target topology, badge state, eligibility, effectiveness, and exact per-route travel. Display scale no longer implies DSP application: frequency targets apply octave multiplication; Global Filter Resonance, OTT Time, Flanger Rate, and Phaser Rate remain linear exactly as DSP applies them.
- Route identity is one source-target pair. Normalization and runtime mutation use deterministic stable first-route-wins handling for previously loaded duplicates; Mod Matrix Add selects the next free pair. Rack, matrix, stored state, badges, and runtime therefore agree.
- Route bypass survives live amount editing. Effect bypass has a distinct editor treatment and suspends glow without changing route objects. Selected/configured Free timing targets remain visible but suspended under Sync; Filter targets are suspended under Filter Off. Choice controls remain ineligible.
- Validation: pure modulation/presentation 18/18, complete responsive browser 127/127, shared hooks 26/26, rack DSP 59/59, TypeScript clean, iPhone production bundle built, simulator standalone built/installed/launched, and packaged `patch_gui` contains only the five approved runtime files. A rendered iPhone 16 simulator frame verified the packaged WebKit UI; the exact seeded route overlays remain covered by Chromium computed-style/geometry tests because simulator state was not seeded.
- The compiled standalone/AU/VST3 build passed and installed `/Users/winterfell/Library/Audio/Plug-Ins/VST3/CosimoDesktopNative.vst3`. Because port 5174 remains owned by active worktree `/Users/winterfell/.codex/worktrees/f8e7/cosimo-synth`, this worktree's fresh standalone HMR review app is running on isolated `http://127.0.0.1:5175`; the compiled build was rerun afterward so the installed Ableton VST3 is self-contained. Do not terminate the separate 5174 worktree implicitly.

Browser-comment correction pass — 2026-08-08
- Rack route removal now names the armed source, the mobile preset bar is restored, live Mod Bar amount gestures share the `MOD` HUD, and the Global Filter starts in Lowpass while remaining independently FX-bypassed. Seven-action parameter menus stay inside short phone viewports.
- Approved Variant-D source faces remain byte-identical; distinct fontaudio MSEG/ADSR/Macro identity glyphs now disambiguate them. The browser harness gives wavetable selection a deterministic loading-to-active lifecycle and the UI names the pending table without replacing the still-audible active title.
- Validation: responsive browser 130/130, shared hooks 26/26, shared components/wavetable 64/64, rack presentation/assets 6/6, rack DSP 59/59, and TypeScript clean. The canonical iPhone bundle built, simulator standalone built/installed/launched, and its packaged `patch_gui` contains exactly the five approved runtime files. A fresh 5175 HMR standalone is running; the compiled VST3 was rebuilt afterward and installed at `/Users/winterfell/Library/Audio/Plug-Ins/VST3/CosimoDesktopNative.vst3`.
