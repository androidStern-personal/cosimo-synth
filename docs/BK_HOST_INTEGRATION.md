# Builder Kit host cleanup queue

Authority: [approved host cleanup](../BUILDER_KIT_HOST_CLEANUP.md), Andrew via Woods on September 4, 2026. Only the specification was copied from the historical planning checkout. That branch is never merged. Andrew subsequently removed the decision-provenance skill mandate; active instructions were corrected in `d209d93c`, while historical BK-24 records remain intact.

Coordinator: Bob, task `01a06ded-e922-7b60-83a7-44332a0e3fee`, verified Astra/xhigh, unrestricted execution with approval policy never. Woods `01a03885-0e68-7411-8b3f-f69ae41b6d89` owns the later independent complete customer acceptance.

Fresh source baseline: `origin/master@1e57fd3ff75833001b66e410abee7b10a7903a08`. Coordinator branch `codex/bk-host-cleanup-integration` in `/Users/winterfell/.codex/worktrees/c57c/cosimo-synth`. The primary checkout and dirty customer playground remain untouched; the linked live tracker receives only this queue's narrow updates.

| Work | Owner and isolated checkout | Current state |
| --- | --- | --- |
| BK-13B literal human plugin name | SOL/xhigh `/root/build`; `codex/bk-13b-name-fix`, `/Users/winterfell/.codex/worktrees/bk-host-names/cosimo-synth`; Cmajor `codex/bk-13b-name-safe`, `/Users/winterfell/.codex/worktrees/bk-host-names/cmajor` | Clean Cmajor candidate `66aaabfd6fc3c97048a299d6e2a831e953364b7a`, independently source-reviewed. Final 11-case CMake/compiler matrix passes. Public JUCE setup and internal metadata stay unchanged; only compiled host display-name definition changes. Actual isolated VST3 factory names/IDs pass on all three interfaces; Ableton browser, device and editor display the configured readable name. Unicode was explicitly removed from scope by Andrew |
| BK-22A host keyboard ownership | SOL/xhigh `/root/native`; `codex/bk-22a-keyboard`, `/Users/winterfell/.codex/worktrees/bk-host-keyboard/cosimo-synth` | Final CHOC `11f7dc63d7cb78f6dbaa559fe09ade8e941c0188` is source-review clean and pushed to the existing fork. Worker qualification `6b76d810` rebased to `7d8900a78efdf06b73449118b1b2924899fb491f`; root reran 23/23 checks without skips after the rebase. Composed cmaj passed 57/57 units; isolated SeqFX VST3 built, passed pluginval and was installed under a distinct identity. Real Ableton START number-field Space passes stopped/playing/stopped with focus and value 1 preserved |
| Exact private customer candidate | SOL/xhigh `/root/guidance`; `codex/bk-host-candidate`, `/Users/winterfell/.codex/worktrees/bk-host-candidate/cosimo-synth` | Existing dry-run and short-entry APIs support an ephemeral capability and numeric-loopback delivery. Temporary adapter reviewed with final pin/hash/route checks. Final-source packaging follows source integration; no publication |
| Current host reproduction and final composition | Bob `/root` | Andrew authorized this set and all necessary app-control technologies through Woods. Original three tracks/clips are preserved; owned `BK22A Host Baseline` track hosts unchanged installed Enhancer Lite and SeqFX instances. Baseline Space failed focused SeqFX START; the final shared repair passes that exact focus path. Genuine preset text and the native rate selector retain Space, ordinary controls and a Mix drag forward it. Original three track snapshots and installed binaries remain unchanged |

## Serialization and safety

- Coordinator owns rebases, merged fork/gitlink/pin composition, source master integration/push and exact candidate handoff. Workers do not push/merge master or deploy.
- Engineering native/build/install/host qualification is complete. Bob retains the serialized native slot for final candidate tool packaging; no DAW input or HMR is delegated. Host input/install slots are released, with transport stopped and the distinct test instance available in the owned track. Each slot must be explicitly released before reassignment.
- Use existing Cmajor/CHOC repositories and ordinary dependency cache. Never edit shared CPM source or print its remote URLs: private feed capabilities can be embedded in them. Provenance uses verified repository owner/name plus exact commit IDs.
- Preserve existing installed products, CmajPlugin and associations, unsaved host sets, other worktrees/servers and all BK-24 behavior. Do not use the existing global quit/discard keyboard harness against the live session.
- One composed source/dependency/tool identity must produce the final candidate. No immutable release, customer update channel, automatic release infrastructure or public publication is authorized. Woods must receive a working private entry for the actual fixes, not the old release or stale compiler payloads.

Current native scheduling: detached engineering checkout `/Users/winterfell/.codex/worktrees/bk-host-engineering/cosimo-synth` at `b42a1098` completed normal CPM configuration with exact clean Cmajor/CHOC/JUCE pins. It holds only the uncommitted, distinct SeqFX host-test wrapper. During the large LLVM download, Bob lent a bounded two-job compiler slot to the independently authorized resident-data research task `01a06e84-5388-76d3-a085-1a06ec177c37`; that task completed its single baseline target and explicitly released. Bob completed composed cmaj units and the isolated SeqFX native build, pluginval and install; the remaining heavy phase is final candidate tools. The research remains outside this integration queue and has no host/install/master ownership here.

## Required observations and completion

Current baseline: Ableton Live 11.3.43 PID 1252; installed `EnhancerLiteBuilderKitTest.vst3` executable SHA-256 `ec72cea7d854f54b4ac86d422eab90203ba2276f5b3faf7187f748cacb116bac`, strict signature verification passed. Installed SeqFX executable SHA-256 is `e34fbb502d827761bdb2cd3b74db309d3d31da16f026acbff5d8d3714633da76`; the exact 7,363-byte CHOC `98b52fb5` router occurs twice in its universal binary, with SHA-256 `5b94be493a9f28559a7160bc6579d9d46e29c6eb5b9f52ef088a95615b9d2246`. Pre-test session/track snapshot and actual focus/transport observations are outside Git at `/Users/winterfell/.codex/visualizations/2026/09/04/01a06ded-e922-7b60-83a7-44332a0e3fee/bk-host-evidence/`.

Use `cliclick` for mouse input and System Events `key code 49` for Space, with observed AX focus and Ableton API transport readback. CUA coordinate clicks fail; `cliclick kp:space` also fails the host-focused control case and is invalid product-failure evidence. With verified System Events input, Enhancer Frequency AXSlider retains focus while transport goes stopped/playing/stopped. SeqFX START AXTextField reports number-field role, value 1 and focus true, while both Spaces leave transport stopped. Clicking Pattern 1 releases numeric focus to the Web area and restores stopped/playing/stopped behavior. Genuine PRESET NAME text accepts `BK22A Probe` with transport stopped; that dialog was cancelled without saving. No existing installed binary was replaced.

- BK-22A needs current confirmed focus/gesture failure and an isolated Ableton before/after result. Router messages/native-event proof and real transport behavior are distinct evidence.
- BK-13B needs literal spaces, apostrophes, quotes, semicolons, backslashes and interpolation-looking input handled through the owning reusable layer, with stable immutable identities. Actual generated VST3 factory and Ableton display are final evidence; tiny boundary executables alone are insufficient.
- Read source/scoped diffs, repair focused regressions, then run composed complete relevant qualification once. Keep install/host input/native builds/candidate packaging serialized.
- Final handoff records exact source/fork/tool hashes, clean worker scope and tests, host observations, private candidate/entry location, remaining acceptance and released slots. Woods performs the complete customer journey after that handoff.

## Source review findings, September 5

Real browser key input exposed an inherited dispatch-timing defect that synthetic `dispatchEvent` checks missed: a microtask scheduled in window capture can run before target/plugin handlers. The first numeric-only candidate therefore forwarded Space even when a number field subsequently called `preventDefault()`. A bare next-task decision fixed ownership but allowed a real button's default activation as well as host forwarding, so it was rejected. The accepted source appends an event-specific window-bubble listener after already-registered plugin handlers and cancels browser default before dispatch finishes. Stopped propagation initially fell back to discard; independent root/worker review found that this lost a matching host release when only key-up was stopped. Actual browser musical-key and numeric-Space regressions reproduced it. The final repair forwards an already-tracked keyup and otherwise keeps stopped events inside the plugin, with completion marked before either route. Native queue lifetime and original-event forwarding remain unchanged. A new window listener registered during that same dispatch may run too late to claim the event; independent review rates this low severity because current product code and ordinary pre-registered handlers do not use that pattern. IME composition remains a constructed browser event; the other named keyboard cases use real Playwright input.

Root composed naming `66aaabfd` plus the final CHOC gitlink into Cmajor `7820a453f25e1b6eaf898d0bb2feb7e4ce01c207`, branch `codex/bk-host-composed`, worktree `/Users/winterfell/.codex/worktrees/bk-host-composed/cmajor`. The existing canonical Cmajor and CHOC fork branches were pushed in dependency order. No fork master or Cosimo master was changed. Kit compiler pins, SeqFX provenance pins, their current notices/checklist and matching tests advance together. The focused SeqFX release/provenance checks pass 44/44. Initial runs exposed the machine default Node 22.16 versus the suite's existing approved 22.22.3 and macOS `/var` versus `/private/var` temporary-path alias. The rerun uses a privately downloaded official Node whose executable hash matches the existing approved pin and `TMPDIR=/private/tmp`; no machine default, tool policy, or test assertion was weakened. Evidence is `composed-pin-tests.tap` in the private evidence directory.

## Resolved scope decision — no JUCE or Unicode work

Andrew explicitly denied a JUCE fork and said he does not want Unicode work, relayed by Woods on September 5. Bob stopped the proposal worker immediately and instructed both owners not to continue refinement/testing or modify JUCE source, pins or delivery. Unicode is outside this queue and is not a blocker. The approved source remains the narrow Cmajor readable-name fix plus shared CHOC keyboard repair, with stock JUCE `501c07674e1ad693085a7e7c398f205c2677f5da` and the existing Cmajor/CHOC-only private candidate path. Do not reopen the fork question. The matching amendment is recorded in `BUILDER_KIT_HOST_CLEANUP.md`.

Historical Unicode factory investigation and outside-Git patch/proof files remain in `bk-host-evidence/unicode-factory-seam/`. The worker confirmed it stopped before any compilation, with no process remaining. No JUCE fork, cache modification, source pin change or dependency-delivery extension was made. The proposal is rejected and must not be used as release or customer-acceptance evidence.

## Composed engineering and Ableton qualification, September 5

Detached engineering source `b42a1098d3302021313b8c57b8f031a4bd573158` used fresh normal CPM checkouts of Cmajor `7820a453`, CHOC `11f7dc63` and unchanged JUCE `501c0767`. The compiler passed all 57 unit tests, including actual generator cases. Its executable SHA-256 is `db69a107f54f4f92ac0d40163dc8996786656d317c04681082083c74a6182dac`. The distinct wrapper uses the unchanged SeqFX DSP/UI/worker; its three untracked wrapper files stay only in the detached checkout and are excluded from integration/export.

`Cosimo SeqFX Numeric Focus Host Test` has bundle ID `dev.cosimo.seqfx-numeric-focus-host-test`, code `CsNF`, target/file `CosimoSeqFXNumericFocusHostTest`, processor CID `ABCDEF019182FAEB436F736943734E46` and controller CID `ABCDEF011234ABCD436F736943734E46`. Actual factory interfaces 1/2/3 report the exact readable name and stable IDs. The universal arm64/x86_64 VST3 passed strict signature verification and pluginval strictness 5, then the canonical selected installer installed `/Users/winterfell/Library/Audio/Plug-Ins/VST3/CosimoSeqFXNumericFocusHostTest.vst3`. Built/installed executable SHA-256 is `f887e5fa1e00e7ed99f62e56493fdc0ff6d5eb0b4cdf0a6ca9bd4a0e83b0e57d`; the exact final 8,988-byte CHOC router appears once per architecture. The actual Ableton browser URI, device name and editor title all contain the readable configured name, and PID 1252 loaded this exact installed executable.

Real System Events Space now toggles stopped/playing/stopped while SeqFX START remains focused at 1. Genuine PRESET NAME accepts `BK22A Probe` with transport stopped, then CANCEL closes it without saving. After Pattern 1 interaction, Space toggles transport; during a mouse-down Mix drag at about 0.70, it also toggles without changing the dragged value. Mix was restored through the existing host parameter API to 1.0 and verified. The focused native Sequence rate selector retains Space at 1/16 with transport stopped; after leaving it, the Space pair again toggles transport. One menu harness attempt sent Escape after the native selection had closed, closing the plugin editor and invalidating its next sample; that attempt is recorded separately and is not product-failure evidence. The editor was reopened normally.

The original three track snapshots compare exactly before/after, both original installed executable hashes are unchanged, the unsaved Untitled set remains open, and transport is stopped. The owned fourth track retains the baseline instances and distinct fixed instance for review. These observations qualify the focused host fixes, not Woods's later unchanged-example customer, saved-state/update/recovery or listening acceptance. Private evidence filenames include `seqfx-fixed-numeric-space.json`, `seqfx-fixed-text-space.json`, `seqfx-fixed-pattern-space.json`, `seqfx-fixed-mix-drag-space.json`, `seqfx-fixed-menu-space.json`, and `host-after-fixed-qualification.json` in the directory above.

Independent final scope review found no code/pin/BK-24 regression or unrelated expansion. It caught two documentation leftovers: the candidate plan called old pins current, and the active SeqFX checklist retained the removed audit mandate. Both were corrected without changing historical evidence.

## Final source gates and private delivery record

The final source gate `npm test` passed 1,228 tests with zero failures. Its one inherited skip is the locally ignored optional Spectre shelf corpus, unrelated to these fixes. `npm run typecheck` is clean. Exact router qualification remains 23/23 without skips; release/provenance remains 44/44; plain CPM remains 5/5. No implementation changed after those focused/native/host checks; the final edits record qualification and remove stale documentation.

The coordinator integrates the containing clean source revision against freshly fetched master, then runs the reviewed adapter outside Git at `/private/tmp/builder-kit-host-candidate.sh870fvh/`. Fresh canonical Cmajor/CHOC mirrors there contain the final commits and the relative CHOC URL. `candidate-summary.json` will bind the exact integrated source, private lineage, tool/archive and installer hashes; `terminal-handoff.json` will record remote readback, validation, released slots and delivery to Woods. Those files are the authoritative post-build completion record; their absence means candidate preparation has not completed. The private short-entry file is `customer-delivery/delivery.txt`; never copy its populated contents into Git or ordinary chat. The candidate is local/private, with no release, customer-channel or CmajPlugin installation change.
