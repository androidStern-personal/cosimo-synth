graph BenchIsolateChorus [[ main ]]
{
    output stream float32<2> audioOut;
    node source = wt::BenchSource;
    node fx = wt::ChorusBus;
    connection
    {
        source.out -> fx.in;
        fx.out -> audioOut;
    }
}
