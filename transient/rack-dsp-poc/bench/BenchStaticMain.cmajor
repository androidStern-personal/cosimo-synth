graph BenchStatic [[ main ]]
{
    output stream float32<2> audioOut;

    node source = wt::BenchSource;
    node rack = wt::StaticRack;

    connection
    {
        source.out -> rack.in;
        source.pitchOut -> rack.trackingPitchHzIn;
        rack.out -> audioOut;
    }
}
