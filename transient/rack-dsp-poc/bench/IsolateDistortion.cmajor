graph BenchIsolateDistortion [[ main ]]
{
    output stream float32<2> audioOut;
    node source = wt::BenchSource;
    node fx = wt::DistortionBus;
    connection
    {
        source.out -> fx.in;
        fx.out -> audioOut;
    }
}
