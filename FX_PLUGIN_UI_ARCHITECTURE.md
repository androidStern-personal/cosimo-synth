# Effect Plugin UI Architecture

This document moved. The current effect-plugin architecture reference —
discovery registry and build sidecars, the shared view loader (including the
production `devModule` strip), worker builds, generated runtime layout, and how
to add a plugin — is:

- `kit/docs/PLUGIN_ARCHITECTURE.md`

The description that used to live here predated the Builder Kit extraction (it
documented a hand-written plugin registry and an older loader algorithm) and is
preserved in git history only.
