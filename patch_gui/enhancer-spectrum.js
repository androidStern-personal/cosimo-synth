// Generated from ui/shared/enhancer-spectrum.ts by node ui/build.mjs. Do not edit this file directly.
/** The accepted logical SVG and plot rectangle for every Enhancer graph. */
export const ENHANCER_SPECTRUM_PLOT = Object.freeze({
    width: 760,
    height: 272,
    left: 42,
    right: 42,
    top: 18,
    bottom: 28,
    minimumHz: 20,
    maximumHz: 20_000,
    minimumGainDb: 0,
    maximumGainDb: 12,
    minimumLevelDbfs: -72,
    maximumLevelDbfs: 0,
});
/** Frequency ticks on the shared 20 Hz to 20 kHz logarithmic axis. */
export const ENHANCER_FREQUENCY_TICKS = Object.freeze([
    20, 50, 100, 200, 500, 1_000, 2_000, 5_000, 10_000, 20_000,
]);
/** Row-aligned relative-gain and absolute-level labels for every Enhancer graph. */
export const ENHANCER_DB_ROWS = Object.freeze([
    { gainDb: 12, levelDbfs: 0 },
    { gainDb: 9, levelDbfs: -18 },
    { gainDb: 6, levelDbfs: -36 },
    { gainDb: 3, levelDbfs: -54 },
    { gainDb: 0, levelDbfs: -72 },
]);
const spectrumPointCount = 241;
const spectrumAttackMs = 55;
const spectrumReleaseMs = 190;
const responseModelSampleRate = 48_000 * 4;
function clamp(value, minimum, maximum) {
    return Math.min(maximum, Math.max(minimum, value));
}
function isRecord(value) {
    return typeof value === "object" && value !== null && !Array.isArray(value);
}
function normalizeSpectrumMessage(message) {
    const payload = isRecord(message) && Object.hasOwn(message, "event")
        ? message.event
        : message;
    if (!isRecord(payload)) {
        return null;
    }
    const sampleRateHz = payload.sampleRateHz;
    const magnitudes = payload.magnitudes;
    if (typeof sampleRateHz !== "number"
        || !Number.isFinite(sampleRateHz)
        || sampleRateHz <= 0
        || !Array.isArray(magnitudes)
        || magnitudes.length < 8) {
        return null;
    }
    return {
        sampleRateHz,
        magnitudes: magnitudes.map((value) => (typeof value === "number" && Number.isFinite(value)
            ? Math.max(0, value)
            : 0)),
    };
}
function interpolateLogFrequency(normalized) {
    return ENHANCER_SPECTRUM_PLOT.minimumHz * Math.pow(ENHANCER_SPECTRUM_PLOT.maximumHz / ENHANCER_SPECTRUM_PLOT.minimumHz, clamp(normalized, 0, 1));
}
const spectrumRanges = Object.freeze(Array.from({ length: spectrumPointCount }, (_, index) => {
    const normalized = index / (spectrumPointCount - 1);
    const centerHz = interpolateLogFrequency(normalized);
    const previousHz = interpolateLogFrequency(Math.max(0, index - 0.5) / (spectrumPointCount - 1));
    const nextHz = interpolateLogFrequency(Math.min(spectrumPointCount - 1, index + 0.5) / (spectrumPointCount - 1));
    return {
        centerHz,
        lowHz: index === 0 ? ENHANCER_SPECTRUM_PLOT.minimumHz : previousHz,
        highHz: index === spectrumPointCount - 1
            ? ENHANCER_SPECTRUM_PLOT.maximumHz
            : nextHz,
    };
}));
function magnitudeToDbfs(magnitude) {
    return 20 * Math.log10(Math.max(1e-9, magnitude));
}
function sampleFrame(frame) {
    const binHz = frame.sampleRateHz / (frame.magnitudes.length * 2);
    const maximumBin = frame.magnitudes.length - 1;
    return spectrumRanges.map(({ lowHz, highHz }) => {
        const firstBin = clamp(Math.floor(lowHz / binHz), 0, maximumBin);
        const lastBin = clamp(Math.ceil(highHz / binHz), firstBin, maximumBin);
        let maximumMagnitude = 0;
        for (let bin = firstBin; bin <= lastBin; bin += 1) {
            maximumMagnitude = Math.max(maximumMagnitude, frame.magnitudes[bin] ?? 0);
        }
        return magnitudeToDbfs(maximumMagnitude);
    });
}
function smoothSpectrum(previous, targetDbfs, timestampMs) {
    if (previous === null || previous.magnitudesDbfs.length !== targetDbfs.length) {
        return targetDbfs;
    }
    const deltaMs = clamp(timestampMs - previous.timestampMs, 0, 1_000);
    return targetDbfs.map((target, index) => {
        const prior = previous.magnitudesDbfs[index] ?? target;
        const timeMs = target > prior ? spectrumAttackMs : spectrumReleaseMs;
        const coefficient = Math.exp(-deltaMs / timeMs);
        return target + (prior - target) * coefficient;
    });
}
function findPeakDbfs(magnitudesDbfs) {
    let peak = ENHANCER_SPECTRUM_PLOT.minimumLevelDbfs;
    for (const magnitudeDbfs of magnitudesDbfs) {
        peak = Math.max(peak, magnitudeDbfs);
    }
    return peak;
}
/** Project a frequency onto the one logarithmic X-axis shared by every graph layer. */
export function enhancerFrequencyX(frequencyHz) {
    const normalized = Math.log(clamp(frequencyHz, ENHANCER_SPECTRUM_PLOT.minimumHz, ENHANCER_SPECTRUM_PLOT.maximumHz) / ENHANCER_SPECTRUM_PLOT.minimumHz) / Math.log(ENHANCER_SPECTRUM_PLOT.maximumHz / ENHANCER_SPECTRUM_PLOT.minimumHz);
    return ENHANCER_SPECTRUM_PLOT.left
        + normalized * (ENHANCER_SPECTRUM_PLOT.width
            - ENHANCER_SPECTRUM_PLOT.left
            - ENHANCER_SPECTRUM_PLOT.right);
}
/** Invert a logical SVG X coordinate through the shared logarithmic frequency transform. */
export function enhancerFrequencyFromX(plotX) {
    const plotWidth = ENHANCER_SPECTRUM_PLOT.width
        - ENHANCER_SPECTRUM_PLOT.left
        - ENHANCER_SPECTRUM_PLOT.right;
    const normalized = clamp((plotX - ENHANCER_SPECTRUM_PLOT.left) / plotWidth, 0, 1);
    return interpolateLogFrequency(normalized);
}
/** Translate one frequency to the rendered browser X coordinate of a responsive SVG. */
export function enhancerFrequencyClientX(frequencyHz, renderedRect) {
    return renderedRect.left
        + enhancerFrequencyX(frequencyHz) / ENHANCER_SPECTRUM_PLOT.width * renderedRect.width;
}
/** Translate a browser pointer X coordinate back through the exact rendered SVG plot. */
export function enhancerFrequencyFromClientX(clientX, renderedRect) {
    if (!Number.isFinite(renderedRect.width) || renderedRect.width <= 0) {
        return ENHANCER_SPECTRUM_PLOT.minimumHz;
    }
    const plotX = (clientX - renderedRect.left)
        / renderedRect.width
        * ENHANCER_SPECTRUM_PLOT.width;
    return enhancerFrequencyFromX(plotX);
}
/**
 * Apply a relative browser drag through the rendered shared plot, preserving
 * the picked-up frequency even when a user grabs the response curve off-center.
 */
export function enhancerFrequencyAfterClientDrag(originFrequencyHz, originClientX, currentClientX, renderedRect) {
    const renderedOriginX = enhancerFrequencyClientX(originFrequencyHz, renderedRect);
    return enhancerFrequencyFromClientX(renderedOriginX + currentClientX - originClientX, renderedRect);
}
/** Format a real frequency tick with explicit Hz or kHz units. */
export function formatEnhancerFrequencyTick(frequencyHz) {
    if (frequencyHz < 1_000) {
        return `${Math.round(frequencyHz)} Hz`;
    }
    const kilohertz = frequencyHz / 1_000;
    return `${Number.isInteger(kilohertz) ? kilohertz : kilohertz.toFixed(1)} kHz`;
}
/**
 * Select readable label density for the rendered width while retaining the
 * exact frequency values and X transform of the detailed axis.
 */
export function enhancerFrequencyTicksForWidth(renderedWidth) {
    const frequencies = renderedWidth < 320
        ? [20, 200, 2_000, 20_000]
        : (renderedWidth < 480
            ? [20, 200, 1_000, 10_000, 20_000]
            : (renderedWidth < 720
                ? [20, 100, 200, 1_000, 5_000, 10_000, 20_000]
                : ENHANCER_FREQUENCY_TICKS));
    return Object.freeze(frequencies.map((frequencyHz) => ({
        frequencyHz,
        x: enhancerFrequencyX(frequencyHz),
        label: formatEnhancerFrequencyTick(frequencyHz),
    })));
}
/** Project accepted Enhancer gain onto the shared left-hand gain axis. */
export function enhancerGainY(gainDb) {
    const normalized = (clamp(gainDb, ENHANCER_SPECTRUM_PLOT.minimumGainDb, ENHANCER_SPECTRUM_PLOT.maximumGainDb) - ENHANCER_SPECTRUM_PLOT.minimumGainDb) / (ENHANCER_SPECTRUM_PLOT.maximumGainDb
        - ENHANCER_SPECTRUM_PLOT.minimumGainDb);
    return ENHANCER_SPECTRUM_PLOT.height - ENHANCER_SPECTRUM_PLOT.bottom
        - normalized * (ENHANCER_SPECTRUM_PLOT.height
            - ENHANCER_SPECTRUM_PLOT.top
            - ENHANCER_SPECTRUM_PLOT.bottom);
}
/**
 * Evaluate the accepted Enhancer bell model used by the curve renderer at one
 * frequency. Amount is normalized and maps to the accepted 0 to +12 dB range.
 */
export function enhancerBellResponseDb(frequencyHz, centerHz, q, amount) {
    const gainDb = 12 * clamp(amount, 0, 1);
    const amplitude = Math.pow(10, gainDb / 40);
    const centerOmega = 2 * Math.PI * clamp(centerHz, 20, 20_000)
        / responseModelSampleRate;
    const alpha = Math.sin(centerOmega) / (2 * clamp(q, 0.1, 10));
    const centerCosine = Math.cos(centerOmega);
    const omega = 2 * Math.PI * clamp(frequencyHz, 20, 20_000)
        / responseModelSampleRate;
    const z1Real = Math.cos(omega);
    const z1Imaginary = -Math.sin(omega);
    const z2Real = Math.cos(2 * omega);
    const z2Imaginary = -Math.sin(2 * omega);
    const numeratorReal = 1 + alpha * amplitude
        - 2 * centerCosine * z1Real
        + (1 - alpha * amplitude) * z2Real;
    const numeratorImaginary = -2 * centerCosine * z1Imaginary
        + (1 - alpha * amplitude) * z2Imaginary;
    const denominatorReal = 1 + alpha / amplitude
        - 2 * centerCosine * z1Real
        + (1 - alpha / amplitude) * z2Real;
    const denominatorImaginary = -2 * centerCosine * z1Imaginary
        + (1 - alpha / amplitude) * z2Imaginary;
    const numeratorPower = numeratorReal * numeratorReal
        + numeratorImaginary * numeratorImaginary;
    const denominatorPower = denominatorReal * denominatorReal
        + denominatorImaginary * denominatorImaginary;
    return clamp(10 * Math.log10(Math.max(numeratorPower / denominatorPower, 1e-30)), 0, 12);
}
/** Project absolute signal level onto the shared right-hand dBFS axis. */
export function enhancerLevelY(levelDbfs) {
    const normalized = (clamp(levelDbfs, ENHANCER_SPECTRUM_PLOT.minimumLevelDbfs, ENHANCER_SPECTRUM_PLOT.maximumLevelDbfs) - ENHANCER_SPECTRUM_PLOT.minimumLevelDbfs) / (ENHANCER_SPECTRUM_PLOT.maximumLevelDbfs
        - ENHANCER_SPECTRUM_PLOT.minimumLevelDbfs);
    return ENHANCER_SPECTRUM_PLOT.height - ENHANCER_SPECTRUM_PLOT.bottom
        - normalized * (ENHANCER_SPECTRUM_PLOT.height
            - ENHANCER_SPECTRUM_PLOT.top
            - ENHANCER_SPECTRUM_PLOT.bottom);
}
/**
 * Build a response path whose sampling and X coordinates are owned by the
 * shared frequency geometry; callers supply only the stage-specific Y law.
 */
export function createEnhancerFrequencyPath(yAtFrequency) {
    return spectrumRanges.map(({ centerHz }, index) => (`${index === 0 ? "M" : "L"} ${enhancerFrequencyX(centerHz).toFixed(2)} `
        + yAtFrequency(centerHz).toFixed(2))).join(" ");
}
/** Parse, smooth, and project one analyser event into the shared plot. */
export function advanceEnhancerSpectrum(message, previous, timestampMs) {
    const frame = normalizeSpectrumMessage(message);
    if (frame === null) {
        return previous;
    }
    const magnitudesDbfs = smoothSpectrum(previous, sampleFrame(frame), timestampMs);
    const path = createEnhancerFrequencyPath((frequencyHz) => {
        const index = Math.round(Math.log(frequencyHz / ENHANCER_SPECTRUM_PLOT.minimumHz)
            / Math.log(ENHANCER_SPECTRUM_PLOT.maximumHz / ENHANCER_SPECTRUM_PLOT.minimumHz)
            * (spectrumPointCount - 1));
        return enhancerLevelY(magnitudesDbfs[index] ?? ENHANCER_SPECTRUM_PLOT.minimumLevelDbfs);
    });
    return {
        path,
        peakDbfs: findPeakDbfs(magnitudesDbfs),
        magnitudesDbfs,
        timestampMs,
    };
}
