COSIMO SYNTH — PRODUCT WORKSTREAM TODO
Updated: 2026-08-25
Source: 2026-08-18 spoken workstream inventory, accepted ADR-024/feature spec, 2026-08-25 follow-up inventory, and current repository evidence.
Roadmap: `OPEN_WORK_ROADMAP.md`

HOW WE WILL USE THIS FILE

- Work from top to bottom unless a dependency or new product decision changes the order.
- Keep at most one task marked [>] IN PROGRESS.
- At the end of each task, record the evidence beside it and promote the next unblocked task.
- Put durable implementation gotchas in PROGRESS.txt; keep this file focused on what remains.
- Status: [ ] queued, [>] in progress, [?] product decision/review, [x] implementation evidence exists, [-] parked.

CURRENT FOCUS

[ ] Phone pass on the shipped mobile surface: shell feel, rail default dock position over the Voice paddle, T12 auto-preview cadence/sync tuning, and the new T05 filter card + T06 dwell feel. T05 and T06 are implementation-complete with gate evidence below. Now fully unblocked (2026-08-20): T12C (manual playing overrides Auto-preview + chord memory), T03C (active-tab re-tap scroll fix incl. compiled shadow DOM), and T02C (Index range shading on the wavetable) are implementation-complete below — the pass should also confirm held-note continuity while turning knobs, chord replay, tab re-tap scroll, and the shading feel on device.

PHASE 1 — SETTLE THE MOBILE VOICE SURFACE

[x] T01 — Finish the accepted Variant-D wavetable preservation contract
    Settled while this tracker was being prepared:
    - Variant D is the accepted production hierarchy.
    - ADR-024 and docs/MOBILE_VOICE_FOCUSED_OSCILLATOR_SPEC.md are the implementation authority.
    - Provisional graph axes are horizontal = Warp Amount and vertical = Index / Frame Position. The graph uses the numeric controls' rolling dominant-axis behavior: one value changes at a time, the active axis may switch before release, and ignored motion never becomes diagonal debt.
    - A future physical-use review may replace graph X with discrete wavetable switching. The current cutover keeps the explicit picker and must isolate X behind one graph-axis projection seam rather than shipping both behaviors.
    - At idle, Wavetable overlays the graph at top left and opens the picker; Warp Mode overlays at top right and cycles on tap. There is no permanent Frame/Index display.
    - Once graph motion classifies, the top-left selector smoothly becomes the active Warp percentage or Index value, follows any in-gesture axis switch, and returns to Wavetable on finish.
    - The real graphic fills the complete graph rectangle behind both overlays, with no reserved control inset, distortion, or dead space. The full Voice parameter strip remains below the graph as Variant D established.
    Preservation evidence (2026-08-18):
    - Pre-cutover source checkpoint: commit 28fa20db (the complete prior compact Voice surface and renderer behavior are rebuildable from it).
    - The retained renderer is provably unchanged by default: drawWavetableModel gained opt-in `paintBackground`/`showSliceCaption` seams, and tests/test_wavetable_display.mjs asserts the default draw stream (background fill, caption, artwork stroke counts) is identical with the options off leaving the artwork untouched.
    - The final visual reference is the design canvas "Voice Wavetable Prototype" (real embedded renderer), user-approved for integration.
    Done when:
    - The corner-overlay, transient-readout, rolling-axis, and frame-fill behavior are explicit.
    - The ADR/spec continue to distinguish the chosen layout from the existing renderer/graphic that must be preserved.
    - Before-state captures cover the real graphic at representative phone widths.
    Evidence:
    - Prototype: ui/desktop/voice-mobile-prototype/
    - Decision: docs/ADR-024-mobile-voice-focused-oscillator-editor.md
    - Handoff: docs/MOBILE_VOICE_FOCUSED_OSCILLATOR_SPEC.md
    - The accepted prototype remains throwaway and is not production-wired.

[x] T02 — Productionize the chosen Voice and wavetable layout
    Depends on: T01
    Closed 2026-08-18: all Done-when gates executed (see Final gates below). One parked cosmetic follow-up remains: the 320px label/value gap defect recorded under PARKED below — it is deliberately NOT a blocker for this task.
    2026-08-18 implementation status:
    - Shared modules landed in ui/shared: mobile-voice-parameter-manifest (22-control coverage test), rolling-axis-classifier (accepted calibration, one-write-per-sample, in-gesture switching), mobile-voice-rail-projection (rail truth table + aggregate Tune adapter), wavetable-graph-axis-projection (provisional X=Warp behind the replaceable seam), parameter-knob-artwork (ADR-025 dual-ring), and MobileVoiceFocusedEditor + mobile-voice-editor.css.
    - Compact DesktopPatchView now renders the focused editor inside the existing DesktopOscillatorConnectionBoundary; noncompact desktop is untouched. The compact Controls card keeps Articulations plus Play/Glide and drops the superseded four-knob and unison decks.
    - IOSPatchView consumes the same editor with a real armed-source selector; its pre-rack tabs/performance-controls/wavetable panel (~390 lines) were deleted. iPhone shell suite: 20/20.
    - Renderer seams (background fill / slice caption) default to the unchanged desktop drawing; 33/33 renderer cases including the byte-preserving default assertion.
    - Browser acceptance: adapted the compact cases to the accepted surface (same behavioral assertions) and added tabs-mute/solo, graph one-axis-at-a-time with transient readout, vertical route-amount editing under the fixed HUD, and 320-430 no-overflow cases.
    Verification (2026-08-18, second pass): full desktop browser suite 160/160 (two touch-mechanics test fixes: harness readiness probe widened to the new graph, and the mod-source chip drop approaches closed-loop to defeat Chromium touch prediction); web bundle contract 14/14; production web build green and served for physical phone review at https://primary-mac.tail5ef964.ts.net/ (HTTPS via tailscale serve — AudioWorklet needs a secure context).
    Live-review feedback batch (2026-08-18, commit d3c23284): HUD fill/contrast/no-layout-shift/no-message-strings fixes, unmapped vertical stays on base HUD, chip route-presence dots, sticky semitone route detents, toolbar scroll ownership fix + permanent scroll regression case (suite now 161 cases). Details in PROGRESS.txt and the ADR-024 second amendment.
    Mod-range repair batch (2026-08-18, commit a85512de, pushed to origin/master): amp route amounts became symmetric full-span ±54 dB offsets with engine-side base+offset domain clamping (upward Level modulation was frozen at base + 6 dB); pitch modulation presented by Semi alone on mobile AND the desktop deck (Oct/Fine base-only); HUD amounts in real units; new amount-limits contract suite + dB rail cases + two browser repros + a native-gate amp-headroom invariant. Full gates green: engine native+wasm, rack 61/61, desktop browser 163/163, iPhone 20/20, web bundle 14/14, tsc. Details in PROGRESS.txt and the ADR-024 third amendment. The tree is clean; T02B's clean-tree precondition is now satisfied once T02's remaining verification lands.
    Final gates (2026-08-18): physical-phone acceptance confirmed by the user on the tailnet build after the mod-range batch ("i already tested and it works on my phone"). Visual captures taken at 320x568, 393x852, and 430x932 (Shape + Tune pages, 2x, no horizontal overflow at any width). Compiled AU + VST3 rebuilt from this checkout with the engine amp-clamp fix and installed to ~/Library/Audio/Plug-Ins (strict codesign verify passes on both).
    PARKED (cosmetic, own follow-up): at 320px width the toolbar cell label and value can render with no gap between them (Tune page shows "OCT0oct"/"SEMI0st"; Shape shows "LVL-9.5dB"). Grid tracks and the 4px column-gap resolve correctly in a layout probe, yet the label box overflows its track, so the first-guess CSS fix (shrinkable label track + ellipsis) provably changed nothing and was reverted. Diagnose for real before fixing.
    Build:
    - Rewrite the chosen hierarchy into the real mobile Voice surface; do not copy the throwaway prototype wholesale.
    - Reuse the real WavetableStageSection drawing/data path and preserve its artwork, table loading, and selected A/B/C oscillator bindings.
    - Keep the full Voice parameter strip below the graphic according to ADR-024 and its feature spec.
    - Implement provisional graph horizontal = Warp / vertical = Index through the replaceable graph-axis seam resolved in T01, using the numeric controls' rolling one-axis-at-a-time classifier; do not switch wavetables by graph drag in this cutover.
    - Replace the large Frame/Index overlay with top-left Wavetable, top-right Warp Mode, and the transient top-left active-value treatment resolved in T01.
    - Resize and position the graphic so it fills its intended frame without distortion or dead space.
    Done when:
    - Before/after comparison proves the graphic itself was retained while its framing and controls changed.
    - Real touch edits the correct X and Y destinations and cancellation cannot leave a gesture active.
    - The selected oscillator A/B/C remains the sole target of each edit.
    - Responsive tests and visual captures pass at 320, 393, and 430 px widths.

[x] T02B — Single-source the hand-written patch_gui modules and delete the wavetable type shims
    Closed 2026-08-18 (commits 53d147c1, 746efc48, ad6d6d22, 6dbab4ed):
    - Ported in dependency order: ui/shared/theme.ts (new), ui/shared/responsive-layout.ts (new), ui/shared/wavetable-bank.ts (already the real TS twin; gained the parseWaveFile re-export the shim consumers use), ui/shared/wavetable-display.ts (full 1,256-line renderer under strict TS — no any/casts/non-null). All four are registered in ui/build.mjs and their patch_gui twins are now generated emits; theme/wavetable-bank/wavetable-display/responsive-layout .mjs files are one-line re-export shims, so every harness/test import path is unchanged.
    - Mechanical-port proof: whitespace-stripped character diff of the emitted renderer vs the retired hand-written file shows exactly one insertion (the conventional bare class-field declarations); renderer suite 33/33 including the T01 byte-preserving draw-stream assertion; the file:// renderer-harness screenshot is byte-identical before/after.
    - Corrections to this task's premises found during execution: the responsive-layout twins were byte-identical (the "diverged twin" note was stale — no behavior decision existed), and patch_gui/wavetable-mip.mjs had zero consumers anywhere (its math lives in ui/shared/wavetable-mip.ts, bundled into the workers), so it was deleted rather than re-emitted. wavetable-bank's resolvePatchResourceUrl and loadSourceWavetableFramesFromUrl also had zero consumers and died with the hand-written twin.
    - Deletions landed: ui/legacy-patch-gui.d.ts, ui/shared/legacy-wavetable-display.d.ts, patch_gui/wavetable-display.d.ts, and tsconfig's now-empty patch_gui/**/*.d.ts include; no `declare module "../../patch_gui/` remains in the repo.
    - Gates: tsc clean; layout 25/25 (the shared-renderer architecture test now asserts the generated-header direction); desktop browser 163/163; shared hooks 29/29; iPhone shell 20/20; web bundle 14/14; renderer-harness renders over file:// (see PROGRESS.txt for the browser-policy caveat and gate recipe).
    - DoD: fresh HMR standalone relaunched from this repo at 127.0.0.1:5174; final compiled AU + VST3 installed and passing strict codesign verification.
    Depends on: T02. Start only on a clean tree after T02's remaining verification lands — T02's in-flight work edits these exact files (patch_gui/wavetable-display.js, patch_gui/wavetable-display.d.ts, ui/shared/legacy-wavetable-display.d.ts).
    Engineering-debt task, sequenced here so the D04 axis review and later renderer-adjacent work inherit typed source instead of extending the shims.
    Context (verified 2026-08-18):
    - patch_gui/ is a committed output directory: ui/build.mjs emitGeneratedPatchGuiModule() transpiles ~20 modules from ui/shared into it with "Generated from ... Do not edit" headers, and Vite emits desktop/app.js(.map), index.ios.js(.map), wavetable-worker.js, wavetable-test-worker.js there. But five modules inside it are still hand-written source: theme.js, wavetable-mip.mjs, wavetable-bank.js, responsive-layout.js (plus a diverged responsive-layout.mjs twin — same length, different contents), and wavetable-display.js (~1,256 lines; per the "Path B" note in ui/shared/wavetable-energy-field.ts it owns ALL wavetable projection/warp/scan math).
    - The TS tree imports the renderer backwards: ui/shared/wavetable-display.ts is a one-line re-export of ../../patch_gui/wavetable-display.js, typed by the sibling patch_gui/wavetable-display.d.ts.
    - Three hand-maintained declarations exist for CanvasWavetableDisplay. Two are provably inert — ui/legacy-patch-gui.d.ts and ui/shared/legacy-wavetable-display.d.ts use relative-path ambient `declare module`, which is invalid TypeScript (TS2436, masked by skipLibCheck: true); a probe import confirmed the compiler never reads them. The live sibling .d.ts is wrong: it declares setFrames(frames: Float32Array[] | null), but the implementation's assertFrames has thrown on null/empty since its first commit (27697090), and it omits real public methods (invalidateStaticScene, queueRender, getStaticScene, render).
    Build (mechanical port; zero logic changes — the renderer's draw output must remain byte-identical):
    - Port each hand-written module to ui/shared TypeScript under strict (no any, no casts), in dependency order: theme.js, then wavetable-mip.mjs, then wavetable-bank.js, then responsive-layout, then wavetable-display.js. For each: create ui/shared/<name>.ts, register it in the emit list in ui/build.mjs, run node ui/build.mjs, and commit the emitted patch_gui/<name>.js in place of the hand-written file.
    - ui/shared/wavetable-display.ts stops being a re-export and becomes the real implementation; its existing importers (ui/shared/synth-components.tsx imports "./wavetable-display") must keep working unchanged.
    - Type setFrames without null (the implementation throws), and replace the shim's `unknown` theme/options/static-scene surfaces with real types derived from the implementation.
    - Before porting responsive-layout: diff responsive-layout.js against responsive-layout.mjs, enumerate every consumer of each (grep the repo excluding build/, node_modules/, .claude/worktrees/), unify on one canonical behavior in ui/shared/responsive-layout.ts, and record which side won and why in the commit message.
    - Keep patch_gui committed and clone-loadable: WavetableSynth.cmajorpatch points directly at patch_gui/wavetable-worker.js and patch_gui/desktop/index.js. Do not gitignore anything in patch_gui.
    - Keep the no-build file:// harnesses working: tools/patch_gui_harnesses/renderer-harness.mjs and mockup-massive-x-ios.html import patch_gui/wavetable-bank.mjs and patch_gui/wavetable-display.mjs (one-line export-* shims). Enumerate consumers of every patch_gui/*.mjs before deciding whether each stays a shim re-exporting the emitted .js or becomes an emitted file itself; no .mjs import path used by a harness may break.
    - Delete ui/legacy-patch-gui.d.ts, ui/shared/legacy-wavetable-display.d.ts, and patch_gui/wavetable-display.d.ts. Then remove the "patch_gui/**/*.d.ts" entry from tsconfig.json include if nothing matches it anymore.
    Done when:
    - npx tsc --noEmit is clean with all three declaration files deleted and no `declare module "../../patch_gui/` remains anywhere in the repo.
    - Every file under patch_gui/ either carries a "Generated from" header, is a Vite bundle output, or is a one-line .mjs re-export shim; no hand-written implementation remains in the directory.
    - node --test tests/test_wavetable_display.mjs passes against the emitted renderer, including the byte-preserving default draw-stream assertion from T01.
    - npm run test:desktop:ui and npm run test:shared:hooks pass; tools/patch_gui_harnesses/renderer-harness.mjs still renders over file://.
    - Common definition of done applies (fresh HMR standalone launch; rebuilt/installed VST3 unless explicitly waived).
    Do not:
    - Refactor, simplify, or reformat renderer logic while porting; any draw-behavior change is out of scope and grounds for rejecting the port.
    - Preserve the `| null` signature or widen any type to make the port compile faster.
    - Start this task while T02's uncommitted changes to these files are still in the working tree.

[x] T02C — Shade the selected Index modulation range on the wavetable graphic
    Implementation evidence (2026-08-20, Fable-implemented, tests-first):
    - Renderer: drawWavetableModel gained an opt-in modulationRange overlay ({lowPosition, highPosition, color}); in-range interpolated slices and contours mix toward the source color and surface-band skins mix by band-overlap fraction, with geometry, alphas, glow, ribs, guides, caption, and the current slice byte-identical to the untinted draw (pinned by stream-diff tests). CanvasWavetableDisplay.setModulationRange coalesces repaints and value-guards fresh-object props.
    - The range IS the Index cell rail's projected band: wavetableModulationShadingRange (mobile-voice-rail-projection.ts) returns it only for railState "mapped", null for unmapped/zero/bypassed, and throws on a mapped cell without its band. The voice editor derives it from the same presentCell("framePosition") the rail draws from (canonical live amounts included) and colors it with the armed source accent via the new theme hexToRGBColor (hexToRgbTriplet now delegates).
    - Tests: test_wavetable_display.mjs 37/37 (three new overlay stream-diff tests seen failing first; null-overlay pinned identical); test_mobile_voice_rail_projection.mjs 12/12 (truth-table test seen failing first); a rail-shard browser test proves live shading on arm, widening/narrowing/clearing on route amount-zero-bypass reseeds, unrouted-source clearing, per-oscillator scoping (A shades, B never), and re-shading on return — verified to FAIL with the graph's modulationRange prop severed. Desktop sharded suite 186/186, tsc clean.
    - Visual evidence: phone-size harness captures show the swept section source-tinted on both lines and skin with the current slice and untouched regions legible; arming an unrouted source restores the untouched artwork.
    Depends on: T02, T02B, and the selected-source color/state contract settled in T07. This renderer-specific enhancement is independently schedulable and does not block the tab-shell or Global Filter discussions.
    Product behavior:
    - Apply this to the focused mobile Voice oscillator's wavetable graphic. Look only at the modulation source currently selected in the global Mod widget and only at that exact source's route to the focused oscillator's Index / wavetable-position target.
    - When that route exists and is enabled, show its complete possible Index travel—not its instantaneous modulation value. Positive unipolar amounts span from the base Index forward, negative amounts span backward, and bipolar amounts span on both sides of the base.
    - Use the same canonical route amount, polarity, target-domain projection, and 0–100% edge clamping already used by the selected Index parameter rail. The graphic must update live while the route amount, base Index, selected source, or focused A/B/C oscillator changes.
    - If the selected source has no Index route, its route is bypassed, or its amount is 0%, draw no positive-width modulation range. Routes belonging to every other source remain absent from this graphic.
    Visual treatment:
    - Tint the visible waveform slice lines throughout the projected range with the selected modulation source's color while keeping the current-Index slice visually distinct as the current-position indicator.
    - Tint the semi-transparent surface skin connecting those slices with the same source color. Mix the color gently into the existing translucent surface faces; preserve their depth fade, slope lighting, ridge shading, and transparency rather than painting a flat or opaque overlay.
    - Leave all slices and surface faces outside the projected range unchanged, so the result reads as one softly source-colored section of the existing 3D wavetable.
    Implementation constraints:
    - Make the modulation overlay an opt-in renderer input. Preserve the renderer's established default draw stream and every non-mobile or otherwise unconnected caller unless a later product decision explicitly expands the treatment.
    - Reuse the renderer's existing slice frame indices and surface-band frame bounds; do not build a second wavetable projection or approximate route travel separately from the canonical Index range calculation.
    Done when:
    - Renderer and Voice coverage prove no-route, mapped-zero, bypassed, positive, negative, bipolar, low-edge-clipped, and high-edge-clipped behavior.
    - Switching the selected source or focused oscillator immediately moves, recolors, or removes the range without exposing another source's mapping.
    - Visual evidence confirms that both the slice lines and the translucent skin receive a restrained source-color tint while the current slice, 3D lighting, and untouched parts of the wavetable remain legible.
    - Existing default-renderer preservation tests continue to pass unchanged.

[x] T03A — Specify the persistent Voice/FX/Mod tab shell
    Depends on: T02
    Closed 2026-08-19:
    - ADR-026 accepted after product review and a production-coupling audit. It now covers visual design, shared 40 px shell geometry, immediate workspace switching, keyboard-visible/hidden placement, universal Back and compact preset actions, exact per-workspace restoration, full-screen behavior, permanent Mod-widget visibility, accessibility, implementation seams, and test obligations.
    Context:
    - ADR-024 intentionally leaves the existing accordion in place for its production cutover. This is a separate product change that will supersede the accordion portion of ADR-017.
    Decide and record:
    - Keep one compact Voice / FX / Mod tab row directly above the sticky keyboard.
    - Let the active view use the full available content area instead of reserving accordion headers.
    - Keep the keyboard and global Mod rail mounted while switching views.
    - Preserve existing deep links and Back behavior into effects, routes, and modulation sources.
    Done when:
    - A new or amended ADR defines selection, second-tap behavior, deep links, active drags, accessibility, and retained per-view presentation state.
    - The accepted Voice cutover remains independently deliverable before the shell change.
    Settled so far:
    - ADR-026 replaces only ADR-017's accordion decision with a persistent Voice / FX / Mod tab row above the sticky keyboard.
    - When the keyboard is hidden, that same tab row becomes the bottom dock and respects the device safe area; hiding the keyboard gives the released height to the active workspace.
    - Remove the current accordion's redundant top-level Voice / FX / Mod heading bars. The selected tab identifies the active primary workspace; contextual titles and Back controls inside detail pages remain a separate requirement.
    - Visual authority is the user-supplied two-item FX/MOD screenshot adapted to three equal items: one dark, restrained strip; centered uppercase VOICE / FX / MOD labels; subtle outline and dividers; and one short underline for the active item. Do not color-code the three tabs, add icons, use large active fills/glows, or introduce a floating-pill treatment.
    - Use one shared 40 px compact-shell-row token for both the simplified preset/Back bar and primary tab row. This reuses the accepted production Voice tab/toolbar and compact-control tier; do not retain the legacy preset bar's 38 px literal, copy the throwaway screenshot's 30 px row, or inherit the outgoing accordion's 44 px literal. Each tab fills the row and uses the same neutral active-underline color.
    - Workspace changes occur only by activating Voice, FX, or Mod in the tab row. Do not add horizontal page-swipe navigation or any other workspace-switching gesture.
    - Voice / FX / Mod content switches immediately with no slide or fade because those workspaces are already visually distinct. This deliberately differs from the visually similar A/B/C oscillator editors: their fixed tab row stays put while the focused editor beneath slides left or right according to letter order, as now recorded in ADR-024 and its implementation spec.
    - Use one tab-row component in both keyboard states. It stays attached to the keyboard as the keyboard shows/hides, then rests at the bottom safe area when the keyboard is gone; do not swap or cross-fade between duplicate bars.
    - Treat the tab row's top edge as the movable Mod bar's lowest usable boundary. A bottom-positioned bar sits immediately above the tabs and expands upward; a side-positioned bar cannot extend below that boundary. Neither the compact bar nor expanded drawer may cover the tabs, keyboard, or bottom safe area.
    - If the keyboard appears under a low side-positioned Mod bar, temporarily slide the bar upward only far enough to clear the raised tab boundary. Remember its user-chosen position separately and restore it when the keyboard hides; the temporary dodge must not overwrite persistence.
    - Simplify the compact synth preset bar to three visible regions matching the supplied reference: a left universal Back slot, the current preset centered, and `…` on the right. The Back control replaces workspace-local return bars such as `Back to FX`, follows the active workspace's exact return path, and disappears when that workspace has no return path while its empty slot remains reserved to keep the preset centered.
    - Use a compact back glyph in the first cut. Dynamic destination text such as `Back (FX)` or `Back (Mod – MSEG 1)` is explicitly deferred as an enhancement.
    - Move the current previous/next, Save, Save As, Revert, Copy JSON, and Paste JSON controls behind `…`; keep the centered preset name as the direct entry to the searchable preset browser, and preserve every existing operation and enabled/dirty state. Scope the composition to the compact synth shell because the preset-bar component is shared with standalone effects.
    - Keep the existing yellow unsaved-change dot beside the centered preset name. Remove the factory/user source tag and dropdown chevron from the compact row; that information remains available inside the preset browser and action popover.
    - `…` opens a compact popover anchored beneath the preset bar's right side. Use touch-sized action rows and allow the popover to scroll on unusually short screens; do not turn it into a full-screen sheet or merge it with the preset browser.
    - Remove workspace-local return bars after universal Back exists. Ordinary detail screens retain only the specific editor name needed to identify their content (for example DELAY, MSEG 1, or ENVELOPE 2); do not add a second Back control or generic FX/MOD heading.
    - In a true full-screen editor, hide the preset/Back bar, primary tabs, piano keyboard, and normal workspace content; the editor supplies its own specific title and Done/Back action. The global Mod widget is the permanent exception: it stays visible, movable, and usable above every full-screen editor so Note audition and its global controls remain available. Exiting restores the exact previous shell state.
    - Never automatically move, collapse, expand, or restore the Mod widget merely because a full-screen editor opens or closes. Keep its exact user-chosen position and expansion state; the user can move, collapse, or expand it manually over the editor as usual.
    - Switching tabs restores each workspace exactly; tapping the active tab returns a detail to its main screen, then scrolls the main screen to the top on the next tap.
    - Deep-link return paths survive temporary tab switches and end only through Back or an active-tab reset.
    - The same plugin instance retains navigation across editor close/reopen; a new plugin instance or fully relaunched standalone app starts at Home (Voice main screen, top). Background/foreground keeps the live app's place.
    - Full-screen focused editors temporarily replace the tabs; ordinary detail pages retain them. Standard tab-list accessibility and existing gesture ownership are inherited requirements rather than additional product questions.
    Closure audit 2026-08-19:
    - The first review did not specify the tab row's visual design or audit its practical integration with keyboard visibility, safe areas, detail-page navigation, the perimeter-docked Mod bar, transitions, and focused-editor geometry.
    - Those decisions were completed one at a time before ADR-026 was accepted; implementation remains separate and unauthorized.
    - Geometry audit resolved: newest production Voice tabs/toolbars, compact keyboard buttons, and the approved Mod-widget body converge on the selected 40 px shell tier; outgoing accordion/local-Back rows use 44 px; FX rack rows deliberately remain 48 px; the legacy preset bar is 38 px; and the throwaway screenshot row is 30 px.
    - Production audit resolved the remaining cutover seams: the current workspace callback erases return state on every switch; Voice scroll locking, FX drag geometry, compact styling, keyboard layout, browser tests, and Web POC helpers depend on accordion selectors; the shared preset component also serves standalone effects; and the current full-screen MSEG layer sits above the global Mod widget. ADR-026 now specifies how each dependency must change without altering unrelated behavior.

[x] T03B — Implement the persistent Voice/FX/Mod tab shell
    Depends on: T03A
    Implementation evidence (2026-08-19; gates: desktop browser 169/169, iPhone 20/20, web bundle 14/14, shared hooks 29/29, web-poc chromium 15 pass/0 fail, effect presets 113/113, units 57/57, tsc clean; web build redeployed to the tailnet URL):
    - Pure navigation model ui/shared/workspace-shell.ts (tests/test_workspace_shell.mjs, 9 cases): per-workspace detail + Back origin, active-tab-tap reset/scroll-top, deep links, universal Back, versioned session persistence starting at Home on anything unreadable. DesktopPatchView holds one WorkspaceShellState; the old mobileWorkspaceSection/mobileModSource/mobileReturnSection trio and its clear-return-on-every-switch defect are gone (the mod detail is a parsed shell detail string, so the active-tab-tap reset finally gives the deep-linked source editor a way back to the Mod overview).
    - MobileWorkspaceTabs (ui/desktop/mobile-workspace-tabs.tsx): ADR-026 visual contract (one neutral 40px strip, three equal items, short neutral underline, no workspace colors) with the roving tab-list keyboard pattern. Panels stay mounted with hidden+inert; per-workspace scrollTop is stored on switch and restored on activation (display:none drops DOM scroll state). mobile-workspace.css rewritten as the shell stylesheet; the accordion styles and the is-mobile-accordion scope died (now is-mobile-shell), FX/keyboard geometry preserved.
    - Compact preset bar: additive [compact-synth] mode on the shared cosimo-preset-bar element — reserved Back slot wired to universal Back via a composed shadow event, centered name + dirty dot, prev/next/save/save-as/revert/copy/paste behind a 44px-row "..." popover; source tag and chevron leave the compact row. The standalone-effects presentation is untouched (113/113). The 40px compact-shell-row token replaced the legacy 38px literal (host border dropped in compact so the row is exactly the token).
    - Mod rail: the tab row joined its placement boundary (binding constraint when the keyboard hides) and the portal moved above the focused-editor layer (z-60 over the z-50 MSEG backdrop) per the permanent-global-shell rule; compact MSEG editing now hides tabs + keyboard.
    - Tests: toggle selectors migrated to tabs across the desktop suite and the Web POC helper; the accordion structural test rewritten as tab-shell behavior (plus mounted-but-inert assertions); return-bar flows rewired to the shadow Back with settle waits; the old return-bar geometry and 38px assertions updated to the new contracts.
    - Found and fixed during the cutover, unrelated to the shell: the web-poc touch-drop test aimed the finger at the raw target center with no preview-amplification compensation — it had been green by geometric luck and the B2 drawer geometry (T10B) broke it; the desktop suite's compensation helper is now ported there. Its stored-state waitForFunction pattern remains unsound (an async pagefunction that can mask a missing key) — tolerable now that the drop lands, noted for T17's sweep.
    - Product observation for the physical pass: the rail's DEFAULT dock (right edge, 0.42) now overlaps the Voice toolbar's right paddle at 393x852 because the tab row shortened the travel band. Tests pre-dock the rail; on-device, decide whether the shipped default should sit lower.
    - Deliberate scope notes: sessionStorage backs per-instance presentation state (a fresh process starts at Home per the ADR; host WebView-recreation persistence needs a native seam and is recorded as a follow-up, not silently faked). Full-screen editors: tabs/keyboard hide and the rail stays usable; the editor's own focus trap keeping the rail reachable to AT was not separately exercised — verify during the physical pass.
    Build:
    - Replace MobileWorkspaceAccordion with the ADR-026 tab list and one full-height active panel; keep all three workspace trees mounted but make hidden panels inert and unavailable to assistive technology.
    - Define one shared 40 px compact-shell-row token and use it for both the synth preset/Back row and primary tab row. Preserve the deliberately separate 48 px FX rows and existing 40 px keyboard controls.
    - Consume the keyboard show/hide state supplied by the in-flight keyboard work. Do not duplicate its ownership, persistence, or transition state in the tab shell.
    - Keep the keyboard and global Mod bar mounted across switches. Do not interrupt held notes, audio, active parameter gestures, selected oscillator/effect/source, or stored musical state.
    - Preserve each workspace's exact main/detail location, Back origin, selection, and scroll position. Replace the current tab-switch path that clears mobileReturnSection; only universal Back or that workspace's active-tab reset may clear its return path. Implement standard keyboard tab-list behavior exactly as ADR-026 defines.
    - Give each tab panel one stable semantic role and one explicit content scroller. Migrate Voice gesture scroll locks, FX drag/drop visible-panel lookup, Mod-widget geometry, browser helpers, and Web POC helpers from accordion selectors to that contract.
    - Move compact FX-rack and sticky-keyboard styling off is-mobile-accordion without changing their established geometry or introducing nested short-phone scrolling.
    - Add an explicit compact-synth composition seam to the shared preset bar: universal Back at left, centered preset name/dirty dot, and the action popover at right. Keep the shared standalone-effect presentation unchanged, then remove the local Back-to-FX/Mod bar.
    - Add the tab row to the Mod widget's placement boundaries. Put the Mod widget above full-screen editors and include it in their pointer, focus, and accessibility surface without changing the user's saved position or expansion state.
    - Store navigation as plugin-instance presentation state so closing/reopening the same plugin editor restores it without putting it in presets, DAW sound state, or global preferences. Standalone relaunch and new plugin instances start at Home.
    - Keep true full-screen editors outside the persistent tab shell until Done/Back. Leave modulation-source dwell navigation for T06, but provide the tab-shell seam it needs without breaking drag ownership.
    Done when:
    - All three destinations are always discoverable without sacrificing a content-sized accordion header stack.
    - Switching views does not interrupt held notes, active audio, selected oscillator/effect/source, or stored state.
    - Short-phone layouts have one intentional content scroller and no nested accidental page scroll.
    - Browser coverage proves the 40 px rows and screenshot-directed neutral tab treatment; removal of redundant workspace titles and local Back bars; different-tab scroll/detail restore; active-tab reset; deep-link Back retention across a temporary tab visit; hidden-panel inertness; roving keyboard focus; and uninterrupted held-note/gesture ownership.
    - Workspace-switch coverage proves Voice / FX / Mod content changes immediately without a slide or fade; the separate A/B/C directional slide remains owned and tested by the Voice editor contract.
    - Layout coverage proves the same tab component attaches above the visible keyboard and docks above the safe area when hidden, with no blank strip or content overlap; a low Mod widget dodges the keyboard temporarily and restores its saved location afterward.
    - Full-screen coverage proves preset bar, tabs, keyboard, and normal content disappear while the Mod widget stays visible and usable above the editor without being moved, collapsed, expanded, or excluded from focus.
    - Preset coverage proves every displaced action remains available, the dirty dot remains visible, preset-name search still opens, and the standalone-effects preset bar is unchanged.
    - Existing accordion assertions and Web POC helpers are migrated to tab semantics; no compatibility accordion headers, aria-expanded contract, or stale visible-panel lookup remains.
    - Native/plugin-instance coverage proves editor close/reopen restores place for the same instance while a new instance and standalone relaunch start at Home.

[x] T03C — Fix re-tapping the active Voice/FX/Mod tab so it actually scrolls to the top
    Implementation evidence (2026-08-20, codex-implemented, Fable-reviewed and independently verified):
    - DesktopPatchView resolves the three compact workspace panels through a component-owned WorkspaceTabId→element ref map (ref callbacks with unmount cleanup). workspacePanelElement, scroll save/restore, AND resolveMobileVoiceScrollLocks (same shadow-DOM disease — the Voice editor's drag scroll locks were silently dead in compiled builds) all read the map; no document.querySelector crosses the shadow boundary; workspace-shell.ts untouched. Minimal diff: 10 added / 2 removed lines of product code.
    - Tests (191 added lines, zero assertions weakened): per-tab re-tap scrolls each REAL panel to 0 with a pre-tap scrollTop>0 vacuity guard; the two-step detail rule with position preserved by the first tap; switch-away restoration until an explicit re-tap; and a production shadow-root test against the compiled bundle (openBuiltDesktopBundlePage gained beforeGoto) that failed pre-fix with "scrollTop stayed at 989" — exactly the light-DOM-masking story (the dev-harness tests passed pre-fix, as expected). Desktop sharded suite 186/186, tsc clean.
    - Deviation noted in review: codex also ran the native delivery build and installed CosimoDesktopNative.vst3 into ~/Library/Audio/Plug-Ins/VST3 (outside the brief); all patch_gui bundles were rebuilt from the final settled tree afterward.
    - Remaining per spec: the compiled-app smoke on a real device folds into the CURRENT FOCUS phone pass.
    Depends on: T03B
    Origin (2026-08-19 bug found after T03B was marked complete): ADR-026 and T03B both require an active-tab tap on a workspace's main screen to scroll that workspace to the top. The handler exists, but it finds panels with document.querySelector while compiled builds mount the UI inside a shadow root, so the lookup can return nothing and the scroll never occurs. The existing unit test proves only that the navigation model requests "scroll-to-top"; no browser test proves that the real panel moves.
    Fix:
    - On the Voice, FX, or Mod main screen, tapping that already-selected bottom tab smoothly scrolls that workspace's actual content panel to the top. If it is already at the top, nothing changes.
    - Preserve the separate detail-page rule: tapping the selected tab from a detail page returns to that workspace's main screen; only a subsequent tap on the selected tab scrolls the main screen to the top.
    - Resolve the three content panels through component-owned refs or another root-scoped mechanism that works identically in the development light DOM and the compiled shadow DOM. Do not depend on document.querySelector crossing a shadow boundary.
    - Preserve different-tab scroll restoration: leaving a workspace remembers its exact position, returning restores it, and re-tapping its selected tab is the explicit action that resets it to the top.
    - Do not change tab appearance, height, placement, transitions, keyboard behavior, Back behavior, workspace persistence, or any workspace content.
    Done when:
    - A real browser interaction test scrolls each of Voice, FX, and Mod below the top, taps that workspace's selected tab, and waits for its actual panel scrollTop to reach zero.
    - Coverage proves the two-step detail behavior and proves that switching away and back still restores the previous nonzero scroll position until the user explicitly re-taps the active tab.
    - At least one production-equivalent shadow-root test exercises the same path; a development-only light-DOM test is not sufficient evidence.
    - The compiled mobile app or plugin is smoke-tested to confirm the behavior outside the development harness.

[x] T03D — Add a safe Init command to the synth preset menu
    Implementation evidence (2026-08-20, codex-implemented, Fable-reviewed and independently verified):
    - One synth-only Init menu row; a single guard choke point (requestSoundReplacement) fronts Init, Previous/Next, browser selection, and paste/import with the exact three-action dialog (Save and Init continues only after a successful save; cancel/failure leave sound, identity, and dirty state untouched); standalone effect bars are untouched and proven so.
    - Init assembles the canonical default sound at runtime from the CURRENT contract (all parameters + default modulation/articulations/rack) and applies it through the same transactional preset path (preflight, echo suppression with load generations, full rollback). The contract factory throws on any stored-state key or schema version it does not know — a new sound-owned domain can never be silently retained.
    - Adapter audit finding: rack.v1 is NOT in the synth preset adapters; Init resets it through a strict Init-only rack transaction adapter (capture/rollback/echo-dedup). Widening ordinary preset bytes to include rack state is a separate migration decision, explicitly not taken here.
    - The unnamed working sound now reads INIT in the bar (previously "No Preset") with its own dirty tracking — flagged as a small semantic unification for product review. Mid-settle input is applied-and-dirtied via generation advance, never lost or mistaken for an echo.
    - Independently verified: presets 113/113, init suites 22/22, bar browser 4/4, tsc clean; guard sever kills 8 tests; failing-first evidence recorded for all three new test files. Compiled-mobile smoke folds into the phone pass.
    Depends on: T03B and the canonical whole-synth state/default contract in ADR-021.
    Origin (2026-08-19 product requirement): the compact synth preset bar's `…` menu needs an Init action that creates a clean default sound without leaving the previously selected preset active or falsely dirty. Preset application has previously appeared dirty without a user edit, so correct clean-baseline ownership is part of this ticket, not a later polish item.
    Product behavior:
    - Add one `Init` row to the compact synth preset `…` menu. Init is a command, not a factory preset: it does not appear in the searchable preset list or participate in Previous/Next ordering. Do not add it to standalone-effect preset bars as part of this ticket.
    - If the current working sound has no unsaved changes, Init proceeds immediately. If it has unsaved changes, present exactly three paths: `Save and Init`, `Discard and Init`, and `Cancel`.
    - `Save and Init` overwrites the active user preset when that preset is writable. For a factory preset or an unnamed/Init sound, it opens the existing Save As flow. Continue to Init only after a successful save; cancelling or failing the save leaves the sound, active preset, and dirty state untouched.
    - Use the same unsaved-work guard before every command that replaces the current sound: Init, Previous, Next, choosing a preset from the browser, and pasting/importing a preset. Revert remains an explicit request to discard changes and restore the current baseline.
    - Init applies the current-version canonical new-patch sound as one complete operation, not a series of UI-control resets and not a hidden saved preset. Reset every preset-owned sound domain: the complete public parameter map (including all three wavetable selectors and all FX parameters), default modulation sources with no routes, default/empty articulations, and default rack order plus enabled/bypassed state. Audit the canonical state contract at implementation time so any additional sound-owned domain is included rather than silently retained.
    - Init must not erase or reset the user preset library, factory preset catalog, imported wavetable/assets library, Voice/FX/Mod navigation and scroll state, keyboard visibility, global Mod-widget position or expansion, Auto-preview preference, or remembered audition notes. Those are library or presentation/preferences state, not the initialized sound.
    - After Init succeeds, clear the previous active-preset identity and display `INIT` in the preset bar with no dirty indicator. The previous preset remains saved and unchanged.
    - A real user edit after Init marks the unnamed INIT working sound dirty and shows the dirty indicator beside `INIT`; it never marks the previous preset dirty. Saving an INIT sound creates a new user preset through Save As. Revert from a dirty INIT sound returns to the canonical Init defaults.
    - Keep the current established note/voice behavior used by ordinary preset changes; do not invent a separate Init-only held-note policy. If Init removes the route or articulation named by the current detail page, return to that workspace's main screen instead of leaving a dead editor open.
    Clean-load and failure contract:
    - Treat Init and ordinary preset selection as complete load transactions. Preflight the entire target before mutation; on any validation, write, or apply failure, retain the prior sound, preset identity, and dirty state rather than exposing a partial reset.
    - Parameter callbacks, structured-state notifications, asynchronous wavetable activation, rack installation, and other echoes caused by the load itself must never mark the loaded preset or INIT dirty. Establish the clean baseline only after the canonical target has settled successfully; a subsequent genuine user edit must still mark it dirty immediately.
    - User input that arrives while a load is settling must not be silently lost or mistaken for a load echo: either temporarily block that edit or apply it after completion and mark the resulting sound dirty.
    Done when:
    - Interaction tests cover clean Init and every Save and Init / Discard and Init / Cancel path, including writable user presets, factory presets, unnamed INIT state, save cancellation, and save failure.
    - A full-state assertion proves Init equals the canonical current defaults across parameters, table selection, modulation, articulations, and rack state while leaving libraries and presentation/preferences unchanged.
    - After Init, the prior preset remains byte-for-byte unchanged, active preset identity is null, the bar reads `INIT`, and the first actual edit dirties only the INIT working sound.
    - Applying Init and multiple ordinary presets remains clean after delayed and out-of-order parameter/stored-state/runtime echoes; a paired control test proves the next user edit becomes dirty. Merely loading or switching presets can never create the dirty indicator.
    - Failure injection proves there is no half-initialized sound and no mismatched sound/preset label. Compiled mobile verification confirms the compact menu, confirmation flow, and final INIT state.

[x] T04 — Decide the mobile Voice-page filter layout and Mix behavior
    Depends on: T02, T03A. The visual decision can proceed against the accepted tab-shell contract; implementation still follows T03B.
    Status 2026-08-19:
    - Behavioral review is complete, and the three remaining visual questions were decided directly (below) — no prototype. T05 implements this ticket's settled list plus these decisions.
    Final decisions (user, 2026-08-19):
    - Height split: 50/50 — the wavetable editor and the filter each get half of the Voice page's main content area (each with its own attached control row).
    - The keyboard menu button is REJECTED — no room on the keyboard row. The Play Mode/Glide popover toggle goes in the expanded Mod bar drawer instead, PROVISIONALLY (the bar is already crowded; the user will revisit placement). Supersedes the "hidden with the keyboard" rationale — the bar is always reachable. Keep the toggle labeled with the active play mode if it fits the bar's module language; otherwise surface that conflict rather than shipping an unlabeled icon.
    - Noncompact desktop Mix placement: OUT OF SCOPE for now — desktop has not been touched by this workstream and it is not a current question. The Mix parameter itself (appended endpoint, dry/wet behavior, modulation destination) remains in scope for the mobile row; only the desktop UI placement is deferred.
    Current production contract:
    - The Voice-page filter currently has only three sound controls: Mode (Off, Lowpass, Highpass, Bandpass, Notch, or Peak), Cutoff, and Resonance. The separate FX-rack module also called Global Filter is not the surface this ticket changes.
    - Mobile currently shows a large full-width graph below the focused oscillator. Dragging its handle left/right changes Cutoff and up/down changes Resonance; the top-left control cycles Mode, the top-right control cycles the analyzer drawing style, and small Cutoff/Resonance number controls sit at the bottom.
    Settled so far:
    - Keep the complete filter graph permanently visible on mobile; do not add collapsed and expanded filter states. Removing the existing mobile articulation/controls pane gives the Voice page enough space for the graph.
    - Divide the Voice page's main content between the focused wavetable editor and the filter, with each visual surface retaining its own attached control row. Determine the exact height split in the visual prototype rather than inventing it in the ticket.
    - Remove the current articulation pane and its Articulations/Voice mode switch from compact mobile layouts for this cut. Do not remove articulation state, playback behavior, preset/project data, or the noncompact desktop authoring UI.
    - Move Poly / Mono / Legato and Glide out of the removed mobile pane. A dedicated control associated with the piano keyboard opens a small menu containing those controls; settle its exact keyboard placement in the visual prototype.
    - When the keyboard is hidden, its Play Mode/Glide menu button is hidden with it. The user must restore the keyboard from the expanded Mod bar before changing those settings; do not duplicate the menu elsewhere.
    - Label the keyboard menu button with the active play mode (`POLY`, `MONO`, or `LEGATO`) rather than a generic settings icon, so the current mode remains visible without opening the menu. Glide remains inside the popout.
    - Glide affects only `MONO` and `LEGATO`. Keep it visible but disabled and grey while `POLY` is selected, using the same visual meaning for a genuinely inactive control.
    - Preserve the current, simplest Play Mode change behavior: selecting a different mode stops all currently held notes, and the user must release and press them again. Do not expand this layout work into a note-dispatcher rewrite.
    - Add a real Mix parameter to the Voice-page filter. Its absence is a product flaw, not a reason to omit it from the mobile design. This requires a new synth endpoint and audio behavior, not merely another visual readout.
    - Mix is product-wide, not mobile-only: make it visible and editable on the existing noncompact desktop Voice filter as well. Include that placement in the prototype without turning this into a desktop filter redesign.
    - Define Mix as 0% dry/unfiltered and 100% fully filtered. Existing presets and any saved state without a Mix value must resolve to 100%, because that exactly preserves how every currently active filter sounds.
    - Use a linear dry/wet blend: 50% Mix means half dry plus half filtered, with no equal-power or other midpoint loudness compensation.
    - The synth's host-parameter order is append-only because DAW automation uses those positions. The future Mix endpoint must be appended without moving Mode, Cutoff, Resonance, or any later existing parameter, even though the UI presents all four together.
    - Mix is a full modulation destination: every existing modulation source can be mapped to it to move continuously between dry and filtered. Append its modulation identity after all existing Voice destinations so their stable names and target indexes remain unchanged; rebuild internal route-cell addresses from those names rather than persisting the derived addresses.
    - Keep the filter graph interactive. Dragging its handle edits Cutoff horizontally and Resonance vertically as one two-dimensional gesture; the attached control row provides precise, one-parameter-at-a-time editing of those same values.
    - The graph always shows the filter's wet response shape at full visual strength. Mix never fades, flattens, or otherwise changes that curve; the Mix knob alone communicates how much filtered signal reaches the output.
    - Remove the analyzer-style button from compact mobile and always render the live spectrum as rounded bars there. Keep the existing Graph/Bars/Round Bars choice unchanged on noncompact desktop surfaces.
    - Keep Filter Mode as the top-left overlay on the mobile graph. Do not move it into the attached control row; that row contains Cutoff, Resonance, and Mix.
    - Tapping the Mode overlay cycles through Off, Lowpass, Highpass, Bandpass, Notch, Peak, then back to Off, matching the accepted tap-to-cycle Warp Mode interaction.
    - When Mode is Off, disable and grey the graph plus Cutoff, Resonance, and Mix because none can affect the sound. Keep the Mode overlay active so the user can turn the filter back on. This uses the already-approved grey treatment only for genuinely inactive controls.
    - Render Cutoff, Resonance, and Mix with the same shared production knob component and interaction/state contract as the other parameter rows, including value editing and modulation feedback. Do not retain the current filter-specific miniature number fields.
    Done when:
    - A visual prototype at short and tall phone heights settles the wavetable/filter height split, both attached control rows, and the keyboard-triggered Play Mode/Glide menu.
    - Mode, Cutoff, Resonance, Mix, graph gesture behavior, analyzer controls, and modulation feedback are explicit.

[x] T04A — Lock the Voice-filter Cutoff modulation-range overlay
    Depends on: T04 and the selected-source color/state contract settled in T07.
    Closed 2026-08-19:
    - This ticket changes only the Cutoff modulation-range overlay. It does not redesign or alter any other part of the already-specified Voice filter.
    Locked overlay:
    - Use the SeqFX modulation-range treatment: two Cutoff endpoints, the connected colored span, and the same draggable handle interaction for changing those endpoints.
    - Color that overlay with the modulation source currently selected in the global Mod widget.
    - Do not change the filter response, analyzer, base Cutoff/Resonance gesture, Mode overlay, Cutoff/Resonance/Mix control row, filter sound, or Voice-page layout as part of this work.
    Done when:
    - The Voice filter has the SeqFX two-endpoint Cutoff modulation overlay in the selected source's color, and every other accepted filter decision remains unchanged.

[x] T05 — Implement the mobile Voice-page filter and keyboard voice menu
    Depends on: T03B, T04, T04A
    Done when:
    - The full filter graph remains visible and live on mobile with Mode, Cutoff, Resonance, and Mix all reachable.
    - The graph never distorts to fit its container.
    - The compact articulation/controls pane is absent without deleting or corrupting articulation behavior or stored data; noncompact desktop Articulations remain unchanged.
    - Play Mode and Glide remain reachable through the accepted keyboard-associated menu.
    - Changing Play Mode retains the existing held-note release behavior; this ticket does not rewrite note dispatch.
    - The new Mix endpoint preserves all existing host parameter positions and follows the accepted default/migration behavior established during this review.
    - Add only the exact Cutoff modulation-range overlay locked in T04A without changing any other accepted filter behavior or presentation.
    Closed 2026-08-19:
    - Engine (codex on max, reviewed): `filterMix` host parameter appended last (init 1.0, rampFrames 64), per-voice linear dry/wet after the voice filter with dry captured pre-filter, and the `filterMix` modulation destination appended at voice runtime index 50 with base+offset engine clamp. Gates it ran: test:synth:engine 3/3, cmajor rack 63/63 (incl. new VoiceFilterMix.cmajtest), append-only order pinned in tests/test_patch_view_layout.mjs.
    - Voice page splits 50/50 between the wavetable editor and the filter card at every compact height (mobile-workspace.css; browser test at 852 and 700).
    - The filter card keeps the live graph + Mode chip and gains the attached Cut/Res/Mix knob row (shared ADR-025 RackParameterKnob via a new voice `modulationTargetKind` override on the knob; per-knob desktop display formatters). Off greys the graph and knobs while the Mode chip stays live.
    - The T04A Cutoff range overlay ships on the compact graph: two source-colored endpoints + band + guides (`filter-cutoff-range-*` roles), rendered only while the armed source has a Cutoff mapping; handle drags write the route amount through the canonical amount binding (browser test).
    - Play Mode/Glide live in the Mod rail drawer's voice-settings popover (T04 decision, provisional placement): mode-labeled toggle module, POLY/MONO/LEGATO radio, Glide field greyed+inert on Poly, outside-tap and drawer-collapse dismissal (browser test).
    - Legacy presets without filterMix migrate to fully wet through a contract-derived migration (ui/shared/effects/synth-preset-migrations.ts, controller `presetMigrations` function form; tests/test_synth_preset_migrations.mjs + codex's exact-contract test).
    - `filterMix` joined the canonical target catalog end to end (modulation-targets, modulation limits/steps/readout, target-descriptor voice-filter.mix descriptor; articulations deliberately do not own Mix — see T05A).
    - Worker parse budgets re-measured for the appended destination (tests/test_web_bundle_contract.mjs 2026-08-19 note).
    - Deliberate deviation: the compact knob row offers no rack context menu (value edits on the knob, modulation on its ring).
    - Resonance feel decision: the graph handle keeps the curve-lab sigmoid; the Res knob stays an ordinary log-scale knob (equal travel = equal dial fraction). A transfer-unified knob was tried on-device and rejected as lurchy — do not re-unify.
    - Domain-growth ripple (1118 -> 1131 legal pairs): benchmark profiles renamed stored-1131-active-100/active-1131 across the generator, both python runners, the iOS host budget table, and every domain-pinning test; the appended lane tails (459/204) are pinned in the worker-service and web-poc suites.
    - Gates: desktop sharded suite 173/173 (three new T05 cases + the rail popover case), iPhone shell 20/20, web bundle 14/14, web-poc chromium 15/15 AND webkit 15/15 (the webkit layout proof was stale since ADR-024 and now pins the 50/50 split), shared hooks 29/29, effect presets 113/113, node units green (identity/limits/runtime/layout 70, worker-service+adapter+install 94, preset migrations 2, shared logic 36), python benchmark tests 6+49, tsc clean.
    - Known pre-existing failure, NOT from this batch (verified against a HEAD worktree): test_shared_synth_components_browser "shared wavetable stage mounts with plain props and reports select and retry actions" times out clicking Retry Load.

[x] T05B — Filter travel overlay v2 and the one parameter-control contract
    Product decisions (2026-08-19 review of the first T04A cut):
    - The overlay adopts the full SeqFX treatment: the response curves at BOTH travel extremes (source = 0 and source = full), the swept region shaded in the source color, and 2D endpoint handles whose Y edits resonance through the same curve-lab transfer as the base handle.
    - Both filter axes ride ONE source: dropping a source on the filter graph creates the Cutoff + Resonance pair at 0% in that single explicit gesture (knob drops keep mapping only their own destination); each travel axis is live only through its own route, and an axis without one stays pinned at base.
    - Handles (revised in the same review): NO connector arrow — hollow start vs filled end is the only direction cue. Endpoints drag fully independently: the other endpoint stays planted on screen, so endpoint drags co-rewrite base and amount (geometric midpoint for octave cutoff amounts, arithmetic for linear Q offsets); host cutoff/Q gestures bracket these drags. A diamond center grip translates the whole travel rigidly in SCREEN space (both endpoints ride one pixel delta, base + amounts re-derived; parameter-space translate felt broken on the nonlinear Q display) whenever a routed axis is unipolar; with bipolar-only travel the base handle IS the center and keeps plain translate. In unipolar the base handle IS the start (its drags pin the end and compensate the amount). 0% endpoints park as dotted tabs beside base (ADR-025 zero language).
    - EVERY knob joins the voice readout cells' drag contract: horizontal = base, vertical = modulation amount, rolling dominant-axis switching, inert-vertical-when-unmapped (the "CREATE MAPPING +" drag hint is retired), long-press = menu, and the fixed top-center ADR-025 precision HUD instead of the pointer-chasing popover. The popover HUD remains only for the rack's modulation amount slider (not a knob; future convergence candidate).
    Closed 2026-08-19:
    - One shared gesture controller (ui/shared/parameter-gesture.ts) now owns classification, rAF-batched writes, scroll locks, long-press, and every cancellation channel; the voice editor, wavetable graph, and ParameterKnobSurface (all Rack + Base knobs, FX rack included) consume it, so control families can no longer drift.
    - One shared HUD (ui/shared/parameter-hud.tsx): the ParameterPrecisionHud component plus a page-level HUD layer context provided by both patch views (desktop viewports included); knobs pass their owner accent (effect accents on the rack, violet on the voice filter).
    - Travel overlay v2 in FilterResponseGraph (filter-travel-* roles) with the compact FilterSection mapping endpoint drags onto both route amounts; graph pair-drop via a companion-destination attribute on the drop surface flowing through one widened onSourceDrop seam.
    - Behavioral tests: five travel cases (two-axis end-handle edit, inert-axis honesty, independent bipolar endpoints with base following the midpoint, unipolar center-grip translate vs base-pins-end, pair-drop at 0% with parked tab), knob-contract migrations across the rail/fx shards (axis flips, shared-HUD roles, capture-loss survival, inert unmapped vertical), and the HUD-collision case now runs against the fixed HUD.
    - Gates: desktop sharded suite 180/180, iPhone shell 20/20, web bundle 14/14, web-poc routing 34/34 + chromium 15/15 + webkit 15/15, shared hooks 29/29, shared logic 36/36, tsc clean. The shared-components "wavetable stage Retry Load" failure remains pre-existing (verified at HEAD earlier today).

[x] T05C — Fix the Voice Play Mode/Glide popover disappearing when touched
    Implementation evidence (2026-08-20, codex-implemented from a confirmed-diagnosis brief, Fable-reviewed):
    - Root cause as diagnosed: the popover's window-level outside-pointer handler tested event.target containment; the compiled shadow root retargets window events to the host element, so every inside touch read as outside. Fix: membership of the popover/toggle refs in event.composedPath() (effects-rack-workspace.tsx, the one handler block — nothing else touched). Genuine outside touches and drawer collapse still dismiss; Poly-disables-Glide untouched.
    - New production-equivalent regression: tests/test_voice_popover_compiled_browser.mjs — compiled bundle, 393x852, real CDP touch input; selects Mono without closing (playMode=1 host message), operates the enabled Glide (glideTime message), then proves outside-touch and drawer-collapse dismissal. RED first against the pre-fix bundle ("false !== true" on the Mono-stays-open assert), GREEN after the fix + rebuild, re-verified green by the reviewer locally. Light-DOM popover coverage stays green (not the proof).
    - Remaining: physical iPhone confirmation of the photographed failure rides the next phone pass.

    Origin (2026-08-20 physical-use report):
    - The expanded Mod bar's Voice popover opens and displays Poly, Mono, Legato, and Glide, but touching a control makes the entire popover disappear before the control responds. The Mod bar itself remains expanded.
    Confirmed diagnosis:
    - The popover's window-level outside-pointer handler checks only `event.target`. In the compiled product, the React UI lives inside `cosimo-desktop-react-view`'s shadow root, so the browser retargets that window event to the outer custom element. The handler therefore mistakes an inside touch for an outside touch and closes the popover on pointer-down.
    - A production-equivalent compiled shadow-root touch repro fails deterministically. The existing light-DOM browser test passes, which is why the shipped test did not catch the real product failure. The event's composed path correctly contains the touched mode button and the popover.
    Scope:
    - Make inside/outside detection shadow-root-safe so touches on Poly, Mono, Legato, the enabled Glide control, and the popover's own padding remain inside interactions.
    - Preserve intentional dismissal when the user touches genuinely outside the popover or collapses the Mod bar drawer.
    - Preserve the existing Voice behavior: Poly keeps Glide visibly disabled; selecting Mono or Legato enables it; this bug fix does not redesign or relocate the popover.
    Done when:
    - A production-equivalent compiled shadow-root browser regression at phone width uses a real pointer/touch sequence, selects Mono without closing the popover, verifies the play-mode parameter changed, and then operates the enabled Glide control.
    - The same regression proves a genuine outside touch and drawer collapse still dismiss the popover.
    - The existing light-DOM coverage remains green but is not accepted as the sole proof.
    - A physical iPhone check confirms the photographed failure no longer reproduces.

[-] T05A — Find a future mobile home for Articulations
    Parked 2026-08-19:
    - The current mobile articulation pane is deliberately removed to make Voice an instrument surface divided between the wavetable and filter.
    - This task will later redesign mobile articulation authoring and access. It must reuse the existing articulation model and stored data rather than treating removal of the pane as removal of the feature.
    - Decide in that future articulation review whether Filter Mix joins the existing per-articulation Mode, Cutoff, and Resonance overrides; do not expand the articulation schema as an incidental part of the mobile filter layout change.

PHASE 2 — MAKE DRAGGING AND PARAMETER FEEDBACK COHERENT

[x] T06 — Add dwell-to-navigate during an active modulation-source drag
    Depends on: T03B
    Build:
    - Hovering over Voice, FX, or Mod during a source drag switches to that view after a deliberate dwell.
    - Hovering over an effect in the mobile rack selects that effect after a deliberate dwell.
    - Keep the drag alive through navigation so the source can be dropped on the newly revealed target.
    - Audit other navigation surfaces only after tabs and rack rows work; add only surfaces with a real drop destination behind them.
    Done when:
    - Transit across a tab/effect does not switch accidentally.
    - Leaving before the dwell cancels the pending switch.
    - The drag preview, edge autoscroll, target highlighting, cancellation, and final drop keep one owner throughout.
    Closed 2026-08-19:
    - The dwell tracker lives inside useModSourceDrag (the one gesture owner): navigation surfaces declare `data-drag-dwell` keys, the tracker re-arms a 550ms timer per surface using the same drag point as drop targeting, and gesture end/cancel clears it.
    - Surfaces shipped after the audit, each with real drop destinations behind it: the Voice/FX/Mod workspace tabs (activateWorkspaceTab), mobile rack effect rows (selectEffect), and the oscillator A/B/C tabs (selectOscillator).
    - Rack keys resolve inside the rack workspace; everything else delegates upward through one `onDragDwellNavigate` seam handled in DesktopPatchView.
    - Browser test drives one mouse gesture through oscillator-tab dwell, tab transit without switching, cancelled dwell, FX-tab dwell, rack-row dwell to Reverb, and a final drop that creates the env-1 -> rack.reverbSize mapping.

[x] T07 — Lock the visual truth table for parameters and successful mapping
    Review:
    - Compare the current neutral base/selection treatment with the proposed effect-colored base treatment.
    - Define every state once: idle, selected, unmapped source armed, mapped pair, bypassed route, invalid target, hovered target, and successful drop.
    - Identify where unintended white is still appearing in the real Voice, FX, and Mod paths.
    - Decide whether a successful new route starts with a small non-zero amount or keeps the existing explicit amount contract and uses unmistakable transient confirmation.
    Starting recommendation to test:
    - Keep base/selection neutral, reserve source color for real modulation relationships, and add explicit success feedback. A sound-changing default amount should be chosen only after an audition test proves it is preferable.
    Done when:
    - One state matrix and one successful-drop behavior are product-approved.
    - Color never implies a mapping, effectiveness, or sound change that does not exist.
    Closed 2026-08-18:
    - Approved the real-knob state board in `ui/desktop/parameter-color-prototype/`.
    - Active/default/selected controls keep their owning Voice, effect, or source-editor color; grey is reserved for inactive, bypassed, unavailable, or invalid states.
    - A new mapping remains at exactly 0%: the modulation-amount arc's full travel is drawn as a source-colored dotted outline, none of the amount is drawn as a solid arc at 0%, and one solid source-colored point proves the mapping exists.
    - Pending, confirmed success, duplicate hover, failure, live amount editing, bypass, and deletion now have explicit product behavior in ADR-025.
    - ADR-025 records the complete matrix, the successful-drop contract, and the current Voice/FX/Mod neutral-state audit that the implementation ticket inherits.

[x] T08 — Apply the approved parameter/mapping feedback across Voice, FX, and Mod
    Implementation evidence (2026-08-20, Fable-implemented from a cited per-row audit of ADR-025, tests-first):
    - Identity colors: the knob's hardcoded neutral inline accent is gone — the inside carries the OWNING color (effect accents, Voice teal for the desktop oscillator knobs, Voice-filter violet), selection brightens the owning accent, base editing glows in it (Voice cells included), the unmapped dotted ring and capture outlines are pure source color (white mixes removed), a bypassed mapping's ring is grey+dashed, and an effect-bypassed/mode-suspended control goes wholly grey — including at boot, where every rack effect honestly starts bypassed.
    - Creation feedback: duplicates are never droppable (no highlight, no positive haptic, greyscale during the drag, release inert, one deliberate 500 ms-hover warning per target per drag: heavy buzz + top-of-screen DUPLICATE toast); failures raise a top MAPPING NOT CREATED toast + a sharper rigid buzz with no phantom ring/count/row; authoritative confirmation flashes the target, floats a rising source-colored checkmark, ticks one light haptic, and pulses BOTH the Mod rail count and the new 0% matrix row; pending creations pulse their outline and reject further drops. The toast is the app's first shared top-center report layer (portaled into the HUD layer); duplicate/failure only — success never toasts.
    - Matrix rows: amounts wear their route's actual source color, polarity renders truthfully (bipolar ±), bypass keeps the row's identity with a grey amount + BYPASSED label instead of dimming the row, and the rail count is unchanged by bypass. Voice filter knobs gained the same route-count badge as FX; ring state changes settle with a quick fade.
    - Tests: identity-color computed-style test, duplicate/failure test (failure injected via a same-tick document race that makes the bridge refuse the commit), and the cross-surface journey test (drop → confirm choreography → both pulses → 0% → bypass truth-table → delete, no toast) — each verified to FAIL with its mechanism severed. Seven pre-ADR pins migrated faithfully (neutral frames/rings, hardcoded-unipolar row readout, "one haptic" logs now containing the mandated confirmation tick, fade settle). Full gates: desktop sharded 194/194, hooks 34/34, iPhone 20/20, presets 113/113 + init 22/22, web bundle 14/14, web-poc 34+15+15, tsc clean.
    Depends on: T02, T07
    Build:
    - Do not modify the ongoing Voice refactor. Begin only after T02 has landed, then use the shared parameter-knob abstraction that the finished Voice surface consumes.
    - Remove visual states that contradict the approved truth table.
    - Retain the stationary white reset-position marker. At mapped 0%, draw the modulation-amount arc's full travel as a source-colored dotted outline, draw none of the amount as a solid arc, and show one solid source-colored route-presence point.
    - Treat an already-mapped source/target pair as unavailable: no eligibility or hover highlight and no accepted drop. After 500 ms of uninterrupted direct hover, emit one deliberately noticeable warning buzz and one top-screen `DUPLICATE` toast per target per drag; leaving sooner cancels both.
    - After release on a valid target, pulse the source-colored outline and reject further drops while creation is unconfirmed. Do not show a route point, amount arc, count change, or matrix/list row yet.
    - On authoritative confirmation only, create the route at exactly 0%, flash the target, emit a small source-colored checkmark that rises and fades, and give one light haptic tick. Briefly pulse the Mod rail count and new 0% matrix/list row in the same source color. Do not show a success toast.
    - On rejection or timeout, show a top-screen Sonner-style `MAPPING NOT CREATED` toast and a warning buzz shorter than the duplicate buzz. Restore the prior target appearance and retain no route point, amount arc, count change, or matrix/list row.
    - Keep existing routes truthful: live amount edits update the knob's solid outside arc and the matrix/list percentage together; bypass greys and dashes only the outside route ring, retains a grey `BYPASSED` matrix/list row, and leaves the Mod rail count unchanged; deletion removes the ring, row, and count contribution with a quick fade and no toast.
    - When several mappings exist, show their total in the count badge. With no source selected, show no outside ring; with a source selected, show only that source's outside ring.
    - Preserve speed-sensitive knob dragging only as a deferred experiment; do not implement it in this ticket.
    Done when:
    - One cross-surface test follows a route from source drag through pending confirmation, target flash/checkmark, Mod rail count pulse, matrix/list row pulse, live amount edit, bypass, and deletion.
    - Duplicate coverage proves the target never looks droppable, flyover shorter than 500 ms stays silent, deliberate hover reports once, and release creates nothing.
    - Failure coverage proves the global toast and warning buzz occur without leaving a phantom ring, count, or matrix/list row.
    - Voice and FX controls describe equivalent modulation states with the same visual language.

[x] T08A — Restore the active drop-target highlight during modulation-source drags
    Implementation evidence (2026-08-20, Fable, tests-first):
    - Cause exactly as diagnosed: T08's eligibility rule gained two :not([data-drag-creation]) qualifiers, making it (0,5,0) against the capture rule's (0,4,0) — the thin outline won on the hovered target, so the computed treatment never changed. Fix: the capture rule in ui/desktop/styles.css now carries the same :not() qualifiers ((0,6,0) — wins, and structurally bars existing/ineligible pairs from ever taking the strong treatment). The never-winning weaker duplicate in effects-rack-workspace.css was deleted.
    - New phone-width production-equivalent regression in the fx shard: real CDP touch drag (env-1 over Reverb), comparing COMPUTED box-shadows — eligible vs captured must differ (RED first with byte-identical shadows, the exact shipped symptom), capture transfer restores the thin outline, and after a real drop makes the pair EXISTING, hovering it never changes its treatment. Note: capture is sticky (one target always holds the claim), so each target's eligible baseline is read while the other holds capture; treatments fade ~140ms, so every shadow read polls to a settled value.
    - Voice surface checked: the filter stage's hover ring is a ::after pseudo-element (never affected); the corrected rule is global, so Voice and FX targets share one treatment.
    - Physical-phone confirmation rides the next pass.
    Origin (2026-08-20 physical-use report):
    - After T08, dragging a modulation source still marks valid controls as available, but moving over one valid control produces no visible change. The user therefore cannot tell which target will receive the mapping on release.
    Confirmed diagnosis:
    - The drag controller still identifies the correct target, adds `is-mod-hover`, and supplies the dragged source color.
    - T08 added duplicate/ineligible exclusions to the always-on eligibility selector. Those exclusions made that selector more specific than the hover selector, so its thin available-target outline continues to win after the target is hovered. A phone-width browser repro confirms that the computed box-shadow is identical before and during hover; a page-only selector-specificity correction restores the stronger outline. T09 is not involved.
    Scope:
    - Restore ADR-025's existing distinction: every valid target gets a thin source-colored eligibility outline, while the one target that would receive the drop gets an unmistakably stronger source-colored outline/glow.
    - Leaving that target returns it to the thin eligibility outline. Duplicate and ineligible targets remain unavailable and never acquire the stronger highlight.
    - Apply the correction consistently to the existing Voice and FX drop targets without changing hit geometry, drag ownership, route creation, haptics, duplicate dwell/toast behavior, pending confirmation, or successful-drop feedback.
    Done when:
    - A production-equivalent phone-width source-drag test compares the eligible and hovered target's rendered/computed visual treatments and proves they differ; checking only for `is-mod-hover` or a non-empty shadow is insufficient.
    - The same test proves leaving restores the thin outline and a duplicate/ineligible target never gains the stronger treatment.
    - Physical-phone review confirms the intended landing target is immediately obvious while dragging.

[x] T09 — Finish the shared mobile parameter interaction language
    Implementation evidence (2026-08-20, Fable-implemented, tests-first):
    - Settled cleanup done: the FX selected-route AMOUNT slider is deleted along with its dead HUD-overlay plumbing and CSS; the target knob, its ring, and the shared HUD own route amounts everywhere. The three tests that pinned the slider were migrated onto knob drives with their runtime assertions intact (Max-reducer compile, small amount-update path, zero-depth prefix rule).
    - The rack's globalFilterResonance knob joined the dial-walk contract via a descriptor-level modulationDragStyle opt-in (a 40 px vertical drag used to bank −3.5 of dead amount past the audible floor; verified failing-first, now walks the dial like the voice Res knob).
    - Inventory disposition per the ticket's own audit: remaining one-off controls are all superseded by T13/T14/T15 replacements or are direct geometry manipulation (deliberately exempt); no unexplained one-off remains. Exact-value entry unification is T19 (ADR-028).
    Build from the existing baseline rather than replacing it:
    - Use compact numeric readout strips/chips for dense values and knobs for controls that benefit from a larger gesture surface.
    - Show a finger-clear HUD for every continuous touch edit that cannot display its live value unobstructed.
    - Give knobs and numeric readouts a long-press menu with exact value entry and applicable reset/modulation actions.
    - Give discrete parameters discrete snapping and best-effort haptic detents; keep continuous parameters smooth.
    First step:
    - Inventory production controls and list only the gaps. FX dual-ring knobs, oscillator Octave/Semitone detents, exact-value menus, and the collision-aware Mod HUD already have implementation evidence.
    Current production inventory (2026-08-19, read-only audit):
    - The shared `useParameterGesture` path currently has two production control-family consumers: `MobileVoiceEditor` (its numeric readouts and wavetable graph) and `ParameterKnobSurface` through `RackParameterKnob` (FX knobs plus the mobile Voice-filter knobs). Those surfaces already share rolling axis selection, horizontal Base / vertical Mod, the fixed precision HUD, long press, gesture batching, and terminal cancellation.
    - Several older direct parameter controls still bypass that path: the compact Mod source editor's custom MSEG Rate field and Morph rail, its Envelope text fields, its Macro range control, the full MSEG editor's Morph/Time ranges, the mobile Mod Matrix amount range, the FX workspace's selected-route amount slider, and multiple native-iOS range controls for Glide, MSEG, Envelope, route, and effect parameters.
    - T13/T14 supersede the compact Mod-source controls and T15 supersedes the current mobile Mod Matrix detail editor. Do not separately polish obsolete versions immediately before those replacements; verify the replacement controls against this shared contract instead.
    - Direct manipulation of geometry is not a parameter-control inconsistency: MSEG points, Envelope curve handles, the Filter graph's two-dimensional handles, and articulation boundaries retain the interaction dictated by their graphics. They still need clean transaction/cancellation ownership, but must not be forced into horizontal-Base / vertical-Mod steering.
    Settled cleanup:
    - Remove the FX workspace's separate selected-route `AMOUNT` slider. It is redundant now that the target knob itself edits its normal value with horizontal movement and the selected source's influence with vertical movement. Do not replace the slider with another route-amount control in FX; the knob, its modulation ring, and the shared drag HUD own that job.
    Done when:
    - The inventory has no unexplained one-off gesture or value-display pattern.
    - Pointer/touch cancellation, blur, visibility loss, and unmount end every gesture exactly once.

PHASE 3 — KEEP THE SYNTH AUDITIONABLE EVERYWHERE

[x] T10 — Prototype the perimeter-docked Mod bar and universal audition controls
    Closed 2026-08-19:
    - Design approved: "B2 · Linear Glass · Shouldered" on the "Mod Bar Directions" design canvas (https://claude.ai/code/artifact/0f7bc737-a855-446a-b33b-62b9261979cf). 40 px bar, 28 px modules on a 10 px rhythm; route count is a corner badge on the selected-source module, live activity is the thin bar inside it, the Note key carries the Auto-preview dot; the expanded drawer adds ENV/MAC, page dots, and the Keyboard + Auto-preview modules; tab-with-shoulders silhouette in the Linear Glass material with 12 px of clear space inside the body beyond each shoulder.
    - Product decision (2026-08-19): no behavioral prototype. Edge movement, gesture ownership, and expansion behavior are implementation requirements and moved to T10B; the width sweep and the one-/two-handed edge review are dropped as separate work items.
    - The default-pitch decision is recorded below (middle C / MIDI 60).
    Purpose:
    - Redesign the existing mobile Mod bar as a slimmer, cleaner edge widget before adding audition controls to production.
    - Use React Grab's small movable edge widget as the interaction and finish reference, without copying its branding or exact artwork.
    - Keep manual audition reachable from every Voice, FX, Mod, route-detail, and expanded modulation-editor state.
    Current problems the prototype must solve:
    - The compact production bar is 56 px wide and 168 px tall, with a 44 px selected-source control; it feels bulky beside the newer mobile surface.
    - Source icons and their surrounding spacing are oversized.
    - The selected source's glow is clipped by a parent with hidden overflow.
    - The current composition lacks the visual hierarchy and spacing expected of a polished permanent edge control.
    Prototype requirements:
    - Let the complete Mod bar move around the screen perimeter like the React Grab widget, settle cleanly against an edge, respect safe areas, and expand inward from its current edge. Preserve its chosen perimeter position between uses.
    - Make the compact bar meaningfully slimmer than the current 56 px treatment. Shrink the source icons, establish deliberate spacing, and render the active-source glow without clipping.
    - Preserve the compact bar's existing selected-source identity, route count, activity, disclosure, source-drag behavior, and vertical repositioning responsibilities while finding a cleaner composition.
    - Add exactly one new compact control: a Note button. Do not put Keyboard or Auto-preview controls in the compact bar.
    - The compact Note button behaves like one piano key fixed to the most recently played pitch: touch/pointer down starts the note; release, cancellation, suspension, or loss of ownership stops it; touching again retriggers it.
    - Update the remembered pitch from intentional notes played on the on-screen keyboard or a connected MIDI keyboard, not DAW/transport playback. Before any intentional note has been played, use middle C (MIDI note 60).
    - Put the Keyboard visibility control and the Auto-preview toggle in the expanded Mod bar alongside the modulation sources. Keyboard toggles the bottom keyboard; Auto-preview is an explicit visible mode, not a hidden gesture.
    - When Auto-preview is active and the bar is compact, show a small lit status dot on the Note button without adding another compact control.
    - Show compact and expanded treatments at representative 320–430 px phone widths and at enough perimeter positions to resolve side, corner, orientation, and inward-expansion behavior visually.
    - Keep this prototype throwaway. Do not wire it into the production Mod bar or disturb the ongoing Voice implementation.
    Done when:
    - One compact and expanded design is product-approved as clean, well-spaced, meaningfully slimmer, and visually consistent with the current mobile synth.
    - Edge movement, snapping/settling, safe-area behavior, inward expansion, persistence, and interaction ownership are explicit.
    - The approved design proves that source dragging, bar movement, disclosure, Note-button playing, Keyboard visibility, and Auto-preview access do not compete for the same touch target.
    - One-handed and two-handed use are reviewed with the bar on more than one screen edge.

[x] T10B — Replace the production Mod bar with the approved B2 design
    Depends on: T10
    Implementation evidence (2026-08-19):
    - Production MobileGlobalModRail rewritten to B2 in ui/desktop/effects-rack-workspace.tsx with new ui/desktop/mobile-mod-rail.css (rail styles split out of mobile-workspace.css); the old 56 px treatment is gone. Pure geometry (bounds, projection, edge settling, three-anchor snapping, drawer placement, dock persistence codec, both-edge silhouette paths) lives in ui/shared/mod-rail-perimeter.ts under tests/test_mod_rail_perimeter.mjs (property-based, 9 cases).
    - Left/right edge docking with FLIP settle animation; dock persisted as v2 {edge, normalizedY} under the existing storage key with v1 migration to the right edge (browser-proven, including restore-across-launch and legacy migration).
    - Note key wired through synth-hooks handleStart/StopNoteKeyAudition (same sendMidiInputEvent idiom as articulation audition, so held-set/last-played bookkeeping stays correct); remembers its own started pitch; release, cancel, blur, Escape, visibilitychange, and unmount all end it exactly once. The on-screen keyboard now feeds lastPlayedNoteRef via the element's note-down/note-up events (it previously never did — the Note key would have been stuck at middle C on mobile); DAW playback cannot dispatch those DOM events, so transport never pollutes the memory. Hardware-MIDI pitch capture remains impossible from JS today (no Web MIDI; host delivers natively without provenance) — record as a native-bridge follow-up if wanted.
    - Keyboard toggle added (no visibility state existed): keeps the keyboard mounted, hides via display none, fires allNotesOff on hide; the rail's travel band ignores a zero-height keyboard.
    - Glass verified by 2x screenshot review, which caught four real rendering defects the DOM assertions could not see (all fixed): Tailwind preflight's all-sides border-style made chevron border-width shorthand paint diamonds; the rail-level drop-shadow filter made the rail a backdrop root so the blur sampled an empty backdrop (shadow moved onto the silhouette svg); slot numbers floated detached (-8px overhang); the drawer's inherited overflow-clip-margin let the next carousel page peek through the 10 px gap.
    - Gates: full desktop browser suite 168/168 (five new cases: edge docking/persistence/migration, Note key pitch-memory/ownership/suspension, drawer toggles, the T11 path test, auto-preview end-to-end; two legacy cases updated to the B2 visual contract — one had also pinned a real body-corner clipping bug, fixed), iPhone shell 20/20, web bundle 14/14, shared hooks 29/29, tsc clean.
    - Remaining human gate: physical phone review of movement/feel on the tailnet build.
    Design authority:
    - The approved B2 artboard on the "Mod Bar Directions" canvas is the visual contract: 28 px modules on a 10 px rhythm in a 40 px bar, corner-badge route count, in-module activity bar, Note key with Auto-preview dot, Keyboard + Auto-preview modules only in the expanded drawer, shouldered silhouette with 12 px clear space inside the body beyond each shoulder, Linear Glass material (gradient fill, hairline border tracing the whole silhouette, tinted selected module).
    - Make the bar slightly translucent with a backdrop blur (2026-08-19 direction); keep the effect cheap on low-end phones and degrade to the opaque gradient where blur is unavailable.
    Implementation requirements (carried from T10 — honor during implementation, no separate prototype):
    - The complete bar moves around the screen perimeter, settles cleanly against an edge, respects safe areas, expands inward from its current edge, and preserves its perimeter position between uses.
    - Source dragging, bar movement, disclosure, and Note-key playing never compete for the same touch target; extend the existing separate-pointer-ownership pattern in MobileGlobalModRail.
    - The Note key behaves like one piano key fixed to the most recently played intentional pitch (initial fallback middle C / MIDI 60): down starts the note; release, cancellation, suspension, or loss of ownership stops it; touching again retriggers. Intentional pitch updates only from the on-screen or MIDI keyboard, never DAW/transport playback.
    - The bar stays mounted across Voice/FX/Mod view switches (T03A); sequence the landing against T03B so the shell surface is not churned twice.
    Done when:
    - Production MobileGlobalModRail renders the B2 design and the old 56 px treatment is gone.
    - Perimeter movement, snapping, safe areas, inward expansion, and persistence work on-device.
    - Gesture-ownership and stuck-note cases are covered in the existing browser acceptance suites.

[x] T11 — Implement universal manual audition
    Depends on: T10B
    Implementation evidence (2026-08-19):
    - Path test "the Note key triggers audible output from every mobile editor state" (tests/test_desktop_patch_view_browser.mjs) presses the rail's Note key in the Voice accordion, the FX effect editor, the Mod overview, a route-detail row, and the deep-linked source editor, asserting the exact note-on/note-off pair each time without leaving the state.
    - Stuck-note proofs: pointer cancel, capture loss, window blur, Escape, visibilitychange (mobile backgrounding fires no reliable blur), and unmount each end the note exactly once, guarded in both the rail component and synth-hooks.
    - Ownership: holding the Note key and moving does not drag the rail; the existing disjoint-ownership case plus the new Note-key case cover grip vs source-drag vs disclosure vs Note press.
    Done when:
    - A path test reaches every editor state and triggers audible output without closing the editor.
    - Press/release, cancellation, app suspension, and re-entry cannot leave a stuck note.
    - The control does not steal an active parameter or modulation drag.

[?] T12 — Prototype and tune optional Auto-preview
    Depends on: T10B
    Implementation evidence (2026-08-19) — built in production, not a prototype; what remains is the on-phone tuning and the explicit ship/revise/reject call:
    - Seam architecture (from a full write-path audit): exactly TWO instrumented seams, no per-control wiring. Seam A: usePatchParameter's setParameterValue (ui/shared/cmajor-react.ts) covers all 139 scalar engine parameters across all 63 binding sites on both platforms, with an actual-change comparison against the canonical value; preset load, host restore, engine echo, and undo bypass the hook by construction. Seam B: the modulation bridge's setRouteAmount and setMsegSlotShape (ui/shared/modulation.ts), both behind existing equality guards; incoming documents enter via applyStoredState, not these methods. One suppression: applyArticulationSnapshot writes scalar bindings inside runProgrammaticWrites. The stream is ui/shared/user-edit-bus.ts.
    - Pure scheduler (ui/shared/auto-preview-scheduler.ts, 16 cases) implements the locked cadence, including the cross-gesture rate window ("triggers immediately unless a prior preview is still inside the rate-limit window" — a first implementation violated it and the tests now pin it) and the trailing capped note arming the window like any retrigger.
    - Engine (ui/shared/auto-preview-engine.ts, 12 cases): gesture depth for bracketed drags, stillness synthesis (movementStoppedMs) for unbracketed controls, one armed timer at min(stillness, scheduler deadline) with stillness resolving first at a shared instant, deterministic caller-driven time.
    - Note layer (synth-hooks): retriggers the complete held set via heldMidiNotesRef or the remembered intentional pitch; engine-owned strikes use the raw connection so bookkeeping is untouched; user-held gates are never note-offed by cap or endPreview; a preview released in the instant it started keeps a 250 ms minimum audible life; blur/visibilitychange stop preview notes immediately with no trailing note. Toggle wired to the durable localStorage preference (cosimo.auto-preview.enabled.v1) and the rail's status dot.
    - End-to-end browser proof: with the toggle off, a real knob drag edits the parameter with zero audition notes; with it on, the drag strikes the remembered pitch and every note-on is matched by a note-off; toggling suspension mid-hold releases immediately. All under the 168/168 suite.
    - Deferred to tuning: whether rack enable/reorder and articulation range edits should trigger (currently they do not; both are one-line bus additions), and the 250/150/600 ms + minimum-note numbers on a real phone.
    Locked starting behavior:
    - A durable global preference shared across synth sessions and unaffected by presets, projects, or an individual session's lifetime. After first use, reopening the synth restores the previous Auto-preview state.
    - Only direct user parameter edits participate. Preset loading, DAW automation, modulation movement, undo/restore, and incoming state changes never generate audition notes.
    - Corrected requirement (T12C, superseding the current note-layer behavior): while any intentional keyboard note is held, Auto-preview emits no preview strikes; when nothing is held, it repeats the most recently completed note group, falling back to middle C (MIDI note 60) before any group has been completed.
    - Any actual parameter-value change counts. Do not use a change-magnitude threshold. Pointer motion that leaves the canonical value unchanged does not count.
    - The first actual change triggers immediately unless a prior preview is still inside the rate-limit window. Later changes overwrite one pending preview with the newest value; the latest changed value can never be stranded indefinitely.
    - Start physical tuning with a 250 ms minimum interval between in-motion retriggers. At each available interval, retrigger only when a newer value is pending, which caps continuous movement at four retriggers per second.
    - Every actual change resets a starting 150 ms movement-stopped timer. When that timer expires, guarantee one trailing retrigger of the latest value; if the rate-limit window is still closed, fire exactly when it opens. The final changed value must be heard no later than 250 ms after its change.
    - After the trailing retrigger, keep the note or chord held while the finger remains down and the parameter remains still. Do not retrigger again until movement resumes, allowing a looping MSEG to continue for as long as the user deliberately holds still.
    - If the gesture ends before the trailing retrigger, preserve that final retrigger and start physical tuning with a capped 600 ms note. If the trailing retrigger has already occurred, release ends the preview. Cancellation, suspension, visibility loss, or lost gesture ownership stops preview notes immediately and schedules no trailing note.
    - A direct discrete sound-changing parameter change provisionally triggers once; confirm the exact included/excluded parameter classes during physical testing.
    Tune rather than assume:
    - Audition the 250 ms rate limit, 150 ms stopped timer, and 600 ms capped final note on a real phone with short envelopes, long attacks, one-shot MSEGs, and short/long looping MSEGs.
    - After T12C, verify that intentional notes arriving during an active preview cancel only preview-owned work and cannot lose, duplicate, or stick a user-owned note.
    Done when:
    - Real phone trials establish that preview is useful rather than distracting.
    - A tiny final change always produces a bounded trailing preview; continuous movement cannot exceed the configured rate cap.
    - Holding still allows a looping MSEG to continue without further retriggers, and resuming movement restarts the same bounded scheduler.
    - Disable/enable behavior is immediate, deterministic, and cannot create stuck notes.
    - Ship, revise, or reject is recorded explicitly; experimentation alone is not completion.
    Sequencing note (2026-08-19): the physical tuning pass waits for T12B and T12C — cycle sync must be present, and manual keyboard playing must no longer be interrupted by Auto-preview, before the feel can be judged.

[x] T12B — Sync Auto-preview retriggers to the looping MSEG's cycle
    Depends on: T12 implementation evidence (in place); blocks T12's physical tuning.
    Implementation evidence (2026-08-19; final gates: desktop browser 169/169, iPhone 20/20, web bundle 14/14, shared hooks 29/29, modulation runtime 32/32, units 48/48, tsc clean; web build redeployed to the tailnet URL):
    - Pure quantizer ui/shared/auto-preview-sync.ts (quantizeStrikeTime + AUTO_PREVIEW_SYNC_CONFIG) under tests/test_auto_preview_sync.mjs (8 cases): eligibility/invalid-source immediates, the 120 ms sync floor, sweet-band boundary math, per-kind budgets (leading 150 / others 300), both slow-loop policies, the subdivision floor fallback, and far-past anchors. Two contract corrections happened DURING test review: the sweet-band case originally used a leading strike whose waits exceeded the leading budget (test was self-contradictory), and the subdivision loop must halve to the finest grid at or above the floor, not stop at the first grid under budget.
    - Note-layer integration in synth-hooks: lastNoteOnAtRef anchors the loop grid (updated by tracked note-ons AND engine-owned raw strikes, which also restart per-voice MSEGs); resolver picks the slowest looping MSEG with >= 1 enabled route (user decision; unmapped MSEGs ignored); strikes defer via one cancellable timer; a deferred hold strike converts to the capped final note when the gesture releases first (invariant: every cancel path clears the pending strike explicitly BEFORE the engine dispatch, so endPreview-with-pending always means release); suspend/disable/unmount all drop the pending strike.
    - patch_gui emit registered: ui/shared/user-edit-bus.ts -> patch_gui/user-edit-bus.js in ui/build.mjs (the seam import had broken the generated modulation.js emit chain — caught by the suite import failing, fixed in the pipeline, never by hand-editing patch_gui).
    - Browser: "Auto-preview with a routed looping MSEG still strikes, settles balanced, and never sticks" (route MSEG 1 -> reverbSize, auto on, drag) plus the pre-existing no-loop case. DEVIATIONS from the original plan, deliberate: the browser case asserts invariants (strikes arrive, stay on pitch, balance) rather than millisecond grid spacing — grid math is unit-pinned and CI timing assertions would flake; the telemetry cross-check kill-switch is deferred to the tuning pass (the arithmetic model needs no telemetry, so the kill-switch has nothing to compare until the tuning pass measures telemetry accuracy); "note-on timestamps in held bookkeeping" reduced to the single lastNoteOnAtRef since the newest note-on is the only anchor the per-voice model needs.
    Remaining: the tuning pass judges the feel (slowLoopPolicy ships as "subdivision"; "opportunistic" is one config field away), tunes minSubdivisionMs (raise it to coarsen the grid if eighths feel too loose), and folds into T12's ship/revise/reject.
    Origin (2026-08-19 physical finding): with a fastish looping MSEG and Auto-preview on, mid-cycle retriggers chop the loop's rhythm and feel jarring. Fix: when a retrigger becomes due, land it on the loop's grid instead of firing immediately, within a bounded wait.
    Delegation:
    - Implementation may go to codex (max effort) against pre-written contracts and failing tests; the quantizer's tests and the T12 latency-guarantee amendment are architecture work and stay with Fable. Codex must not modify tests.
    Architecture (decided):
    - The scheduler (ui/shared/auto-preview-scheduler.ts) stays untouched: it still decides WHEN a strike is due. Add a pure strike-time quantizer between the scheduler's retrigger command and the note layer's strike: (loop phase, cycle period T, strike kind, now, wait budget) → strike time. The engine/note layer defers the strike with one cancellable timer; cancellation/suspension rules unchanged.
    - Phase sources, in preference order: (a) if the MSEG is note-retriggered, no telemetry is needed — phase is time since OUR last strike modulo T, so sync is pure arithmetic keeping strikes an integer number of cycles apart; (b) free-running loops use the existing per-slot playhead telemetry (observeMsegPlayhead / the harness's emitEffectiveMsegState shape) to anchor phase, predicting the next boundary as now + (1 - position) * T rather than waiting to observe the wrap. MEASURE the telemetry anchor's accuracy/latency before trusting it and record the number.
    Audit findings (2026-08-19, engine + type contracts — these reshape the above):
    - MSEG rate has exactly one kind today: seconds (ui/shared/mseg.ts MsegPlayback.rate). No tempo-synced rate exists, so the BPM-visibility gate is moot until the reserved rateKind gains a sync value; drop it from v1.
    - The loop is a SUB-REGION {startX, endX} of the shape: the cycle period is T = (endX - startX) * rate.seconds, and the boundary to sync to is the region wrap (endX back to startX), NOT playhead 1 -> 0. Default playback loops the full shape with noteOffPolicy "finish_loop" and holdFinalValue true.
    - MSEGs run PER VOICE, and effectiveMsegState reports the newest audible voice (FixedFrameOscillator findNewestMonitoredVoiceIndex). Every preview strike spawns a new voice whose MSEG phase restarts at the note-on (legatoRestarts semantics to be confirmed engine-side during implementation). Consequence: the loop rhythm the user hears is anchored at a note-on JS itself observed, so the PRIMARY algorithm is note-on-anchored arithmetic for every case — engine-owned strikes AND held-chord re-strikes (a re-strike is note-off+note-on, which re-anchors). Telemetry drops to a cross-check/kill-switch input, not a phase source. Held-note bookkeeping must gain note-on timestamps to serve as anchors.
    - noteOffPolicy "finish_loop" keeps a released voice looping; its anchor remains its own observed note-on, so the arithmetic model still holds.
    Eligibility gates (attempt sync only when ALL hold):
    - The slot is in loop playback (one-shots never sync).
    - The slot has at least one enabled route (unmapped MSEGs are ignored entirely). Slot selection when several loop (user decision 2026-08-19): ALWAYS the slowest eligible loop — it is the audible rhythm, and since all per-voice MSEGs share the same note-on anchor, harmonically related faster loops land on its grid for free. The earlier edited-parameter/armed-source preferences are dropped.
    - T is confidently known and stable: skip sync while the MSEG rate itself is being edited, and skip tempo-synced rates unless host BPM is visible from JS (audit first; assumed not).
    - Telemetry anchor fresh within ~100 ms (free-running case only).
    Regime bounds (starting numbers — tune on the phone with everything else):
    - T < ~120 ms (faster than ~8 Hz): never sync. The loop reads as texture, not rhythm, and timer/telemetry jitter (±10-30 ms) is too large a fraction of T for "synced" to be true.
    - Sweet band, T between ~120 ms and the wait budget W: every due strike defers to the next cycle boundary. W by strike kind: leading strike ~150 ms (responsiveness dominates), in-motion and trailing strikes ~300 ms (rhythm dominates; the ear forgives late more than off-beat).
    - Slow loops, T > W (the musically common 0.5-3 Hz range, where the jarring is worst): implement BOTH candidate policies behind one switch so the tuning pass can compare — (a) opportunistic: sync only when the next boundary is ≤ W away, else fire immediately; (b) subdivision grid: quantize to the loop's internal binary grid T/2^k, choosing the finest interval within [~120 ms, W] (a 2 s loop gets a 250 ms grid). (b) assumes rhythmic MSEGs are drawn on binary-ish grids — verify against real patches during tuning and pick one policy explicitly.
    - Kill-switch: if two consecutive synced strikes measurably land > ~25 ms off the boundary, fall back to immediate strikes for the rest of the gesture.
    Contract amendment:
    - T12's "the final changed value must be heard no later than 250 ms after its change" becomes "≤ 250 ms + the active sync wait (≤ W)" while sync is engaged. Record the amendment beside T12's locked behavior when this lands.
    Done when:
    - The quantizer is pure with deterministic tests covering every regime, gate, and the kill-switch; a browser case drives fake playhead telemetry and proves a due strike defers to the boundary (and that ineligible cases fire immediately).
    - The telemetry anchor accuracy measurement is recorded, and tempo-sync/BPM visibility is audited and recorded.
    - Both slow-loop policies are selectable for the tuning pass; the tuning pass then picks one, tunes the bounds, and folds the ship/revise/reject into T12's closure.

[x] T12C — Make manual keyboard playing override Auto-preview and remember the last played chord
    Implementation evidence (2026-08-20, codex-implemented, Fable-reviewed and independently verified):
    - ui/shared/preview-note-memory.ts: pure chord-group memory — group starts at 0→1 held, overlap chains (A~B~C is one group even when A and C never sound together), commits at all-notes-up, newest completed group replaces, replay notes hold the boundaries but never enter the group, middle-C fallback until the first commit.
    - auto-preview-engine.ts gained manualHoldStarted/manualHoldEnded: the hold is state even while disabled (enabling mid-hold stays silent); hold start dispatches scheduler.cancelled; edits during the hold are dropped entirely (nothing queues for after release); gesture depth keeps pairing across the hold so only post-release CHANGES retrigger. Scheduler and sync modules untouched.
    - synth-hooks wiring: one tracking funnel with an intentional flag — keyboard paths true, Note-key/articulation audition replays false (they suppress as held notes but never rewrite the remembered chord). Hold start clears the deferred loop-synced strike and releases preview-owned notes BEFORE notifying the engine (endPreview's pending→capped conversion would otherwise resurrect a strike into the hold). playPreview strikes the whole remembered group simultaneously; the old held-chord re-strike branch is deleted. desktop-keyboard-adapter registers tracking listeners before attachToPatchConnection so a preview note-off precedes a same-pitch user note-on.
    - Tests: test_preview_note_memory.mjs 11 (failing-first), engine +7 manual-hold tests (failing-first), shared-hooks browser +5 exact-MIDI-stream integrations driving the REAL useSynthPatchViewModel (codex's sandbox blocked their RED run; review independently proved two of them fail with suppression severed). Independently re-run: focused suites 57/57, shared hooks 34/34, desktop sharded 186/186, tsc clean; the two untouched rail Auto-preview regressions pass (nothing-played fallback is exactly {middle C}).
    - Remaining per spec: the real-phone continuity check folds into the CURRENT FOCUS phone pass, which this task unblocks (T12 tuning was gated on it).
    Depends on: the existing T12/T12B Auto-preview implementation; blocks T12's physical tuning pass.
    Origin (2026-08-19 corrected product decision): the current note layer re-strikes the notes a user is physically holding and remembers only one released pitch. Manual playing should instead remain uninterrupted, while a released chord should remain available for later Auto-preview.
    Build:
    - Manual playing wins. While at least one intentional keyboard note is physically held, parameter edits never produce an Auto-preview note-on, note-off, deferred strike, or trailing strike. The user hears the sustained note or chord respond directly to the parameter change.
    - When manual playing begins, cancel any pending Auto-preview strike and release only notes owned by Auto-preview. Never release or re-strike user-owned notes. Parameter changes made while a manual note is held count as already heard and must not queue a preview for after release; if the same parameter gesture continues after release, only later changes may trigger Auto-preview.
    - Remember the most recently completed keyboard group. A group begins when the held-note count changes from zero to one, collects every distinct pitch pressed before the held-note count returns to zero, and commits when the last note is released. The next group after an all-notes-up gap replaces it.
    - Any temporal overlap keeps notes in the same group. This deliberately treats rolled notes, uneven releases, and chained overlaps as one chord: if A overlaps B and B overlaps C, the remembered group is A+B+C even if A and C never overlap directly.
    - When no manual notes are held, each due Auto-preview strike plays every pitch in the remembered group together. A one-pitch group repeats one note; a multi-pitch group repeats a chord. Do not replay the original staggered note timing. Before the first completed group, retain the middle-C fallback.
    - Only genuine user keyboard input already visible to the UI may form the remembered group. DAW/transport playback and Auto-preview's own strikes never update it. Preserve the existing Auto-preview cadence, loop synchronization, durable toggle, triggering parameter set, and preview-note velocity/lifetime policies.
    Done when:
    - Holding one note or a chord while changing a parameter produces no extra MIDI strikes and no retriggered envelope or MSEG; releasing the manual notes remains entirely user-owned.
    - Deterministic tests cover one-note groups, simultaneous and rolled chords, chained overlaps, the all-notes-up boundary, and replacement by the newest completed group.
    - Starting manual playing during either a sounding or deferred Auto-preview cancels only the preview-owned work and cannot lose, duplicate, or stick a manual note.
    - Changes made during the manual hold cannot leak into a trailing preview after release, while a new change made afterward can preview the remembered group normally.
    - A real-phone check confirms that a sustained note or chord remains continuous while a parameter is adjusted and that the released group is subsequently repeated correctly.

PHASE 4 — REDESIGN MODULATION AROUND QUICK EDITING

[x] T13 — Add an iOS-style quick-editor drawer for the selected modulation source
    Implementation evidence (2026-08-20, Fable-implemented, tests-first):
    - The readout-cell language was extracted into ui/shared/parameter-readout-strip.tsx (useReadoutCells + ReadoutCell shells): ONE code path for rolling-axis drags, truth-table rails, the fixed HUD, long-press seam, keyboard nudges, and choice cells. The Voice editor was migrated onto it (its strip rows, graph-overlay chips, and Tune-aggregate HUD all drive the one hook; its four cell-contract browser tests pass unchanged), so the sheet reuses the EXACT component rather than a copy.
    - ui/desktop/mobile-quick-source-sheet.tsx: non-modal bottom sheet over the current Voice/FX workspace for the LIVE selected source (one-selection contract): compact ~26% detent, grab-rail drag snapping to ~half, drag-all-the-way-up transitions into the real full-screen editor (MSEG modal; env/macro detail page), drag-down past threshold dismisses, an explicit Full editor button stays discoverable, and the graphic area re-orients by its own aspect ratio while the strip never moves. The sheet sits below the Mod bar (z 55 < 60) and the HUD layer, so audition and the active-value HUD are never hidden.
    - Corrected tap semantics: the collapsed bar's active-source icon opens the quick editor (the grip alone owns expansion), and tapping the already-selected source in the expanded drawer opens the sheet; on the Mod page the same tap keeps the existing full-editor navigation. Source drags still map (tap/drag discrimination untouched, pinned by the existing collapsed-source mapping test).
    - Anchoring (2026-08-20 product ruling): the sheet is a bottom DRAWER fixed to the screen bottom; covering the workspace tabs and keyboard beneath it is intended behavior. (A brief re-anchor above the tab row was reverted on the user's correction.)
    - Tests: a full journey (open from collapsed icon without expanding, strip Rate drag reaching the runtime under the shared HUD, half-detent snap, drag-to-full replacing the sheet with the real editor, button route, dismiss revealing the unchanged Voice context) plus a drawer/Mod-page test (env sheet with the four envelope cells; Mod-page tap navigates, never sheets) — both verified to FAIL with the sheet mount severed. One test-mechanics note: a second tap on a just-armed drawer chip is dispatched directly because Playwright's actionability retries race the collapse the tap itself triggers (the live browser proves the real flow).
    Depends on: T03B, T11
    Current correction:
    - The collapsed Mod widget currently treats a tap on its active-source icon as a request to expand the widget. That is wrong. The widget's own grip remains responsible for expanding/collapsing it; tapping the active-source icon opens that source's quick editor in the sheet.
    - In the expanded widget, tapping a different source selects it; tapping the already-selected source opens that source in the same quick-editor sheet. Full-editor navigation is then available from the sheet instead of being the selected source's immediate tap action.
    Required behavior:
    - Use a conventional iOS-style bottom sheet over the current Voice or FX workspace instead of forcing navigation to Mod.
    - Open at a compact detent around one quarter of the usable screen height. The exact safe-area-aware height may be tuned, but it must read as the small default state rather than an arbitrary fixed pixel panel.
    - Dragging the sheet's top grab rail upward can snap it to a larger state around half of the usable screen height. The same editor remains available at both detents; resizing does not reveal a different feature set.
    - Put a parameter-control strip immediately below the sheet's top lip. Reuse the exact numeric-readout bar component and interaction language shown below the mobile Voice oscillator graphic; do not invent drawer-specific knobs or controls. This strip stays fixed at the top of the sheet at every detent.
    - Only the modulation graphic changes orientation with the sheet's usable graphic aspect ratio: render it horizontally while its available area is wider than tall, and vertically once that area becomes taller than wide. Do not rotate or rearrange the fixed control strip with it.
    - Dragging all the way upward transitions into the existing full-screen editor layout for that exact source. Do not leave the user in a nearly-full sheet that duplicates the full editor.
    - Dragging the grab rail downward past the dismissal threshold closes the sheet and reveals the unchanged Voice/FX context beneath it.
    - Keep the conventional direct path to the full editor available from the sheet as well; dragging to full screen is an additional physical shortcut, not the only discoverable route.
    - Preserve source selection, route count, drag-from-source mapping, the movable Mod widget, and its universal Note control. Opening or resizing the sheet must not reinterpret a source drag as a tap.
    Done when:
    - The source can be edited and auditioned without leaving the current Voice/FX context.
    - The compact, half-height, full-screen, and downward-dismiss transitions feel continuous and land on their intended states without accidental source selection or route creation.
    - Sheet geometry never hides the Mod widget's audition control or the active-value HUD.
    Product-discussion status (2026-08-19): locked for later implementation. No prototype or implementation was started or authorized during this clarification pass.

[x] T20 — Wire the long-press parameter context menu everywhere (the ADR-017 menu)
    Origin (2026-08-20, user): "Long press should always open a context menu with quick entry / type to edit as an item. But there's also supposed to be other items like reset and so on."
    The spec exists: docs/ADR-017-mobile-accordion-and-dual-ring-rack-controls.md — a stationary long press opens the parameter menu with exact editing, base reset, route bypass, polarity, voice-source reducer, one-route removal, and confirmed removal of every route to the target; exact editing never creates a missing route; base reset never removes existing routes. docs/MOBILE_VOICE_FOCUSED_OSCILLATOR_SPEC.md ("Tap and long press") applies the same accepted menu to the Voice cells; movement past slop cancels the long press.
    Current state (audited 2026-08-20): the FX rack knobs are ALREADY wired (RackParameterContextMenu in effects-rack-workspace.tsx has exactly the ADR-017 items and the knob long-press opens it). The dead zones: the mobile Voice editor's readout cells (useReadoutCells has the onRequestParameterMenu seam; DesktopPatchView never passes it), the Voice-page BaseParameterKnob instances (enableContextMenu hardcoded false), and the quick sheet's strip. ADR-028 explicitly recorded this gap and delegated the wiring to T13, which shipped without it — that handoff is the miss this ticket repairs.
    Scope:
    - Generalize/reuse the existing RackParameterContextMenu for voice-cell hosts (the menu's item set adapts to the target: no polarity/reducer rows where the concept does not apply; exact entry uses the T19 specs, e.g. parameterEntrySpecForMobileVoiceControl).
    - Wire: Voice editor cells + graph chips, Voice-page knobs, quick-sheet strip cells. Long press = the existing 500ms gesture seam; movement cancels (already the gesture contract).
    - Every control that supports the shared drag gesture must long-press into the menu — "always", per the user.
    Done when: a phone-width browser test long-presses a Voice cell, a Voice knob, a quick-sheet cell, and an FX knob; each opens the menu; exact entry commits a typed unit-aware value to the right place; reset restores the default without touching routes; movement-past-slop never opens it; and a physical-phone check confirms the feel.
    Depends on: nothing (menu, gesture seam, and entry specs all exist).
    Implementation evidence (2026-08-20, Fable):
    - The rack's menu/value-sheet/remove-all trio extracted to ui/shared/parameter-context-menu.tsx (same classes and data-roles — one look everywhere); the rack re-imports thin wrappers. Bindings now carry initialValue (patch-controls), the ADR-017 reset source of truth.
    - Hosts package the full editing context into ParameterMenuRequest (label, targetKind, baseSpec, baseValue, defaultValue, commitBase): the Voice editor resolves per-oscillator target kinds; the quick sheet builds specs per cell kind. DesktopPatchView owns ONE menu state machine; route actions resolve against the armed source exactly like the rack's; amounts ride the canonical ADR-023 amount binding.
    - Phone-width browser test: movement-past-slop never opens; long-press on a Voice cell opens the menu; Edit values commits "62%" through the binding; reset restores the initial; a quick-sheet document cell hides reset (no canonical default) and its typed "500 ms" reaches the mseg1Rate runtime. Discrimination proven by severing the voice-editor prop (red) and restoring (green).
    - COMPLETED SWEEP (2026-08-20, second pass after the user's every-control correction): the menu opener is now a React CONTEXT (ParameterMenuContext) provided by both shells, so controls self-serve instead of relying on per-host prop threading (the threading is exactly how surfaces kept missing the menu). Newly wired: the Voice FILTER knobs (their callback was a literal no-op), the desktop OscillatorPerformanceControls knobs (entrySpec prop added to BaseParameterKnob), EVERY NexusNumberField and EVERY PrecisionNumberField (component-level long-press via useLongPressParameterMenu — covers Warp amount, Glide, all six unison fields, and any future callsite automatically), and the MSEG editor modal's Morph/Time sliders. The shell machinery extracted to ui/shared/parameter-menu-shell.tsx and the iOS shell now provides it too (its Voice cells and fields long-press the same menu). ParameterMenuRequest gained routeIndex pinning for row-hosts (T15's mapping rows will use it). Tests: the compact journey adds the filter knob (after powering the filter via its Mode chip — disabled controls deliberately keep pointer-events off) and the modal Time slider; a desktop-width test covers the perf knob (with reset present), the Warp Nexus field, and the Unison detune Precision field. Matrix ROWS are deliberately T15's (their numbers edit in the typeable detail view today and T15's spec owns the row menu — the machinery is ready for it). Physical-phone feel check rides Andrew's next pass.

[x] T21 — Cross-page mod-source drags lose all target highlighting
    Repro (user, 2026-08-20, confirmed by probe): start a source drag on FX, dwell-navigate to Voice via the tab, hover a Voice parameter — no eligibility outline, no capture highlight. Capture itself WORKS (is-mod-hover is set, the ghost latches, drops land); only the paint is gone.
    Root cause (probe-verified): the rail toggles `is-mod-source-mapping` on `.cosimo-surface` IMPERATIVELY (classList), but that element's className is REACT-CONTROLLED — DesktopPatchView's template includes `is-mobile-effects-page`, which flips on the page switch, so React rewrites className and silently drops the imperative class. Every eligibility/capture rule requires that ancestor class, so nothing paints. One MutationObserver hit at the exact tab switch; the rail never remounts; mappingActive stays true.
    Fix options (pick at implementation): (a) canonical — make the mapping flag React state on the surface's own className (thread or context from the rail's drag state); (b) minimal — move the imperative flag to a data attribute React never writes (data-mod-source-mapping) and switch the CSS selectors in styles.css/effects-rack-workspace.css/mobile-mod-rail.css.
    Done when: a browser test drives the exact repro (FX-origin drag, dwell to Voice, computed treatment on a Voice target present; leaving restores) and the class survives ANY React re-render of the surface mid-drag.
    Fixed (2026-08-20, option b): the flag is now `data-mod-source-mapping` — an attribute the surface's JSX never declares, so React re-renders cannot clobber it; the five CSS selectors migrated 1:1 (attribute and class have equal specificity, so the T08A eligibility/capture ordering is untouched); the rail's effect owns set/remove exactly as before. Browser test drives the exact repro RED-first (captured Voice target painted "none" on shipped code) and now proves capture paints AND leaving keeps the thin eligibility outline across the page switch. Desktop suite 204/204.

[x] T22 — Wavetable loading is egregiously slow
    Measured (2026-08-20, real WASM engine, desktop Chromium, localhost): BOOT upload window 9.0s after audio start; a table SWITCH ~455ms (with prewarmed source). On the phone both are worse. UI boot itself is fast (ready in ~220ms) — the wavetable→engine upload dominates.
    Where the time goes (pipeline audit): ui/worker/wavetable-worker.ts uploads each table to the engine as ONE EVENT PER MIP FRAME with `samples: Array.from(float32)` (JSON numbers over the bridge), 11 mip levels × frameCount frames — and every mip level is a FULL 2048-float frame (band-limited via FFT, never downsampled): 128-frame table = 1,408 events ≈ 2.9M floats ≈ tens of MB of JSON. maxFramesInFlight DEFAULTS TO 1: strictly serialized send→engine-ack round trips, each paced by the audio-block/event-queue cadence. Boot pays this per oscillator slot. WAV fetch + FFT are minor by comparison.
    Options (roughly in bang-for-buck order; combinable):
    1. Raise maxFramesInFlight (already an option; ack protocol tracks a Set, tolerates a window) — likely 5-10x alone.
    2. Batch many frames per event (fewer bridge round-trips; same bytes) — multiplies with (1).
    3. Audible-first streaming: send the mip level and frame region the current pitch/Index actually plays FIRST, sound becomes usable immediately, fill the rest in background.
    4. True downsampled mips (2048/1024/512/…): engine table layout already carries per-mip offsets/lengths (FixedFrameOscillator rendererMipLengthOffset) — investigate whether variable mip lengths are already supported; ~5.5x fewer floats.
    5. Boot dedup: A/B/C boot with the same default table — investigate engine-side slot sharing or copy instead of three identical uploads.
    6. Persistent source cache (IndexedDB/native) saves re-fetch+FFT only — transport dominates, so low priority.
    Done when: the user approves a strategy; boot-to-playable and switch latency are measured before/after on the phone; no fidelity loss (mip content byte-identical where kept).
    Implemented (2026-08-21, codex + Fable review with a config correction): mip frames now upload in BATCHED bridge events (frameIndexBase + frameCount + fixed-size zero-padded payload; 1,408 → 473 events per 128-frame table per slot); one ack per batch with exact session/generation/table/mip/base/count matching; mip CONTENT untouched (wavetable-mip.ts byte-identical). Engine loop-copies batches (cmajor/FixedFrameOscillator.cmajor); worker tracks in-flight batches with all prior guards (generation, session, watchdog, retry, abort, cache, prewarm).
    Shipping config (measured, not assumed): batch=3, window=1. Codex shipped 3/2, but the QuickJS restore probe FAILED at its 8s deadline while HEAD passed — a real native regression. Bisect: 3/1 passes, 3/2 fails, 4/1 fails, 5/1 fails — the native performer's 64 KiB input FIFO reliably carries exactly ONE 24 KiB three-frame payload alongside the session's other event traffic. The pipelining lever is therefore capped at 1 by native reality; the win is the batching.
    Measured (real WASM engine, desktop Chromium): BOOT upload 9.0s → 3.3s; table SWITCH ~455ms → ~203ms. Native QuickJS probe suite green on the final config; worker suite 28/28 (batch shape/tails, ordering, exact ack matching, rejection paths, watchdog, retry, cache, prewarm — failing-first per codex); npm run test:synth:engine green.
    Still open for the future (the approved-deferred options): audible-first streaming and true downsampled mips — NOTE for that ticket: the engine ALREADY stores mips downsampled internally (writePackedMipFrame packs packedMipLength(mip) points); only the WIRE is full-size, so the wire format can shrink per-mip without any engine storage change. Phone before/after ride the next device pass.
[x] T17B — Cut the FULL gate's wall clock
    Origin (2026-08-20, user): the end-to-end gate (desktop shards + hooks + iOS + presets + init + units + web) now dominates batch turnaround; speeding it up is explicitly wanted "at our earliest convenience".
    Method (user-directed): codex runs the FIRST investigative pass so the measurement grunt work costs few premium tokens; Fable then applies judgment to choose the strategy. Do not let codex choose or implement the strategy.
    Codex first-pass scope (measure, don't change): per-suite and per-test wall clock across the whole gate; fixed startup costs (vite/harness/server boots per FILE vs per test, browser launches, bundle builds hidden inside npm scripts — test:web:poc rebuilds the web bundle every run); which suites serialize that could shard or share a server; slowest individual tests (the 30s+ actionability timeouts); redundant coverage candidates (report only, never delete); how test-concurrency interacts with browser-suite flake history (the tainted-run races are documented in PROGRESS.txt).
    Constraints for any eventual change: T17's rule stands — no lost coverage, no lost stringency; timing-sensitive browser suites have a documented flake history under parallelism, so any added concurrency needs a stability run; discrimination evidence (failing-first/sever) must survive whatever restructuring happens.
    Done when: a measured profile exists, Fable has picked and recorded the strategy, the gate's wall clock is measurably cut, and a full green gate at the new speed plus a repeated-run stability check are recorded here.
    CLOSED (2026-08-21) — the analysis found the suite was never slow; the MACHINE was poisoned:
    - Finding 1 (the big one): the harness vite config polls the ENTIRE repo every 120ms (usePolling, Andrew's 88d0b198 dev pin) — ~4 CPU cores PER SERVER, and killed test runs orphaned servers that survived their npm/shell parents: three orphans had burned ~12 cores for up to 24 HOURS. Every timing ever measured on this machine was taken under that burn; the "59s test" and the 139-test 10-20s band were starvation, not test cost.
    - Fixes: harness-spawned servers (COSIMO_TEST_HARNESS=1) run with watch:null — tests never use HMR; dev keeps Andrew's polling untouched — and carry an orphan-suicide plugin polling the TEST PROCESS pid (passed explicitly via COSIMO_HARNESS_SPAWNER_PID, because the DIRECT parent is an npm/shell intermediate that outlives a SIGKILL; verified: harness vite self-exits within ~4s of kill -9 on the spawner). web:poc's two engine runs now execute in parallel after its one build.
    - Measured: desktop sharded suite 852s -> 473s (orphans killed) -> 69s (no-watch) = 12x, stable across three runs (69/69/70s, 204/204 each); web:poc 525s -> 282s; FULL gate ~25min -> ~8min, all sections green. Fixed per-test cost now ~3s (page boot); fixed sleeps total ~40s suite-wide (immaterial).
    - Remaining recorded (not blocking): web:poc is now the dominant section (build ~2min + engines ~3min) — per-test engine reuse is the next lever if wanted; the init-trio duplicate runs cost ~1s (ignore); browser-orphans' internal builds ~100s total (fine).
    Depends on: nothing; schedule opportunistically between product batches.
    Measurement pass complete (2026-08-20, codex): scratchpad t17b-profile.md. Key facts: prior desktop TAP shows 14m12s for the four shards; worst single test 59s, a dozen in the 20-30s class; a full gate implies ~29 server boots and ~22 browser launches; browser-orphans runs ui/build.mjs three times inside test_patch_view_layout plus a spectral plugin build; test:web:poc rebuilds the web bundle then runs the same file serially under two engines; units-orphans is 21s and embarrassingly parallelizable-adjacent. CONFIRMED DUPLICATION: the three synth-init files run twice (the gate's init trio AND the orphan scripts).
    Strategy (Fable, recorded 2026-08-20 — implementation is its own batch):
    1. Delete the double-runs first (drop the init trio from the gate; the orphan scripts own those files). Zero-risk minutes.
    2. Run web-poc's two engines in parallel after its one build; run units-orphans concurrently with the desktop shards (pure Node, no server contention — the documented races were browser-vs-browser and build-vs-serve, not pure CPU).
    3. Attack the 59s and 20-30s tests individually — the profile suggests fixed waits (readiness polls, waitForTimeout paddings), not genuine coverage cost. No timeout may be cut below its documented flake margin.
    4. Amortize browser-orphans' internal builds with a once-per-section prebuilt fixture ONLY if the repeated-build test's claim (rebuild idempotence) is preserved by an explicit dedicated test.
    5. Keep desktop shard concurrency at 4 (PROGRESS-documented boot starvation above that); do not interleave any section that writes patch_gui/build with any section that serves it.

[x] SFX-1 — RESOLVED AS A FALSE POSITIVE (2026-08-20, codex-diagnosed, Fable-verified): the packaged view rendered the trapezoid correctly all along; the TEST's path-sampling helper snapped to the NEAREST SVG vertex, so sampling 80% returned the 75% plateau vertex. The dev helper interpolated correctly — the packaged one now interpolates along segments too. No product change; suite 7/7 locally and wired into test:browser:orphans. Lesson: before declaring a model/view divergence, verify the measuring instrument against the model as rigorously as the view.
    Found 2026-08-20 while de-orphaning test suites. The canonical envelope math is green
    (tests/test_seqfx_stutter_envelope.mjs: evaluateStutterEnvelope(0.8, shape 0.125, gate 1) = 0.8,
    a right-wall downslope), but in the PACKAGED shadow-root flow, clicking the morph track at 12.5%
    then reading the rendered envelope path shows a flat top through x=0.8 — the pure-Gate shape —
    and a 5-second poll proves it is not a render race. Either the morph pointer mapping fails in the
    packaged flow (compare T05C's shadow-root retargeting class of bug) or the path renderer diverges
    from the model. tests/test_seqfx_production_view_browser.mjs pins this and stays RED (and therefore
    deliberately OUT of test:browser:orphans) until fixed; its other six tests pass after brand/theme
    pin migrations. Fix the product, then wire the suite into the gate script.

[x] T14 — Redesign the Mod overview without hidden functionality
    Depends on: T03B, T11
    Required behavior:
    - The selected Envelope/MSEG/Macro preview and the route matrix/list are both discoverable on entry; the matrix cannot sit invisibly below a large graph.
    - The compact MSEG preview preserves its coordinate system/aspect ratio and supports direct point editing.
    - Expanded editing remains available for precision work.
    Settled so far:
    - Replace the vertical source-editor/mapping-list stack with two top-level panels: `SOURCE` and `MAPPINGS`. Each selected panel receives the available Mod-workspace height instead of pushing the other below the phone viewport.
    - Reuse the Voice page's A/B/C selector exactly as the visual and behavioral authority: the same tab-bar component, dimensions, typography, active treatment, focus/accessibility behavior, and fixed-bar/directional-panel transition. Only the two labels and panel contents differ.
    - Order the panels `SOURCE`, then `MAPPINGS`. Activating a tab slides the content beneath left or right according to that order while the tab bar itself remains stationary; do not invent a separate Mod-specific tab design or transition.
    - On the first Mod visit in a plugin instance, select `SOURCE`. Later visits restore that instance's last selected `SOURCE`/`MAPPINGS` tab under the persistent workspace-state contract already settled for the primary shell.
    - In the `SOURCE` panel, make the compact MSEG graph directly editable instead of treating the entire graph as the full-editor button. Dragging its points edits the shape; a separate explicit Expand control opens the full-screen MSEG editor.
    - Defer compact structural gestures until the real layout can be judged: adding points, deleting points, and curve shaping may remain expanded-editor-only if the compact graph feels cramped. The prototype/review must compare them rather than letting implementation choose silently.
    - Keep the `SOURCE` panel's own source-family and source-number selectors. Synchronize them bidirectionally with the source selected in the floating Mod bar so either surface can change the same selection without requiring the bar to be expanded or repositioned.
    - Open `MAPPINGS` with all routes visible. Changing the selected source in either selector must not silently filter the list; apply a source filter only through the mappings panel's explicit Filter control.
    - When already inside the Mod workspace, choosing a source from the floating Mod bar selects that source and switches to `SOURCE`; do not open the quick-editor drawer over the full Source panel. The quick drawer remains the way to edit a source while staying in Voice or FX.
    Mappings table and toolbar:
    - Treat `MAPPINGS` as a polished responsive data table, not a loose stack of cards. Its logical data columns are Source, Target, Base, Mod Amount, Direction (`+ / ±`), State (active/bypassed), and row Actions. Wide layouts may show ordinary headers and columns; compact phone rows may reflow them, but must retain the same data model and the inline editing controls locked in T15.
    - Start a new plugin instance with every mapping visible, no search or filters, and a stable Source-then-Target ascending order.
    - Keep a compact, visually disciplined toolbar visible at the top of the scrolling table. It provides result count, find-in-table search, Filter, Sort, display choices where useful, and Add Mapping. Do not let controls wrap into an accidental pile or hide the table at 320 px; settle the responsive composition in the required prototype rather than inventing another arbitrary toolbar height.
    - `Add Mapping` inserts an inline draft row instead of navigating through separate full-screen source/category/effect/target pages. The draft provides searchable Source and Target pickers plus explicit Create and Cancel actions.
    - Disable source/target pairs that already have a mapping so the draft cannot submit a duplicate. A confirmed mapping starts at exactly 0% and becomes a normal editable row; a rejected creation leaves no row or route state and uses the mapping-failure feedback already locked in ADR-025.
    - Search matches the visible source, destination surface/effect, and parameter names and filters results as the user types. It has an obvious clear action.
    - Make every meaningful data column sortable and filterable with controls appropriate to its data: multi-select for Source and Target, choices for Direction and State, and unit-safe ranges for Base and Mod Amount. Actions are not data and do not sort or filter.
    - For numeric Base/Mod filtering across unlike parameter units, present normalized percentage of each parameter's legal span. When the table is narrowed to one compatible target/unit, the filter may additionally accept that unit's exact values under T19. Never compare raw Hz, dB, seconds, and other unrelated units as though they were one numeric scale.
    - Combine selections within one column as OR and filters across different columns as AND. Do not add a nested query builder, AI filter, board mode, shared saved views, or other project-management cargo cult to this synth table.
    - Bulk selection and bulk actions are out of scope. Do not add a checkbox column or a multi-select mode; enable, bypass, and delete remain per-row actions.
    - Show active search/filter/sort state plainly through selected toolbar controls, compact removable criteria, an `n of total` result count, and one Clear All action. A zero-result view explains that no mappings match and offers Clear All; it must not look like the synth has no mappings.
    - Preserve explicit search, filter, sort, and display choices while the same plugin instance remains alive, including visits to `SOURCE` and back. A new plugin instance resets to the all-mappings default. These are UI-view preferences, never preset sound state, and selecting a modulation source never rewrites them.
    - If sorting or filtering depends on Base or Mod Amount, a row being edited must not move or disappear under the user's finger. Freeze its membership and position for the active gesture, then apply the current sort/filter once that gesture ends.
    - Use Linear's current list/view discipline as the quality benchmark: obvious controls, immediate results, persistent user choices, sortable columns, and legible active criteria. Copy the interaction quality, not Linear's branding or project-management feature set.
    Done when:
    - A first-time user can identify every major action from the initial Mod view without blind scrolling.
    - Compact and expanded MSEG points render at the same normalized coordinates.
    - Browser coverage proves the Mod tabs match the Voice A/B/C selector and its directional content transition rather than merely approximating its appearance.
    - A real multi-route table prototype at 320 and 393 px proves the toolbar remains composed, every logical column can be sorted and filtered, active criteria and result count remain understandable, and inline row editing never causes the active row to jump or disappear.
    Planning note (2026-08-19): the product behavior above is locked enough to move on. The user explicitly deferred the real-device-width prototype; do not start that prototype now, and do not treat its deferral as implementation approval.
    Implemented (2026-08-21, user-authorized "let's tackle the mod matrix tickets"; Fable UI + codex pure model, reviewed):
    - ONE selector: ui/shared/segmented-editor-tabs.tsx — the Voice A/B/C bar extracted as SegmentedEditorTabs + useDirectionalPanelTransition, the Voice editor migrated onto it, and the SPEC'D-BUT-NEVER-IMPLEMENTED directional slide finally built (sanitized static-snapshot ghost with canvas repaint, live-from-first-frame incoming panel, programmatic switches swap instantly, reduced-motion removes translation; harness runs reduced-motion by default with one test proving both slide directions). The Mod page's SOURCE/MAPPINGS pair IS this component (test pins the literal classes/roles, not an approximation).
    - SOURCE panel: the compact MSEG graph is DIRECTLY point-editable through the full editor's own handlers (EditableMsegSurface; the shared geometry ref is claimed just-in-time on pointer-down so the modal/sheet/panel share one editing brain sequentially); explicit ⤢ Expand moved BESIDE the morph rail after a fixed corner chip provably collided with the default shape's end point; the live playhead is preserved as a progress line so editability never cost the activity light. First visit lands SOURCE; the instance restores its last panel (sessionStorage, the shell's scope); bar-selection inside Mod surfaces SOURCE (never the quick sheet) — mount-run guarded so restore is not clobbered.
    - MAPPINGS: pure list machine in ui/shared/mod-mappings-table-model.ts (codex, 19/19 failing-first with genuine discrimination probes — OR-across-columns and null-base-as-zero both caught): default all-visible Source→Target sort with a pinned deterministic tie-break chain, per-column filters (OR within, AND across), term-AND search, criteria chips + Clear All identity, range filters that never claim unbacked bases, gesture freeze (row keeps its previous index through sort/filter boundary crossings), draft classification + duplicate-target listing. UI: one composed 40px toolbar (count/find/Filter/Sort/+Add) proven single-row inside 320px; chips; inline draft with duplicate-disabled options and create-at-0%; empty/zero-result views with Clear All; list is the ONLY scroller under the fixed toolbar (the workspace grid forwards definite height).
    - ui/shared/modulation-target-base.ts: the one targetKind→base contract resolver (rack descriptors / oscillator address map / shared voice-filter descriptors extracted to ui/shared/voice-filter-descriptors.ts); unbacked targets resolve null and their rows edit amount-only.
    - Old-suite migrations preserved every claim (list completeness, 320/393 containment, >100 routes with reload/edit/delete, ADR-025 journey incl. bypass-never-dims + grey treatment now carried by the rail band, Note-key states, panel-fit replacing the overflow-era pin, swipe-scroll on the seeded list; the web:poc realtime-cadence perf test now drives the row rail's real pointer gesture instead of the retired amount slider, on both engines). The iOS harness gained the desktop harness's reduced-motion default when the shared tab slide reached IOSPatchView. Playwright gotchas recorded in PROGRESS.
    Deferred within scope: compact structural gestures (add/delete points) stay expanded-only pending the device pass (the spec allows exactly this, to be judged on the real layout); DESKTOP's two-column Mod layout keeps the previous presentation this pass.

[x] T15 — Revisit Mod Matrix row editing against the newer product direction
    Depends on: T14; the immediate-delete behavior also depends on T18's synth-wide Undo recovery
    Current baseline:
    - A mobile list/detail implementation already exists; rows show the relationship and amount, while detail owns exact editing, polarity, bypass, and delete.
    Settled direction:
    - Make each mapping row the editing surface instead of requiring a tap into a separate route-detail page. Tapping the row must not navigate away; put the commonly used route controls directly on the row.
    - Put any less-common route controls that genuinely do not fit on the row in a context menu opened by long-pressing that row.
    - Keep Delete directly on every row as a trash-can button immediately beside the power button that switches that mapping between active and bypassed; Delete is not hidden in the long-press menu.
    - Tapping the trash can deletes the mapping immediately. Do not show a confirmation dialog, toast, or other intermediate feedback; recovery belongs to the planned synth-wide Undo history, which must be specified separately before this destructive row behavior ships.
    - Put a full parameter slider/rail on each mapping row. It shows the target's normal/base position together with that row's selected mapping range using the same visual language as the app's other parameter controls.
    - Reuse the app-wide parameter gesture unchanged: horizontal movement edits the target's base value, vertical movement edits that row's modulation amount, the rolling dominant axis may change during one drag, and one movement sample changes only one value.
    - Reuse the same finger-clear parameter HUD, including its live Base/Mod mode and values. Do not create a Mod-Matrix-specific drag controller or HUD.
    Implemented (2026-08-21, with T14): every mapping row IS the editing surface — its rail is ONE shared ReadoutCell (the app-wide rolling-axis gesture: horizontal edits the target's base through the modulation-target-base resolver, vertical edits THIS route's amount through the canonical ADR-023 binding; same fixed HUD; no matrix-specific controller). Long-press on the rail opens the ADR-017 menu PINNED to this exact route (routeIndex), with T19 unit-aware Base + Amount fields. Power and trash sit directly on the row; trash deletes immediately with no confirmation and no toast (recovery remains T18's — still the recorded risk until Undo lands). Row identity never dims on bypass; the rail band takes the grey treatment and the row labels BYPASSED. Pinned by the migrated 100+-route suite (row long-press exact entry, on-row delete) and the ADR-025 journey.
    Follow-on (2026-08-21, user live review of the shipped rows; options board reviewed, LED variant chosen):
    - Readout: the mapping cell dropped its duplicated target label and became an LED amount meter (segmented fill lit from the base tick in the source color, amount readout fixed top-left in the source color, base value fixed top-right (fixed corners after the edge-riding chip collided with the base value)). Only mapping rows changed; standard readout cells everywhere else are untouched.
    - The +/- marker is now a REAL polarity toggle button (its old confusion: it looked like a control and did nothing).
    - Bypass language rebuilt per ADR-025 amendment: whole mapping dims as one piece + grayscale source art + unlit power ring; BYPASSED text deleted; power never dims; a live gesture lifts a bypassed row to full emphasis. Row sits on one 6px spacing unit with three equal 32px control modules (user: spacing must be intentional and even).
    - The taller meter exposed two REAL presentation bugs, now fixed at the resolver (railProjection on resolveModulationTargetBase, 6/6 failing-first units): log parameters place the base tick logarithmically (1000 Hz at 57%, not 5%), and octave-application amounts travel in octaves (a ±2.3 oct band was invisibly thin as raw Hz).
    Closed gap (2026-08-21, user device pass: cutoff/resonance drags felt "sigmoid" — same argument as the knobs, again): the base drag now walks the DISPLAY scale (railProjection.denormalizeValue; equal finger travel = equal octaves on log tracks, tick glued to the finger; keyboard nudges 1% of track), matching rack-parameter-knob's settled rule. Failing-first browser test pins the exact octave walk (40px from 1000 Hz -> 1000*1000^(40/220) ≈ 3511 Hz; the old raw-Hz walk measured 4633).
    Closed gap #2 (2026-08-21, user, angrier — rightly: SAME saga, other axis): resonance AMOUNT drags were still linear in the amount domain; the knob rule modulationDragStyle "effective-value" (vertical walks the MODULATED value along the dial) now routes through the resolver (amountDragStyle) into the row cells, and the voice-filter resonance twin descriptor carries the field it had been missing. Failing-first pinned old 4.42 Q vs dial-walk 1.15 Q for the same 40px. Rule memorized: port settled arguments by MECHANISM (all descriptor behavior fields), test every axis.
    Device fix (2026-08-21): continuous targets (entrySpec step 0: drive dB, cutoff Hz, scalar depths) NaN'd on horizontal base drags ("must be a finite number" toast per frame, user's phone) — the shared cell now treats step<=0 as continuous (unsnapped writes, 1%-of-span keyboard nudges); failing-first repro test pins finite writes.
    - When several mapping rows share a target, a base-value edit from any one row updates the shared base display on every other row for that target; each row continues to show and edit only its own mapping range.
    - Keep the mapping-direction control visible on every row as a compact `+ / ±` toggle: `+` is one-way modulation and `±` is centered modulation around the base. The slider/rail updates immediately to show that choice.
    - Put exact-value editing and the uncommon Max/Mean voice-combining option in the row's long-press context menu.
    - The context menu provides separate, clearly labeled Base and Mod Amount number fields. Both use the unit-aware entry contract from T19, and each has decrement and increment paddles that use that parameter's existing step/detent rather than a new Mod-row-specific increment.
    - When a mapping is bypassed, keep its range grey/dashed and remove its effect from the sound, but leave both Base and Mod Amount editable. Bypass disables the mapping's audible effect, not its editing controls.
    - Only the slider/rail area owns the horizontal-Base/vertical-Mod parameter gesture. Dragging the source identity, target identity, or unused row space scrolls the mappings list normally.
    - A stationary long press anywhere on the row opens its context menu; once movement has committed to list scrolling or parameter editing, it must not open the menu.
    - Include `Reset Base` and `Reset Mod Amount to 0` in the long-press menu. Each reset is one synth-wide Undo action.
    - Inherit the shared parameter control's keyboard, focus, cancellation, and exact-entry behavior; do not create a less accessible Mod-row variant.
    Product-discussion status:
    - The interaction and control split are locked. The remaining design gate is a real multi-row prototype at 320 and 393 px that proves the identity, rail, direction, power, and trash controls remain readable and the list remains comfortably scrollable. Do not resolve density by silently removing controls during implementation.
    - The user explicitly deferred that prototype on 2026-08-19. Do not start it now, and do not treat the settled product discussion as implementation approval.
    Decision rule:
    - Keep the list scannable and vertically scrollable. Add direct manipulation only where touch ownership and accidental-delete risk remain clear.
    Done when:
    - The chosen row model is prototyped with multiple routes at 320 and 393 px.
    - The decision explicitly says whether base value belongs in a route row or remains owned by the target control.

PHASE 5 — PARKED UNTIL ITS QUALIFICATION CHANGES

[ ] T16 — Nested Effects Lane: UNPARKED, hard-cut build (2026-08-22)
    Desired product:
    - Multiple effect instances nested inside utilities such as Parallel and Frequency Split, using an Ableton-like lane model.
    Current blocker:
    - Current-product requalification v2 is a browser no-go; Chromium C2 fails the frozen cross-platform gate and WebKit C2/H3 overload.
    - The physical AUv3 campaign is also unexecuted, but phone access alone cannot clear the browser failure.
    Resume only when:
    - The baseline performance/capacity contract changes or the current product gains enough measured headroom to pass it.
    - Then rerun browser qualification, complete the physical-phone campaign, review the existing Effects Lane branch, and make an explicit merge/rework/abandon decision.
    Do not:
    - Merge or begin production Effects Lane implementation merely because the phone becomes available.
    Requalification v3 (2026-08-21, codex-executed browser campaign against master ed6b39d7, Fable-verified: independent raw-record reduction matches the report; verifier green; freeze-before-measurement and the experiments/-only fence held; branch codex/effects-lane-requalification-v3, RESULT-V3.md):
    - STILL PARKED under the frozen rule — C2 (two extra active instances of every effect) fails the 0.75 average-load gate on BOTH backends (Chromium 0.85, WebKit 0.92-0.97), H3 exceeds 1.0 on both.
    - Real movement since v2 all the same: every variant improved ~0.04-0.09 absolute load; WebKit's BASELINE flipped fail->pass (0.76->0.72), so the v2 "campaign cannot validate its own baseline" defect is gone and what remains is genuine capacity; WebKit H3's misses collapsed 695-788 -> 22-53.
    - Remaining C2 gap to the ceiling: ~0.10 absolute on Chromium, ~0.17-0.22 on WebKit.
    - The physical iPhone/AUv3 campaign (authoritative) remains unexecuted behind the xctrace-offline device blocker — needs the user's hands on the phone.
    Direction change (2026-08-22, Andrew): the frozen performance gates are VOID as requirements — they were arbitrary. No governor, no capacity ceiling; overload glitches. HARD CUT: every effect becomes an addressable lane device (lane.<instanceId>.<endpointID>); the rack.* namespace, the permanent eight, the 45 per-parameter host endpoints, and rack state v1 are DELETED, not shimmed — one application, no backwards compatibility (never shipped). Architecture: EFFECTS_LANE_ARCHITECTURE_PLAN.md. No spike (every mechanism proven or arithmetic — Andrew rejected the spike as busywork and was right: no design fork existed for it to decide). Build order: M1 engine (pool slots + extended target matrix + laneState upload; the five former spike criteria become M1's permanent acceptance tests), M2 the namespace hard cut, M3 lane UI, M4 product surface + phone pass. Sound quality: never claim parity from the existing differential harnesses (Andrew distrusts their coverage); DSP-touching changes get their own scoped verification conversation first.
    Current master status (2026-08-25):
    - M1-M4's production foundation is present on master: the engine pool and lane addressing, lane.v2 state, Parallel/Frequency Split groups, the subway graph, dynamic modulation targets, device add/remove, and the compact starter trio.
    - The original task remains open only for its physical iPhone/AUv3 acceptance pass. T24, T30, T31, and T32 below own the separate drawing, compact-layout, insertion, and live-drag improvements.

EFFECTS LANE FOLLOW-UP

[x] T24 — Draw continuous rounded splits and joins in the FX graph
    Active owner (2026-08-25):
    - Codex task `FX Graph Foundation`, thread `01a03a79-d020-7782-98c8-5111a3c930ff`, branch `codex/fx-graph-foundation`, worktree `/Users/winterfell/.codex/worktrees/9acf0081-2413-41b0-b3a5-0abc11458d94/cosimo-synth`, model SOL, reasoning max.
    Confirmed problem (2026-08-25):
    - In Parallel and Frequency Split groups, the incoming line stops at the split symbol. The branch lines begin lower down as disconnected rails instead of emerging from one point and visibly diverging.
    - This is a drawing bug introduced with the original map, not a rack-state or audio-routing error. The full diagnosis and browser measurements are in `FX_GRAPH_DRAWING_FINDINGS.md`.
    Build:
    - Keep the existing rack tree, row layout, labels, symbols, controls, and interaction elements.
    - Replace the fork and merge connector rectangles with continuous scalable paths. One incoming path must reach one junction; every branch must leave that exact junction on a smooth rounded curve. The merge must use the same geometry in reverse.
    - Cover Parallel and Frequency Split at every branch count the product permits and at responsive narrow and wide widths.
    - Preserve branch colors, empty-branch dashes, bypass dimming, selection state, and all current click and drag targets.
    Do not:
    - Change DSP routing, rack storage, drag-and-drop behavior, menus, or parameter handling as part of this repair.
    - Call the repair complete merely because extra square vertical bars hide the current gap; the required result is a legible rounded flow diagram.
    Done when:
    - A browser regression inspects the rendered pixels and proves that the incoming rail is visibly connected to every outgoing branch and that every branch reconnects to the outgoing rail.
    - The proof covers both group types, every supported branch count, and representative narrow and wide layouts; existing graph interaction tests remain green.

[x] T30 — Fit a three-band Frequency Split inside the graph without overlapping effects
    Active owner (2026-08-25):
    - Codex task `FX Graph Foundation`, thread `01a03a79-d020-7782-98c8-5111a3c930ff`, branch `codex/fx-graph-foundation`, worktree `/Users/winterfell/.codex/worktrees/9acf0081-2413-41b0-b3a5-0abc11458d94/cosimo-synth`, model SOL, reasoning max.
    Required result:
    - A LO/MID/HI split must remain completely inside the graph container. Its branch lines, labels, effect pills, and crossover readout must neither overflow nor be clipped.
    - Effects on neighboring bands must never overlap or hide one another, while the order within each band remains obvious from top to bottom.
    Starting layout:
    - Keep the three branch lines closer together horizontally, then stagger effect pills vertically whenever neighboring bands would otherwise place pills at the same height. Use only the vertical offset needed to avoid collisions; do not spread the whole graph wider again.
    - Treat the no-overflow/no-overlap result as fixed. The exact staggering rule may change if a simpler compact layout proves clearer in the real container.
    Coordinate with T24:
    - The rounded split and merge paths must follow the final compact branch positions and remain visibly connected through every staggered row.
    Done when:
    - Browser geometry checks prove that every rendered part stays within the graph at representative phone, desktop, and plugin widths and that no pair of effect pills intersects.
    - Screenshots with populated LO, MID, and HI bands show which effect belongs to which band without relying on clipping, horizontal scrolling, or shrinking the effect labels until they are unreadable.

[ ] T31 — Show the proposed result while an effect is dragged and animate every move
    Current gap:
    - The code already builds a proposed rack order as the pointer crosses drop targets, but it redraws that order instantly. The dragged effect stays in the grid, other effects jump, and the destination is not shown as an opening in the chain.
    Required drag behavior:
    - Lift the real effect pill out of the layout so it follows the mouse or finger throughout the drag.
    - As the pointer approaches a valid position, open a correctly sized gap there and show a ghost copy of the dragged effect in that gap.
    - Animate the other effects and the graph paths into their proposed positions instead of snapping them. When the target changes, the old gap closes and the new one opens smoothly.
    - Support reordering within the trunk or one branch, moving earlier or later in the chain, and moving between the trunk and any branch.
    - On release, settle the dragged pill into the visible ghost position and commit exactly that arrangement. On cancellation, restore the original arrangement.
    State boundary:
    - The preview is visual only. Keep the stored rack and audio routing unchanged during the drag, then publish one change on a successful release; cancellation publishes nothing.
    Done when:
    - A held-pointer browser test captures the start, middle, and settled frames and proves that the lifted pill tracks the pointer, the destination gap and ghost exist before release, and displaced effects travel between measured positions rather than changing position in one frame.
    - Cross-branch, same-branch, and trunk moves commit the arrangement that was previewed; cancellation restores the starting picture and state. Reduced-motion mode may shorten movement but must retain the clear lifted pill, gap, and ghost.

[x] T32 — Put a small add button at the tail of every FX path
    Active owner (2026-08-25):
    - Codex task `FX Graph Foundation`, thread `01a03a79-d020-7782-98c8-5111a3c930ff`, branch `codex/fx-graph-foundation`, worktree `/Users/winterfell/.codex/worktrees/9acf0081-2413-41b0-b3a5-0abc11458d94/cosimo-synth`, model SOL, reasoning max.
    Current gap:
    - The trunk already ends with an add button, and an empty branch has one. A populated branch currently continues directly from its last effect to the merge with no way to append another effect there.
    Required behavior:
    - Show a small `+` immediately after the last effect in every Parallel and Frequency Split branch, before that branch merges back into the trunk.
    - Keep the trunk's existing trailing `+` at the end of the main chain. An empty trunk or branch must still have its `+` at its first insertion position.
    - Tapping a branch-tail `+` opens the existing effect picker and inserts the chosen effect at that exact branch tail. Keep the same point available as the exact tail destination for T31 dragging.
    Done when:
    - Browser coverage proves the `+` exists at the correct insertion path for empty and populated branches of unequal lengths, for both Parallel and Frequency Split groups, and at the trunk tail.
    - Adding from each button inserts one effect at the pictured location without changing the order or membership of any other path.

MSEG, MOD BAR, AND SYNTH CONTROL FOLLOW-UP

[x] T33 — Make the full-screen MSEG editor look like the drawer editor, expanded
    Active owner (2026-08-26):
    - Codex task `Implement T33/T34/T36 MSEG editor`, thread `01a03bb7-4af7-78f2-bed4-e58c4bfa0b10`, branch `codex/t33-t34-t36-mseg-editor`, worktree `/Users/winterfell/.codex/worktrees/d8948efb-c318-481b-a7d8-f013e757e192/cosimo-synth`, model SOL, reasoning max.
    Implementation evidence (2026-08-26):
    - `MsegEditorShell` now owns the actual header, graph, and compact-control composition mounted by both surfaces. The drawer retains its established title → controls → graph order; the reviewed full-screen presentation is title → dominant graph → one 40 px bottom row, with A/B first and Rate/Morph/Loop following. Full screen has no old modal footer, coordinate HUD, `synth-modal-frame`, border, floating shadow, backdrop blur, or unrelated wrapper decoration.
    - `MsegEditorControlStrip` now owns the complete A/B, Rate, Morph, Loop toolbar rather than accepting full-screen-only actions. Drawer and full screen render the same ordered controls and state. One symmetric four-track layout gives A/B and Loop matching outer widths, Rate and Morph matching inner widths, four-pixel gaps, equal 32 px tool heights, one corner radius, and balanced left/right insets at the review width.
    - The drawer remains the same fixed bottom sheet, keeps the same mounted DOM node while switching Envelope → MSEG (T43), and retains its original `border-top: 1px solid color-mix(in srgb, var(--quick-sheet-accent) 45%, var(--cosimo-line));` declaration unchanged. Production-view captures and exact geometry are in `docs/evidence/t33-t34-t36-mseg/`.
    Reopened after review rejection (2026-08-26):
    - The shipped full-screen editor retained the old modal composition and only borrowed drawer colors/classes. Its controls remained below the graph instead of using the drawer's title → controls → graph structure, so the recorded visual evidence did not satisfy T33.
    Current gap:
    - The editable graph is already shared, but the full-screen shell has its own heavy frame, border, spacing, and controls, so the two versions look like unrelated products.
    Required result:
    - Use the drawer editor's surfaces, typography, control styling, spacing rhythm, and graph treatment in full screen. Full screen may expose its additional editing actions, but it must read as the same editor with more room.
    - Keep the drawer's existing top border and color unchanged. This task only brings the full-screen editor into the drawer editor's visual language.
    - Share the relevant components and style tokens so later visual changes cannot silently make the two editors diverge again.
    Done when:
    - Reviewed screenshots of the same MSEG in the drawer and full screen show one visual system; the drawer's existing top border is unchanged and the full-screen editor has no heavy enclosing frame or unrelated decoration.
    - Existing MSEG editing, undo, A/B, looping, dismissal, and drawer resizing behavior remain intact.

[x] T34 — Replace the edge-bound MSEG timing controls with compact knobs without making the tab taller
    Active owner (2026-08-26):
    - Codex task `Implement T33/T34/T36 MSEG editor`, thread `01a03bb7-4af7-78f2-bed4-e58c4bfa0b10`, branch `codex/t33-t34-t36-mseg-editor`, worktree `/Users/winterfell/.codex/worktrees/d8948efb-c318-481b-a7d8-f013e757e192/cosimo-synth`, model SOL, reasoning max.
    Implementation evidence (2026-08-26):
    - Drawer and full screen use the same compact Rate/Morph knob cells built from the existing stippled knob artwork and shared parameter gesture/menu/HUD contract. Rate keeps the exact 0–2 s domain, exact entry, arrow/Home/End keyboard operation, and slider accessibility semantics.
    - At 320 px, the complete bordered drawer row is exactly 40 px tall, the Rate knob has at least 100 px of physical travel on either side, and touch tests traverse minimum-to-maximum and back without leaving the viewport. The mapping is deliberately linear; T35's non-linear response, T37's copy action, envelope color changes, and new HUD designs are not included. A later explicit visual-review correction gives only MSEG curves, points, and fill their existing purple source identity.
    - The full editor now owns an above-editor HUD portal, so physical Rate drags show the changing value instead of rendering behind the modal. Drawer Morph shows its live amount in the shared precision HUD. Every editable graph always layers purple shape A, gray shape B, and the realized A/B result as a separate cyan curve. The realized curve rises from a clearly visible resting state to a thicker full-opacity glow during Morph drag, then returns to the resting state without disappearing or mutating either stored shape.
    - Failing-first composition, shared-toolbar state, balanced-toolbar geometry, physical-hit, live-HUD, persistent realized-Morph, distinct-color, drag-emphasis, release-persistence, time-ruler, and color-contract tests are green across compact drawer, half drawer, and full screen. Post-rebase combined master verification passes MSEG browser 54/54, MSEG unit 20/20, iPhone browser 21/21, and the complete desktop UI matrix 262/263; the sole failure is the established React Grab `navigator.webdriver` development-harness assertion. The combined compiled VST3 is installed and strict-code-sign valid, and a fresh HMR standalone is running from master at `http://127.0.0.1:5199` with `usesViteClient: true`.
    Reopened after review rejection (2026-08-26):
    - Rate/Morph values changed, but the full-screen precision HUD was rendered below the modal and the drawer disabled its effective Morph curve. No acceptance test proved visible live feedback, so the controls were not complete in use.
    Required result:
    - Replace the drawer and full-screen Time/Rate/Speed slider-style controls with compact knobs that fit inside the existing control strip. If a neighboring control uses the same compact row, keep its treatment visually consistent.
    - Move the timing knob far enough in from the screen edge that a person can drag left to slow down and right to speed up without immediately running out of screen.
    - Do not increase the height of the drawer tab, its top control strip, or the corresponding full-screen control row. The knobs must be sized for the space that already exists, not become large showcase knobs.
    - Preserve exact numeric entry, keyboard operation, accessibility naming, and the current parameter range.
    Done when:
    - Before/after geometry checks prove the control row is no taller, representative phone screenshots show clear margins on both sides of the knob, and touch tests can traverse the full timing range in both directions without hitting the screen edge.

[ ] T35 — Give MSEG time dragging a tunable non-linear response with extra precision below 500 ms
    Current gap:
    - The compact editor currently converts horizontal pixels directly into a linear share of the entire time range, which makes short times difficult to set precisely.
    Required result:
    - Use a smooth, monotonic response curve that devotes more drag travel to values below 500 ms while still making the full supported range reachable. Do not change exact text entry or stored parameter values.
    - Add the small set of curve controls needed to tune that feel to the existing developer-only `Performance tuning` page reached from the preset-bar menu. Changes must take effect without rebuilding or relaunching.
    - Keep the exact curve and final coefficients open for an Andrew-led live tuning pass; bake the accepted values as the normal defaults after that pass.
    Done when:
    - Automated checks prove the mapping is monotonic, continuous, clamped, and gives materially more pixel travel below 500 ms than the old linear mapping.
    - A live tuning pass can adjust every exposed coefficient, reset them, and feel the result in both drawer and full-screen MSEG timing knobs.

[x] T36 — Put MSEG time on whichever editor axis is longest
    Active owner (2026-08-26):
    - Codex task `Implement T33/T34/T36 MSEG editor`, thread `01a03bb7-4af7-78f2-bed4-e58c4bfa0b10`, branch `codex/t33-t34-t36-mseg-editor`, worktree `/Users/winterfell/.codex/worktrees/d8948efb-c318-481b-a7d8-f013e757e192/cosimo-synth`, model SOL, reasoning max.
    Implementation evidence (2026-08-26):
    - `EditableMsegSurface` resolves from its measured SVG area and re-evaluates through `ResizeObserver`; an 8% near-square hysteresis band prevents flicker. Only the presentation/pointer orientation state changes—the MSEG document and stored points are never rewritten.
    - Geometry tests cover both orientations and round trips. The live drawer-resize browser proof crosses both hysteresis boundaries, preserves the stored shape exactly, then adds, selects, and drags a point with vertical time while proving the uploaded buffer matches the edited document.
    - The active time axis now labels 25%, 50%, and 75% of the current total in both orientations. The mounted product uses seconds because MSEG has no note-sync rate today; the shared ruler formatter already reduces quarter subdivisions for a future real notes mode without adding or mutating stored playback state.
    Current gap:
    - The shared MSEG drawing code already supports horizontal and vertical time, but the drawer and full-screen mounts leave it fixed horizontally.
    Required result:
    - Measure the actual editable graph area. When it is wider than tall, time runs left to right; when it is taller than wide, time runs top to bottom.
    - Re-evaluate while the drawer is being resized, including when dragging it upward crosses from a wide layout to a tall one. Use a stable rule near a square shape so the graph does not flicker between orientations.
    - Reorient the presentation and pointer math only. Do not rewrite, swap, or otherwise mutate the stored MSEG points when the available shape changes.
    Done when:
    - Browser coverage exercises both aspect ratios and a live resize across the boundary, proving that drawing, adding, selecting, and dragging points all use the visible time axis and preserve the same underlying envelope.

[x] T37 — Add one button that copies every Performance tuning setting
    Completed (2026-08-26):
    - Codex task `Implement T52 and T37 Developer Settings`, thread `01a03db8-dc75-71b3-b910-2389fcaf3567`, branch `codex/t52-t37-developer-settings`, worktree `/Users/winterfell/.codex/worktrees/5f3e/cosimo-synth`, model SOL, reasoning max.
    - The page now has one `Copy settings` action with pending, success, and rejected-write feedback. Its deterministic text projection covers every current Auto-preview algorithm and Mod drag feel value, and exhaustive typed projections make an added top-level or nested setting a compile-time obligation for the export.
    - Generated-build browser proof edits every value in both current sections, exact-compares the clipboard payload in stable order, and proves a rejected write never reports success.
    Required result:
    - Add a `Copy settings` button to the developer `Performance tuning` page. It copies the complete current tuning state—not only the MSEG controls—as one deterministic, readable text block suitable for pasting into a message or source change.
    - Include every nested value after live edits, and keep the export tied to the tuning-state definition so new settings cannot be silently omitted.
    - Show clear success or failure feedback; never say the copy succeeded if the clipboard write was rejected.
    Done when:
    - A clipboard test changes values in every settings section, presses the button, and proves the copied text contains the complete current state in a stable order.

[x] T38 — Add the synth's full Amp Envelope
    Completed (2026-08-27):
    - Codex task `T38 Build Full Amp Envelope`, thread `01a04334-3063-7d92-ab3b-544288810cb8`, branch `codex/t38-full-amp-envelope`, worktree `/Users/winterfell/.codex/worktrees/fd2f/cosimo-synth`, model SOL, reasoning max.
    - Coordinator thread `01a042bf-abd2-7820-9d47-b10405b67c5d` source-reviewed the clean task tip, returned the final UI identity/Release-floor repair, rebased the accepted 71-file scope onto current master without importing stale tracker or design text, and rebuilt every generated desktop/iOS/worker mirror from source.
    - Final integration qualification passes the complete Cmajor rack suite 82/82, orphan/unit suite 738/738, sound-share/preset/URL suite 28/28, and iOS host contract 82/82. The desktop UI suite is 260/261 with only the established unrelated React Grab `navigator.webdriver` assertion; all T38 cases pass.
    - A fresh HMR standalone was verified from the coordinator worktree with `usesViteClient: true` and then stopped. The final compiled, strict-code-sign-valid VST3 is installed at `/Users/winterfell/Library/Audio/Plug-Ins/VST3/CosimoDesktopNative.vst3` (executable SHA-256 `e5196b1d4a138d8bd57fe0c6525735d856da29633a2f3badb86619a712f5e223`). Physical Ableton and iPhone/AUv3 acceptance remain unperformed.
    Code-backed starting point:
    - The voice loudness path currently has a fixed 10 ms attack and an adjustable release, not a complete user-facing amplitude envelope. The recently added real decay stage belongs to the modulation envelopes, not to overall loudness.
    Locked signal behavior:
    - Replace that limited loudness shape with one shared Attack/Decay/Sustain/Release design. Each sounding note runs its own copy of the envelope; there is not one envelope applied after all notes have been mixed together.
    - Within each note, first combine Oscillators A, B, and C or the Bounce source, then run that combined note through the Voice Filter, then apply the Amp Envelope. Sum the completed notes only after that, and send the result into the Effects Lane.
    - Bounce must follow the complete Amp Envelope exactly like the oscillator mix. Remove its current special-case loudness behavior.
    - Reuse the synth's existing Poly, Mono, and Legato rules for note starts, connected notes, retriggering, voice stealing, velocity, note-off, and release tails. Do not invent separate restart rules for the Amp Envelope.
    Interface and modulation:
    - Add `Amp Envelope` beside Envelopes 1, 2, and 3 in the Mod tab, using the same envelope editor and design vocabulary.
    - Its permanent, non-removable job is controlling each note's overall amplitude at the signal point above.
    - Also expose it as a normal modulation source that can be assigned to other synth parameters under the same routing rules as Envelopes 1, 2, and 3.
    State migration:
    - Save and restore all four stages with the complete sound. Existing sounds that only store the old Release control must load with an audibly equivalent shape: the former 10 ms attack, full sustain, and their saved release.
    Done when:
    - Polyphonic tests prove that notes have independent envelope progress while sharing the same four settings, and Mono/Legato tests prove the Amp Envelope follows the already-established voice-mode restart rules.
    - Signal-path tests prove the envelope is applied once to each note after its oscillator-or-Bounce source and Voice Filter, before notes are summed and before effects.
    - Bounce, velocity, voice stealing, release tails, old-sound migration, Init, preset and URL recall, and host state all reproduce the expected loudness shape.
    - Modulation tests prove Amp Envelope is always connected to note amplitude and can simultaneously modulate another eligible parameter like a normal envelope.

[x] T39 — Add one modulatable Global Tune control
    Completed (2026-08-26):
    - Codex task `Implement Global Tune`, thread `01a03b80-6fc6-7cf1-b9c6-b77f17f435c6`, branch `codex/t39-global-tune`, worktree `/Users/winterfell/.codex/worktrees/b054cfa6-fe72-4fd4-9a5f-0c5571aea806/cosimo-synth`, model SOL, reasoning max.
    Code-backed starting point:
    - There is no synth-wide tune parameter today. Tuning and pitch modulation are oscillator-specific, and the pitch sent to effects currently inherits Oscillator A's private tuning and modulation.
    Locked behavior:
    - Put one continuous Global Tune control in the top-level Voice interface, outside the individual oscillators. Its knob runs from -24 to +24 semitones, is centered at zero, and is displayed in semitones/cents without snapping.
    - Make Global Tune a modulation destination. A route may contribute up to +/-48 semitones, using the existing unipolar or bipolar source behavior.
    - Define the shared played pitch as the MIDI note after glide, plus pitch bend, the Global Tune knob, and every current modulation contribution routed to Global Tune.
    - Apply each oscillator's own octave, semitone, fine tuning, and oscillator-local pitch modulation only after that shared pitch is established. Those private oscillator settings must never change the pitch followed by another control.
    - Transpose Bounce playback with Global Tune. A zero value remains completely neutral.
    State and integration:
    - Save and restore the knob, modulation routes, and modulation amounts through presets, URL sharing, Init, undo, and host state. Init sets the knob to zero.
    - Add the host parameter without renumbering existing host parameter slots.
    - Replace the Oscillator A-derived effects pitch feed with this shared played pitch so T50 has one correct source for Key Track.
    Done when:
    - Tests prove that +12 semitones doubles the pitch of every oscillator and Bounce playback; pitch bend, glide, knob movement, and modulation routed to Global Tune all reach the shared played pitch.
    - Oscillator-local tuning and pitch modulation remain relative to Global Tune but do not leak into the pitch followed by filters or effects.
    - Zero is neutral, the full knob and modulation ranges work continuously, and complete sound save/restore reproduces the same setting and routes.

[ ] T39A — Move Global Tune into the Voice settings popout and make it a usable modulation target
    Reported problem (2026-08-26):
    - The Global Tune control works, but its current location in the top-level Voice layout is wrong and is not accepted as the product design.
    Product placement decided (2026-08-27):
    - Put the one existing Global Tune control inside the Voice settings popout beside Voice Mode (Poly/Mono/Legato) and Glide Time. That popout is opened from the Mod bar. Do not add a preset-bar control or leave a second copy in the main Voice layout.
    - This location keeps synth-wide pitch behavior with the other synth-wide note behavior. The tradeoff is that the popout must remain usable during modulation routing instead of disappearing as soon as the person touches the Mod bar.
    Confirmed interaction blocker:
    - The current Mod bar explicitly closes the Voice settings popout whenever a modulation-source drag starts. Remove that behavior: touching or dragging a source must leave the already-open Voice popout visible.
    - While the source is dragged, Global Tune must present itself as an eligible destination and accept the drop. A successful drop creates the route from the dragged source, and the Global Tune control then edits that route's amount normally.
    - Switching to a different source in the Mod bar must update the armed source without closing the Voice popout. Explicit close and genuine outside-dismiss behavior may still close it after the source interaction is over.
    Move, do not redesign the feature:
    - Move the existing Global Tune control to the approved location and remove its old presentation completely. Keep one Global Tune control, not two synchronized copies.
    - Preserve all accepted T39 behavior unchanged: the -24 to +24 semitone knob, continuous cents, modulation up to +/-48 semitones, shared played-pitch math, Bounce and effects tracking, host parameter identity, automation, presets, URL state, Init, undo, exact entry, and modulation routes.
    - Keep the approved location reachable and legible at representative phone, desktop, and plugin sizes without crowding neighboring controls or changing their layout accidentally.
    Done when:
    - Browser tests open the Voice popout, touch and drag multiple Mod-bar sources without closing it, show Global Tune as the drop destination, create a route by dropping, and then edit that route's amount.
    - Browser and state tests prove exactly one Global Tune control appears in the Voice popout, the rejected location is gone, ordinary explicit/outside dismissal still works, and every existing T39 interaction and saved/automated value still drives the same underlying parameter.

[ ] T40 — Make the drag heads-up display match the parameter being changed
    Required result:
    - Keep the existing generic knob display for ordinary parameters, but allow the shared heads-up display to choose a purpose-built presentation for named controls.
    - Wavetable Index shows the real wavetable graphic moving through its index plus a large numeric value; it must reuse the production wavetable data and renderer rather than a decorative stand-in.
    - The main synth Filter Cutoff and the Effects Filter Cutoff show the correct live frequency-response graph plus a large Hz/kHz value, reusing the same response math as their editors.
    - MSEG Morph shows the actual realized envelope between Shape A and Shape B plus a large percentage, using the production interpolation and curve renderer. The persistent realized curve already landed in the drawer/full editor; this task owns replacing the remaining generic knob/readout HUD with the real morph graph.
    - Suppress the heads-up display only when the person is directly dragging the filter graphic, because the edited graph is already visible beneath the finger. Dragging the cutoff knob still shows the specialized display.
    Done when:
    - Browser tests select each named parameter and prove the correct graphic and current value appear, direct filter-graph dragging produces no duplicate display, MSEG Morph shows its realized curve at 0%, midpoint, and 100% with no generic knob, and an unrelated parameter still gets the generic knob.

[ ] T40A — Show the real Distortion Drive graphic instead of a knob in the heads-up display
    Required result:
    - While the Distortion Drive control is being adjusted, replace the generic knob picture in the heads-up display with the production Drive transfer-curve graphic.
    - Update the curve live as Drive changes, using the selected Symmetric, Asymmetric, or Wavefold shape and the same drawing code and curve math as the Distortion editor. Do not use a decorative copy or change the sound-processing behavior.
    - Add this to the shared heads-up display behavior described in T40; do not create a second floating display.
    Done when:
    - Browser tests adjust Drive with each distortion shape and prove that the correct production curve updates live, the generic knob picture is absent for Drive, and ordinary parameters still show the generic knob.

[x] T41 — Make every effect Mix or Wet control start at 50%
    Completed (2026-08-26):
    - Codex task `Effect Defaults and Mod Drawer Toggle`, thread `01a03a7c-26fa-7b70-aa0b-fd5914ac87ca`, commits `e5121978` and `18664be9`, model SOL, reasoning max.
    Required result:
    - A newly added or reset effect that exposes a Mix/Wet control starts at 50%, never zero or fully wet. The current known set is Distortion Wet, OTT Mix, Chorus Mix, Flanger Mix, Phaser Mix, Delay Mix, and Reverb Mix.
    - Update every source of the default together: effect descriptions, newly created lane state, DSP parameter annotations/constants, initial/reset state, factory defaults, mocks, and test fixtures. Preserve the different numeric units where 50% is represented as `0.5` versus `50`.
    - Do not overwrite an explicit Mix/Wet value recalled from an existing saved preset; this changes defaults, not user-authored values.
    Done when:
    - A test creates and resets every mix-bearing effect, verifies the visible value and the value delivered to DSP are both 50%, and guards future effects from silently choosing a different Mix/Wet default.

[x] T42 — Scale the floating Mod bar up by about 10% without disturbing its geometry
    Completed/superseded (2026-08-27):
    - The coherent roughly 1.10 scale is on master and deployed. The earlier fixed-size approval gate is superseded by T60, which will expose one whole-bar scale coefficient and let Andrew tune it live without repeatedly rewriting geometry.
    Active owner (2026-08-26):
    - Codex task `T42 Scale Floating Mod Bar`, thread `01a03bb7-9856-78d1-b2ca-781e3847eba0`, branch `codex/t42-floating-mod-bar-scale`, worktree `/Users/winterfell/.codex/worktrees/3a0a3eb6-5f1b-4c2d-935e-50f01a90c88f/cosimo-synth`, model SOL, reasoning max.
    Required result:
    - Increase the icons, modules, gaps, labels, badges, activity marks, arrows, outline, and real tap areas as one proportional system, using roughly a 1.10 scale as the starting point.
    - Preserve the current even spacing, visual centering, alignment, and left/right symmetry. Do not independently enlarge a few icons and leave the surrounding geometry behind.
    - Keep collapsed, expanded, paging, modulation dragging, and both left- and right-docked layouts inside the safe screen area at representative narrow and tall phone sizes.
    Done when:
    - Measured before/after overlays show a coherent roughly 10% increase with unchanged spacing ratios, no clipping or overlap, and no regression in drag targets or source selection; Andrew approves the visual result on the real composed interface.

[x] T43 — Let a second tap on the active Mod source close its drawer
    Completed (2026-08-26):
    - Codex task `Effect Defaults and Mod Drawer Toggle`, thread `01a03a7c-26fa-7b70-aa0b-fd5914ac87ca`, commits `e5121978` and `18664be9`, model SOL, reasoning max.
    Checklist audit (2026-08-27):
    - The separate old branch `codex/t43-reviewed` is obsolete and must not be merged. The completed behavior above is already on master; that old branch is based on pre-MSEG, pre-Global-Tune code and would revert newer work.
    Required behavior on the Voice and Effects screens:
    - Tapping a source while its quick drawer is closed selects it and opens that source's drawer.
    - Tapping the same source while its drawer is open closes the drawer.
    - Tapping a different source while a drawer is open keeps the drawer open and switches its contents directly to the new source, without a close/reopen flash.
    Interaction boundary:
    - Preserve modulation dragging and distinguish a completed tap from a drag before toggling anything. Preserve the Mod screen's existing source-panel behavior rather than introducing a second drawer there.
    Done when:
    - Browser interaction tests cover closed → open, open → closed, open source A → open source B, left/right docking, and tap-versus-drag without duplicate state changes or stale editor contents.

BROWSER SHARING AND ENVELOPE FOLLOW-UP

[x] T44 — Merge the MSEG Morph display work into the shared parameter-specific HUD task
    Administrative merge (2026-08-27):
    - Commit `fff44427` made the production realized A/B curve persist in the drawer and full editor and emphasize itself during Morph dragging.
    - The remaining missing behavior is the dedicated Morph graph inside the floating heads-up display. It is now an explicit part of T40 so there is one shared dynamic HUD system rather than a separate MSEG-only implementation.

[x] T45 — Keep browser audio playable after leaving the page and returning
    Completed (2026-08-26):
    - Codex task `Browser Audio Recovery`, thread `01a03a79-bd19-7b70-9648-b3d328b12e10`, branch `codex/t45-browser-audio-recovery`, commits `0738ef1d`, `9b5e8ad3`, and `e1fad3ef`, model SOL, reasoning max.
    - The recovery handles real page/window lifecycle changes without reconnecting the graph, clears held UI notes, preserves the patch, and gives Safari's gesture-gated restart a clear play-again path. The generated-product proof passes Chromium 5/5 and headed WebKit 4/4; Andrew confirmed the work complete.
    Reported failure:
    - After the browser synth has loaded and made sound, switching to another tab or window and then returning can leave it permanently silent in Safari and other browsers.
    Current baseline to investigate, not trust blindly:
    - The web host already requests Web Audio resume from a later touch and has synthetic interruption tests, but the real window/tab lifecycle still fails. Reproduce the actual sequence and record the audio-context state, playback-session state, worklet progress, graph connection, held notes, and output before choosing a fix.
    Required result:
    - On page return, resume automatically where the browser permits it. Where a browser requires a fresh gesture, the person's first intentional key or control touch retries audio and otherwise follows the ordinary input path. After any gesture-based restart, show a brief instruction to play again; if that first note was silent, release it normally. Do not classify the triggering control, delay note-off, or retain special recovery-note state.
    - Handle the real focus, visibility, page-show, and audio-context interruption signals without connecting the audio graph twice, leaking a worklet, leaving held notes stuck, or resetting the patch.
    Done when:
    - A regression reproduces the failure before the repair and proves non-silent output after repeated leave/return cycles in Chromium and WebKit.
    - A physical Safari pass switches apps/windows, returns, follows the browser's permitted gesture path, and confirms a subsequent note sounds. A test that only changes `document.hidden` in place is not sufficient evidence.

[ ] T46 — Requalify and finish the existing first version of patch sharing by URL
    Existing implementation:
    - Master already captures the current sound into a versioned `#p=1.<compressed-data>` URL fragment, exposes Share in the preset menu, asks before replacing the current sound, removes the fragment after loading, warns above 2,000 characters, and refuses above 8,000. Do not rebuild this machinery from scratch.
    Current audit (2026-08-27):
    - The real capture path already reuses the current preset contract plus the Effects Lane document, so this is completion and requalification work rather than a new sharing system.
    - Custom-wavetable refusal is still missing: the current shareability check rejects only sampled/Bounce sounds and does not prove that every selected wavetable is a shipped table available to the recipient.
    - The current browser round-trip test runs in Chromium and samples only a small subset of parameters/documents. It does not exhaustively fill and compare the present maximal sound, prove equivalent rendered audio, or exercise Safari/WebKit.
    - The 2,000/8,000 character limits are implemented, but they have not been remeasured against representative and maximal patches after Global Tune, the current Distortion state, current MSEG/articulation state, and the latest Effects Lane contract landed.
    Required first-version contract:
    - A link made from a patch using only wavetables shipped with Cosimo must restore the complete sound in a fresh browser session: every public parameter, both shapes and playback settings for every MSEG, every envelope value, modulation routes, articulations, oscillator and filter state, and the complete Effects Lane graph and effect settings.
    - Audit the current sound contract for fields added since the sharing work landed. Compare the captured document with a normal preset capture so no state is omitted merely because it lives outside the ordinary parameter list.
    - V1 carries shipped wavetable identity, not wavetable audio. Detect custom or otherwise unavailable wavetable data and refuse clearly rather than making a link that restores the wrong or silent table. Keep bounced/sample-backed sounds refused as they are today.
    - Measure representative and deliberately maximal current patches in Safari and Chromium. Retain or revise the existing 2,000-character warning and 8,000-character refusal only from those measurements and real copy/open behavior.
    Done when:
    - Two clean browser sessions round-trip the same maximal supported patch and compare the entire normalized sound document exactly, then produce the same audible/rendered state after load.
    - Desktop and phone browser surfaces can copy and open the link; malformed, truncated, unsupported, oversized, custom-wavetable, and cancelled-load cases fail visibly without changing the current sound.

[?] T47 — Add server-backed sharing for custom wavetables after URL-only sharing is proven
    Deferred second version:
    - When a shared patch uses a custom wavetable, store that wavetable on a Cosimo-controlled service and put a stable content reference in the shared patch. Loading the link downloads and verifies the exact table before applying the sound.
    Decisions required before implementation:
    - Choose the storage service, content-addressing scheme, upload limits, retention/deletion policy, ownership and privacy rules, abuse controls, and behavior for missing or expired assets.
    - Decide whether sharing is anonymous or account-bound and how a recipient can preserve the asset. Do not expand this task to bounced audio unless that is approved separately.
    Preserve:
    - The URL continues to carry the complete ordinary patch state. The server is only for binary wavetable data that cannot safely fit in the link, and a failed download must never silently substitute another table.
    Done when:
    - An approved design settles the open service rules, then a sender with a custom wavetable can create a link and a clean recipient session restores a byte-verified table and the exact patch; unavailable assets produce a clear non-destructive error.

[ ] T48 — Redraw ADSR envelopes responsively without stretching their handles, and settle the envelope color
    Confirmed drawing bug:
    - The compact ADSR editor sets the SVG to stretch freely to its container. That non-uniform transform stretches the entire diagram, including circular breakpoints, into long ovals.
    Required result:
    - Lay out the ADSR path, regions, labels, visible handles, and hit targets from the actual available width and height, or use another responsive method that never non-uniformly scales the finished drawing.
    - Visible breakpoint handles stay circular with consistent stroke weight and size in the short drawer, tall drawer, full editor, and during live resizing. The envelope remains legible rather than merely preserving circles inside a skewed curve.
    - Choose one reviewed envelope color treatment for both ADSRs and MSEGs and apply it through their shared design tokens. The exact replacement color remains open until it is viewed in the real interface; do not let the implementer pick it in isolation.
    Done when:
    - Geometry tests prove every visible handle's rendered width equals its height across representative aspect ratios and no path or hit target escapes the graph.
    - In-context screenshots cover the compact drawer, expanded drawer, and full editor, and Andrew accepts both the responsive shape and the shared color.

[ ] T49 — Make ADSR graph editing precise without covering the graph
    Current gap:
    - Dragging the decay/sustain breakpoint changes both decay time and sustain level on every move. There is no dominant-axis lock, no value next to the finger, and the sustain segment itself cannot be dragged.
    Required interaction:
    - Reuse the existing rolling axis-lock behavior. Once horizontal or vertical intent wins, keep that axis for the gesture: horizontal movement of the decay/sustain point changes Decay only; vertical movement changes Sustain only. One-dimensional Attack and Release handles remain constrained to their time value.
    - While any ADSR handle is dragged, show a small value label just above the finger that follows it without covering the breakpoint or curve. Show only the value actually being changed—Attack, Decay, or Release in time units; Sustain as a percentage. Do not open the large global heads-up display, and do not rely on the bottom toolbar.
    - Make the entire horizontal sustain segment a vertical drag target. Dragging it moves Sustain only and leaves Decay, Attack, and Release unchanged.
    - Preserve large invisible touch targets, pointer cancellation, exact value entry, and current parameter limits.
    Done when:
    - Touch tests prove diagonal gestures settle on one axis without cross-changing the other value, the sustain line can be acquired away from its breakpoint, and the floating value remains above the finger and inside the viewport at both screen edges.
    - Cancellation restores normal interaction state with no stuck label or extra parameter write, and mouse/pen editing remains usable.

[x] T50 — Add Key Track to controls that can be musically tuned
    Completed (2026-08-27):
    - Codex task `T50 Build Shared Key Track`, thread `01a0435e-576b-7c31-8de8-592b1c274673`, branch `codex/t50-shared-key-track`, worktree `/Users/winterfell/.codex/worktrees/fbf2/cosimo-synth`, model SOL, reasoning max.
    - Coordinator thread `01a042bf-abd2-7820-9d47-b10405b67c5d` source-reviewed and semantically rebased the clean task commits onto the T38/T66 master. The accepted result keeps T39's shared played pitch as the sole authority, preserves ordinary untracked values, and composes the append-only host/state/modulation changes without importing stale tracker or design text.
    - Shared continuous Key Track now covers every currently shipped qualifying control, including both Frequency Split markers and the new Flanger Base Delay/Tune. Delay Sync/Key Track and all enable sequences publish dependency-safe prefixes; pre-T50 Chorus sounds retain their exact nonlinear clamp through one hidden append-only migration flag, while newly authored Chorus state uses the continuous law.
    - Final integration qualification is effectively green across the complete Cmajor rack suite 88/88, orphan/unit suite 744/744, and desktop UI suite 263/264 after focused repairs to issues exposed by each one-time broad run. The sole remaining desktop red is the established unrelated React Grab `navigator.webdriver` assertion. Focused pitch authority/fallback/latch, target-base, state, preset/URL, exact-entry, route, generated-map, and T38/T66 composition checks pass.
    - A fresh HMR standalone reported the exact coordinator repo root and `usesViteClient: true`, then was stopped and port 5174 released. The final compiled-mode VST3 is installed and strict-code-sign valid at `/Users/winterfell/Library/Audio/Plug-Ins/VST3/CosimoDesktopNative.vst3` (installed executable SHA-256 `aad0efe08e6e5b5403efc75635a1612c67737eee0fa571c2b037bb2ae8825bfb`). No physical Ableton/listening, MIDI-controller, or iPhone/AUv3 acceptance is claimed.
    Dependency:
    - Build this on T39's shared played pitch. Do not derive tracking from any oscillator's private tuning.
    - This shared optional-Key-Track contract is also the prerequisite for the standalone Enhancer Lite release in T61 and the per-voice Enhancer in T62. The standalone effect follows the post-mix held-note rule below; while Key Track is on, each active synth voice calculates its own T62 Enhancer frequency from that voice's final played pitch.
    User contract:
    - Put the same compact button, literally labeled `Key Track`, beside each qualifying control. It is off by default, and its state is saved and restored with the complete sound.
    - Turning Key Track on sets the neighboring knob to its exact center: 1:1 ratio, 0 semitones, 0 cents. Do not convert the current Hertz or millisecond setting, do not use a reference note, and do not restore an earlier Key Track offset.
    - Keep the ordinary non-tracked value stored and untouched while Key Track is on. Turning Key Track off restores that value. Existing modulation assignments remain attached in both modes.
    - While Key Track is on, the knob remains fully modulatable. The knob is a continuous pitch offset, and each modulation route adds another continuous pitch offset; do not snap either one to semitones or integer harmonic ratios.
    Shared pitch math:
    - The pitch being followed is the played MIDI note after glide, plus pitch bend, the Global Tune knob, and all modulation routed to Global Tune.
    - Add the control's knob offset and all of its current modulation contributions in semitones, then convert once. A tracked frequency multiplies by `2^(offsetSemitones / 12)`. A tuned delay or comb period uses the reciprocal, so +12 semitones halves its time.
    - Choose and document a useful continuous knob and modulation range for each parameter type. One range must not be forced onto filters, crossovers, ring modulation, delay, flanging, phasing, and resonators when their safe and musically useful spans differ.
    - Clamp and smooth only after conversion. Preserve the existing unipolar/bipolar modulation behavior and modulation-ring preview.
    Controls that receive the button:
    - Voice Filter Cutoff; Effects Filter Cutoff; Distortion Wet High-pass and Wet Low-pass; Delay Filter cutoff; Delay Time itself; Phaser Frequency; Chorus Ring frequency; resonator and comb tuning controls; and both Effects Frequency Split crossover controls.
    - Add a Flanger Base Delay/Tune control and put Key Track there. Flanger Rate remains the LFO speed and Flanger Feedback remains the regeneration amount; neither gets Key Track.
    - Give future controls the button only when they tune a filter, oscillator-like frequency, resonator, comb, or delay period to the played pitch.
    Explicit exclusions:
    - Do not put Key Track on Flanger Rate, Phaser Rate, envelope Attack/Decay/Release, MSEG time, Glide time, reverb timing, dynamics timing, feedback amounts, or another control merely because its unit is Hertz or milliseconds.
    - A person may still assign ordinary keyboard tracking as a modulation source elsewhere. That existing modulation feature is separate and does not make those controls part of this task.
    Mode and note behavior:
    - Delay Time can use its ordinary value, tempo Sync, or Key Track—only one at a time. Never multiply tempo synchronization and pitch tracking together.
    - A control inside an individual synth voice follows that voice's own played pitch. A control in the post-mix Effects Lane follows the most recently pressed note that is still held; releasing it falls back to the next most recent held note. With no notes held, keep the last pitch latched so effect tails do not retune. This last-note rule is the current product choice and must be checked by ear during review.
    - Key Track on/off is shared across a control's per-note variations, while each variation may save its own offset.
    Exact entry and state:
    - With Key Track off, the long-press editor continues to edit the ordinary value in its normal unit. With Key Track on, label the base field `Key Track Offset`; label a route field with its source, for example `MSEG 1 -> Key Track Offset`.
    - Interpret submitted base and modulation values as the continuous ratio offset expressed in semitones/cents. Never submit them as Hertz or milliseconds, and do not add a separate live Hertz or time readout.
    - Save and restore the button state, ordinary value, per-variation offsets, and all modulation routes and amounts through presets, URL sharing, Init, undo, and host state. Migrate existing Chorus Ring settings and per-note variation settings without changing old sounds while Key Track is off.
    - Use one shared button style, state model, pitch feed, conversion implementation, exact-entry behavior, and test surface across Voice and Effects rather than separate formulas in individual effects.
    Done when:
    - An inventory test proves every named current control is wired and every explicit exclusion remains unchanged. With Key Track off, existing patches sound and behave exactly as before.
    - DSP tests prove octave and continuous inharmonic offsets, the reciprocal sign for tuned delays, Global Tune/pitch-bend/glide participation, parameter-specific limits, and smooth clamping.
    - Interaction tests prove enable centers the knob, active modulation still contributes, disable restores the untouched ordinary value, re-enable centers again, and exact entry writes the correct base or route offset.
    - Flanger tests prove Base Delay controls tuning while Rate and Feedback do not. Effects tests prove last-held-note fallback and the no-notes latch. Preset and URL round trips reproduce every mode, offset, variation, route, and amount.

RECOVERED FOLLOW-UP TASKS — AUDITED 2026-08-26

[ ] T51 — Let an effect be replaced from its graph-node menu
    Current gap:
    - Long-pressing an effect node opens its context menu, but that menu cannot replace the effect.
    Required behavior:
    - Add a menu item named `Swap`. It opens the same effect picker used by the graph's `+` buttons.
    - Choosing an effect replaces exactly the long-pressed node in the same trunk or branch position. Every other effect keeps its path and order. Cancelling the picker changes nothing.
    - Reuse the existing effect-removal cleanup so no parameter state or modulation route belonging only to the removed effect survives the replacement.
    Done when:
    - Browser tests cover replacement and cancellation in the trunk, a Parallel branch, and a Frequency Split band, and prove the resulting rack matches the pictured graph without changing unrelated nodes.

[x] T52 — Restore the developer settings menu on the deployed Codex site
    Completed (2026-08-26):
    - Codex task `Implement T52 and T37 Developer Settings`, thread `01a03db8-dc75-71b3-b910-2389fcaf3567`, branch `codex/t52-t37-developer-settings`, worktree `/Users/winterfell/.codex/worktrees/5f3e/cosimo-synth`, model SOL, reasoning max.
    - Root cause: the existing preset-menu entry was gated only by Vite's development mode, while Codex Sites uses a production Vite build. `sites:build` now explicitly opts into developer settings; ordinary web, standalone, and plugin production bundles keep the row hidden and exclude the page code.
    - Andrew explicitly approved public access to the developer controls on the public Cosimo Sites deployment on 2026-08-26. This is a deliberate product decision, not an access-control boundary.
    - Generated-build browser proof exercises the actual ordinary and Sites artifacts at phone size: preset actions remain present, the ordinary bundle cannot expose the page, the Sites bundle reaches the complete current Performance tuning page, and the restored dialog remains within the responsive viewport. Deployment was intentionally not performed by this task.
    Reported failure:
    - The developer settings page previously reached through the preset bar's three-dot menu is absent from the deployed Codex site.
    Required work:
    - Determine whether the deployed build hides it behind a build flag or whether the menu work never reached the deployed branch, then restore the intended developer access on the Codex site.
    - Keep ordinary-user exposure deliberate. Do not accidentally publish internal tuning controls to everyone merely to make Andrew's deployed development build usable.
    - T35's live MSEG response controls and T37's `Copy settings` button must be reachable through this restored page.
    Done when:
    - The deployed Codex site exposes the complete developer settings page to its intended user, the three-dot route is covered by a generated-build browser test, and the ordinary-user behavior is explicitly defined and tested.

[x] T53 — Make Init and a brand-new patch start with only Oscillator A
    Completed owner (2026-08-26):
    - Codex task `Fix Default Oscillator State and Level`, thread `01a03db9-1a78-7520-90cd-7d3982bb6910`, branch `codex/t53-oscillator-init-defaults`, worktree `/Users/winterfell/.codex/worktrees/d278/cosimo-synth`, model SOL, reasoning max.
    Required behavior:
    - Init and every canonical brand-new/default sound enable Oscillator A and disable Oscillators B and C. The disabled oscillators are visibly inactive and produce no audio.
    - Oscillator A starts at a normal, immediately usable synth level instead of the current extremely quiet value.
    Resolved tuning choice (2026-08-26):
    - Use the requested 0 dB oscillator level. The production Cmajor path remains below the transparent output-stage threshold at maximum MIDI velocity: A alone measures -20.87 dBFS RMS / -17.86 dBFS peak, while three phase-aligned A/B/C oscillators at their stored 0 dB level peak at -8.32 dBFS. The rejected -9.542425 dB value was exactly 9.542426 dB quieter. No alternate level or Andrew decision is required.
    State boundary:
    - Change the canonical default and Init behavior only. Loading a saved sound must preserve its explicit oscillator enable and level values.
    Done when:
    - New-sound and Init tests prove the UI state and audible DSP state agree, B and C are silent, A uses the approved level, and saved sounds remain unchanged.
    Completion evidence (2026-08-26):
    - One oscillator-default contract now feeds UI mocks, new-patch fallbacks, descriptors, articulation fixtures, browser/native state, and regenerated desktop/iPhone/speedrun bundles; the production Cmajor declarations agree exactly at A `mute=0`, B/C `mute=1`, and A/B/C `volumeDb=0`.
    - The generated Chromium product and desktop/iPhone surfaces prove fresh and post-Init state, saved version-3 document values, visible mute styling, and the 0 dB readout agree. A separate version-3 restore case preserves deliberately different A/B/C levels and enable values exactly; there is no migration or compatibility path.
    - The production native gain-path gate drives the real oscillator renderer through filter, fixed 0.18 trim, rack, and output stage. Renderer gains are exactly `1,0,0` for Init, B/C are individually audible at 0 dB when enabled, all-muted output is exactly zero, and all three at 0 dB remain below the limiter threshold.
    - Focused defaults/state passed 25/25, articulation mutation 6/6, renderer/generated/shared-engine gates passed, Cmajor rack passed 79/79, orphan units passed 729/729, and web bundle contract passed 20/20. Current web and iPhone bundles were rebuilt.
    - A fresh HMR standalone is running from this worktree against `http://127.0.0.1:5195` (`usesViteClient: true`; canonical 5174 was owned by another live worktree). The compiled, strict-code-sign-verified VST3 is installed at `/Users/winterfell/Library/Audio/Plug-Ins/VST3/CosimoDesktopNative.vst3`.
    - Adversarial follow-up on the rebased branch single-sourced the production TypeScript base-level range, added Cmajor min/max parity coverage, and removed the unimported generated oscillator-binding module without changing Init or saved version-3 behavior.

[x] T54 — Put the right-side wavetable chips back against the right edge
    Completed owner (2026-08-26):
    - Codex task `Restore Wavetable Right-Edge Controls`, thread `01a03db9-3d6d-7f33-a793-65284f8ec48a`, branch `codex/t54-wavetable-right-edge-controls`, worktree `/Users/winterfell/.codex/worktrees/57a0/cosimo-synth`, model SOL, reasoning max.
    Reported regression:
    - The top-right `Warp` chip and the existing lower-right chip were pushed too far inward during refactoring. Their right inset no longer mirrors the left-side chips' left inset.
    Required result:
    - Restore symmetric edge spacing: each right-side chip sits as close to the right edge as its corresponding left-side chip sits to the left edge.
    - Preserve the existing wavetable graphic, chip sizes, controls, hit areas, and interaction behavior; this is a placement repair.
    Done when:
    - Geometry checks and real-interface screenshots at representative phone, desktop, and plugin sizes prove the left and right insets match and no chip clips, overlaps, or covers important wavetable content.
    Completion evidence (2026-08-26):
    - The accepted placement remains fixed: the Mod rail's 44 px dock is no longer added to the corner controls' original inset, so compact wavetable corners stay at mirrored 8 px graph insets on both rail edges. The wavetable graphic and the controls' dimensions, labels, hit areas, bindings, gestures, and modulation behavior are unchanged.
    - Final correction (2026-08-27): the Mod-bar movement component and compact geometry test were restored exactly to original T54 commit `aadb3d12`. All later Voice collision projection, toolbar-gutter, forced-expansion, measurement-replay, and elevated-control layering experiments are removed.
    - The Mod bar stays above ordinary Voice controls at an overlap. The browser startup screen contains the complete synth beneath it, so no internal control can paint or receive input above `Start audio`.

[x] T54A — Restore free Mod-bar movement, usable expansion, and the full-width wavetable parameter row
    Completed owner (2026-08-27):
    - Codex task `Fix wavetable Mod bar regression`, thread `01a04027-1528-7083-ae49-f16ec0eb5e3c`, branch `codex/t54a-wavetable-mod-bar-regression`, worktree `/Users/winterfell/.codex/worktrees/63ff/cosimo-synth`, model SOL, reasoning max.
    Reproduced regression:
    - On the real 320 x 568 composed browser surface, the right-docked Mod bar reserved a blank 44 px column in the wavetable parameter row; a 100 px upward grip drag moved the bar 0 px; and Expand produced only a 35 px drawer sliver.
    Actual cause:
    - T54 follow-up commits `5d10370b` and `24fd1d60` turned the wavetable corner chips and page arrows into vertical Mod-bar keep-outs, projected every pointer move into the nearest permitted segment, clipped the drawer to that segment, and added a Mod-bar-width toolbar column when the segment was too small.
    Required result:
    - Keep all four wavetable corner chips at the accepted mirrored 8 px graph inset.
    - Keep the bottom wavetable parameter row full width on both Mod-bar edges; never reserve a blank Mod-bar-width column.
    - Let the floating Mod bar follow vertical drags continuously through its normal fixed-chrome travel range on the Voice page. Wavetable chips and page arrows must not trap it in one segment.
    - Expansion must expose a genuinely usable drawer on short phones, not a one-source sliver. Preserve the 10% Mod-bar scale, edge docking, source tap toggle, page-arrow interaction, and chip hit areas.
    Completion evidence (2026-08-27):
    - Removed Voice-control keep-outs and the short-phone 44 px edge gutter. The rail now follows the finger through the fixed-chrome vertical band while the parameter row remains full-width; all four wavetable chips keep their mirrored 8 px graph insets.
    - Final correction restores `aadb3d12` as the exact Mod-bar movement/expansion implementation. The layer-61 Voice rule and the tests that approved chips hiding the bar are removed; the compiled 320 px surface now proves the bar owns the overlap, moves continuously, and expands.
    - A generated iPhone-profile browser check proves the startup screen owns the rendered pixels over Warp, both parameter-page arrows, and the Mod bar until audio starts. The complete Voice/Mod-bar browser suite passes 50/50, compiled integration passes 2/2, pure geometry passes 23/23, and the original T42 phone-geometry check remains green. The current compiled VST3 is installed at `/Users/winterfell/Library/Audio/Plug-Ins/VST3/CosimoDesktopNative.vst3`.
    - Physical iPhone finger comfort and Ableton interaction remain manual acceptance; neither is claimed by browser automation.

[x] T55 — Add exclusive branch Solo to Frequency Split and Parallel groups
    Active owner (2026-08-27):
    - Codex task `T55 Build Exclusive Branch Solo`, thread `01a0435c-7cf6-75c0-8fbb-c2eadc6901d0`, branch `codex/t55-exclusive-branch-solo`, worktree `/Users/winterfell/.codex/worktrees/72e4/cosimo-synth`, model SOL, reasoning max.
    - This is a coordinator-managed implementation task. Final review, rebase, merge, push, and any deployment belong to coordinator thread `01a042bf-abd2-7820-9d47-b10405b67c5d`.
    Required behavior:
    - Give every Frequency Split band and every Parallel branch a small Solo control so one branch can be heard by itself.
    - Solo is exclusive within that group: tapping an unsoloed branch isolates it; tapping another switches the solo directly; tapping the active Solo again returns every branch to normal.
    - Different groups own independent Solo choices. Solos in separate upstream or downstream groups may remain active together, allowing the user to isolate one complete route through multiple groups. Activating Solo in one group must never clear Solo in another group.
    - Support all current two- and three-band Frequency Split layouts and every supported Parallel branch count. The control must remain usable in the compact graph.
    Temporary state boundary (settled 2026-08-27):
    - Solo is audition state, not part of the sound. Never save it in presets, URL sharing, host projects, or rack documents. It remains active while navigating the same running synth.
    - Loading any preset or pressing Init clears every active branch Solo. Deleting a soloed branch or its containing group clears that group's Solo without disturbing Solo choices in other surviving groups.
    - Solo must never reorder, delete, reroute, or otherwise rewrite the rack.
    Done when:
    - DSP and browser tests prove exactly the selected branch is heard, switching and clearing work without changing the rack, and the controls remain visible and tappable in representative phone, desktop, and plugin layouts.
    Completion evidence (2026-08-27):
    - Coordinator thread `01a042bf-abd2-7820-9d47-b10405b67c5d` source-reviewed and semantically replayed the clean owner commits onto current T38/T66/T50 master, without importing stale tracker text. Solo is a WeakMap-owned runtime audition event: it never enters lane documents, presets, URLs, or host state; successful sound replacement clears it while ordinary navigation on the same patch connection preserves it.
    - Review repairs made the first WebView synchronization explicit, retained independent choices across sequential groups, kept every branch processor advancing, and proved the existing 3 ms transition around switches. At 320 px the right-rail Split editor now stacks its crossover and Key Track controls into the one owned track; phone, plugin, and desktop tests prove full containment and pointer ownership.
    - Final integration gates pass the complete Cmajor rack suite 91/91 and orphan/unit suite 745/745. The desktop UI suite is 266/267: every T55 and adjacent composition case passes, with only the established unrelated React Grab `navigator.webdriver` assertion red. Focused Solo lifecycle, generated-map provenance, and compact composed geometry checks also pass.
    - A fresh HMR standalone reported this coordinator repo root and `usesViteClient: true`, then was stopped and port 5174 released. The final compiled-mode VST3 is installed and strict-code-sign valid at `/Users/winterfell/Library/Audio/Plug-Ins/VST3/CosimoDesktopNative.vst3` (installed executable SHA-256 `a051799aceeef62b481af78398ca800851794e7f12a550d5dd57925377d2c3ef`). Physical Ableton/listening and iPhone touch/audio acceptance were not performed.

[x] T56 — Make Init recover from the Distortion Type state change and show one error
    Active owner (2026-08-26):
    - Codex task `T56 Fix Init recovery`, thread `01a03bb6-e997-7fb1-b59b-1fb2e334c46f`, branch `codex/t56-init-distortion-upgrade`, worktree `/Users/winterfell/.codex/worktrees/2cc18342-dcbc-4c28-aeb1-ab56b7df7d81/cosimo-synth`, model SOL, reasoning max.
    Reported failure (2026-08-26):
    - On the deployed mobile browser synth, choosing `Init` from the preset bar produced two identical messages: `lane.v2 device distortion#1 must carry every parameter once`.
    Actual cause:
    - Distortion gained the required saved `distortionType` value, but the browser still restored version-2 saved sounds containing the old six-value Distortion record. Init then correctly rejected that record while capturing the current sound for atomic rollback, so the reset never began.
    - The controller both returned the failed mutation and published the same message as `lastError`; the preset bar displayed both paths, which caused the two identical messages.
    Accepted repair (corrected 2026-08-26 — no migration or backwards compatibility):
    - Hard-cut the complete browser saved-sound payload from version 2 to version 3. Version-2 sounds are ignored as a whole at startup; no old field is parsed, filled, copied, or upgraded.
    - Leave Init and the strict lane schema unchanged. Valid version-3 state restores normally; corrupt version-3 state still fails before any partial reset; the next real Init writes the complete current defaults.
    - Use controller `lastError` as the sole visible controller-error channel so one failed action creates one message and a later independent identical failure remains visible.
    Coordinate with T53:
    - T56 restores the reliability of the existing Init transaction. T53 separately changes the intended oscillator defaults and should build on the repaired path rather than editing the same Init code in parallel.
    Done when:
    - The generated browser product seeds the exact version-2 six-value record, proves startup ignores it, presses the real preset-bar Init command, and ends on the complete current version-3 `INIT` sound.
    - Clean Init, `Discard and Init`, valid current state, corrupt current state, atomic rollback, and one visible message per genuine failed action remain covered. The current web and iPhone bundles are rebuilt and the focused preset/state suites remain green.
    Completion evidence (2026-08-26):
    - The generated test failed before the version cutoff with the exact old Distortion record and `oscBPan: 0.75` still live; Init left both untouched and reported the strict missing-parameter error. After the cutoff, Chromium and WebKit both start clean and real Init writes version 3 with `distortionType: 1` and no error.
    - Focused lane-state passed 10/10, Init passed 24/24, and preset/controller/bar passed 31/31. Current version-3 restore and corrupt-state rejection remain strict; late Init failure still rolls back the complete sound. The web contract/speedrun set passed 25/25, Bounce persistence 12/12, and the rebuilt iPhone surface 20/20.
    - `npm run web:build` and `npm run ios:ui:build` rebuilt the checked-in browser and iPhone bundles. A fresh HMR standalone is running from this worktree against `http://127.0.0.1:5179`; the compiled VST3 is installed and strict-code-sign verified at `/Users/winterfell/Library/Audio/Plug-Ins/VST3/CosimoDesktopNative.vst3`.

[x] T57 — Remove the Create Mapping button from effect editors
    Active owner (2026-08-27):
    - Codex task `Remove effect mapping button and header text`, thread `01a0426b-19ee-7db0-ba88-e21192770e6a`, branch `codex/t57-t58-effect-editor-cleanup`, worktree `/Users/winterfell/.codex/worktrees/cf28/cosimo-synth`, model SOL, reasoning max.
    Required result:
    - Remove the `CREATE MAPPING +` button from the modulation area of every effect editor. Do not replace it with another button or prompt.
    - Remove the space reserved for the button so the effect editor does not retain an empty button-shaped gap.
    - Preserve the existing ways modulation mappings are created, edited, and removed elsewhere; this task only removes this redundant control.
    Done when:
    - Browser tests prove the button and its reserved space are absent for mapped and unmapped effect parameters on representative phone and desktop layouts, while existing modulation gestures still work.
    Completion evidence (2026-08-27):
    - The production `CREATE MAPPING +` component, reserved row, and obsolete styles are gone. Compiled-browser coverage at 393x852, 1120x680, and 1440x900 proves mapped and unmapped modulation areas use the same height and that a real source-to-target drop still creates a route.
    - Coordinator review removed one dead selected-route derivation and required a fresh desktop bundle/source-map rebuild. The post-rebase source-map provenance and compiled-shadow-bundle checks pass, and the generated map embeds the exact current source without checkout-path leakage.

[x] T58 — Show only the effect name in each effect editor header
    Active owner (2026-08-27):
    - Codex task `Remove effect mapping button and header text`, thread `01a0426b-19ee-7db0-ba88-e21192770e6a`, branch `codex/t57-t58-effect-editor-cleanup`, worktree `/Users/winterfell/.codex/worktrees/cf28/cosimo-synth`, model SOL, reasoning max.
    Required result:
    - Every effect editor header shows only the effect name. Remove descriptive subtitles such as `Classic clipping or harmonic-residue saturation.` and remove status text such as `FX BYPASSED`.
    - Keep the existing power control and actual bypass behavior; bypass state must not add text back into the heading area.
    - Apply the same rule to every effect, not only Distortion, and reclaim the removed text's vertical space.
    Done when:
    - Browser tests inspect every effect while active and bypassed and prove the heading area contains only its effect name, with no subtitle, bypass message, clipping, or leftover blank row at representative phone and desktop sizes.
    Completion evidence (2026-08-27):
    - All eight effect editors, active and bypassed, render only the effect name plus the existing power control at phone, plug-in, and desktop sizes. The focused layout matrix proves a compact header, no blank row or clipping, and a power hit area of at least 44 px; group-editor heading copy remains unchanged.
    - The final desktop UI gate was 258/260: all T57/T58 product checks passed, the established React Grab `navigator.webdriver` assertion remained red, and one unrelated Vite optimize-cache startup failure passed immediately in isolated rerun. The complete layout gate was 27/28 with only the same React Grab assertion. A fresh HMR standalone is live at `http://127.0.0.1:5174`; the compiled VST3 is installed and strict-code-sign valid at `/Users/winterfell/Library/Audio/Plug-Ins/VST3/CosimoDesktopNative.vst3` (executable SHA-256 `4f003882d8fc0fa96d783b222edd1c8f7fee1d36207b2088f0849645d1110a7c`). Physical iPhone and Ableton interaction acceptance were not performed.

[x] T59 — Make Sine → Square → Triangle → Saw the default oscillator wavetable
    Active owner (2026-08-27):
    - Codex task `T59 Build Four-Frame Default Wavetable`, thread `01a04337-a792-7d21-a860-49d41f801ddc`, branch `codex/t59-four-frame-default-wavetable`, worktree `/Users/winterfell/.codex/worktrees/a8a5/cosimo-synth`, model SOL, reasoning max.
    - This is a coordinator-managed implementation task. Final review, rebase, merge, push, and any deployment belong to coordinator thread `01a042bf-abd2-7820-9d47-b10405b67c5d`.
    Required result:
    - Create one license-safe wavetable containing exactly four stored frames in this order: Sine, Square, Triangle, and Saw. Every frame is one complete single-cycle waveform. Do not insert crossfade, transition, duplicate, or intermediate frames.
    - Do not change Index behavior, interpolation, modulation, display, dragging, or any other wavetable-engine behavior. This task constructs the four-frame table and selects it as the default only.
    - Add it to the shipped factory wavetable catalog with a stable identity and make it the selected table for every oscillator in Init and every brand-new patch.
    - Greenfield decision (2026-08-28, Andrew): old saved sounds, browser state, shared URLs, and host automation impose no backwards-compatibility requirement. Do not add migration, aliases, a dual catalog, or preservation-only code/tests for unpublished pre-T59 state. The accepted current selector contract may include the new table directly.
    Done when:
    - The shipped table contains exactly four frames and each frame is mathematically and visually verified as Sine, Square, Triangle, and Saw in that order. Tests prove Init/new-patch selection uses it for Oscillators A, B, and C, current-format save/Init/recall still works, and no Index-control or interpolation code changed. No old-format compatibility gate is required.
    Completion evidence (2026-08-28):
    - Coordinator commit `5108e3ba` semantically rebases the owner implementation onto current master; `2e6f4961` removes obsolete preservation-only assertions, regenerates current composed workers/maps, and repairs the native gain-path fixture to upload the metadata-declared default and apply the real Amp Envelope defaults.
    - The shipped `core-shapes` table contains exactly four analytic 2,048-sample frames in the required Sine, Square, Triangle, Saw order. Its WAV is byte-reproducible with SHA-256 `ad6dd10ef0ea14631fb60c7103c575888dec75c7ba6ac89aeba1ca4212657993`; A/B/C Init defaults are selector 35 and current-format save/Init/recall is green. Andrew's greenfield ruling permits the one-time 35–237 shift; placing Core Shapes immediately after the existing 0–34 hosted bank keeps Sites small while including the real default.
    - Final focused qualification passes the default table contract 3/3, oscillator/Init contract 27/27, wavetable-bank suite 30/30, current Speedrun default proof, generated source-map provenance, web build, and the production oscillator-to-output native gain path. Index interpolation/control sources remain unchanged. No Ableton, listening, physical iPhone, or other physical-device acceptance is claimed.

[x] T60 — Make the Mod bar scale adjustable and let it float or park horizontally
    Active owner (2026-08-27):
    - Codex task `T60 Build scalable parkable Mod bar`, thread `01a04429-7283-7553-bf22-8c7633b1e892`, branch `codex/t60-scalable-parkable-mod-bar`, worktree `/Users/winterfell/.codex/worktrees/bf11/cosimo-synth`, model SOL, reasoning max.
    - This is a coordinator-managed implementation task. Final review, rebase, merge, push, and any deployment belong to coordinator thread `01a042bf-abd2-7820-9d47-b10405b67c5d`.
    Product decisions (2026-08-27):
    - Replace the fixed visual-size decision with one scale coefficient that proportionally controls the complete Mod bar: shell, icons, labels, badges, gaps, arrows, outline, and actual touch targets.
    - Put a live coefficient control in Developer Settings and include it in `Copy settings`, so Andrew can tune the whole bar without code changes.
    - Scale and placement are application preferences, not part of a sound. Start live tuning from the current 1.10 scale with a proposed 0.85-1.30 adjustment range; Andrew chooses the shipped default during the composed-interface tuning pass.
    - Offer three explicit placements: floating left, floating right, or parked horizontally immediately above the bottom Voice/FX/Mod strip. Do not merge it into the preset bar and do not switch placement automatically at a breakpoint.
    Parked behavior:
    - The visible parked bar is exactly the same height as the Voice/FX/Mod strip below it. It is one horizontal row and never expands into a larger source-list drawer. Navigate its source/tool groups horizontally within that row.
    - Add a small chevron that hides the complete parked row. Hidden means the row reserves zero vertical space. Leave only a tiny restore-chevron tab emerging from the top edge of Voice/FX/Mod and overlaying the page content, so even the restore affordance consumes no layout height.
    - Tapping the restore tab animates the parked row back into view. Hiding and restoring must preserve the selected source, current source group/scroll position, open source editor or Voice popout, route count, audition settings, and pending application preferences. The restore tab must remain reachable without covering an important page control.
    - In parked mode, keep the row height fixed and let the coefficient scale the contents, widths, gaps, and horizontal density inside that height; every source still owns a full-row touch target. Floating mode may scale the whole shell normally. Do not make the bottom Voice/FX/Mod strip taller merely to follow the Mod-bar coefficient.
    Parked interaction:
    - Use left/right paddles to cycle through source/tool groups. Do not use free horizontal swiping because a horizontal drag beginning on a source icon belongs to the existing drag-to-map gesture.
    - Product decision (2026-08-28): while the parked row is visible, the trigger-note audition button is permanent right-side chrome immediately before the right paging paddle. It must remain visible and reachable while source/tool pages change; do not place it inside a pageable group or let another control occupy that position. Hiding the complete parked row may still hide it together with that row.
    - The parked bar itself never expands. Tapping a source still opens its existing MSEG/Envelope/Macro quick editor, and the Voice button still opens its Voice settings popout. Those editors float independently and do not enlarge the parked bar.
    Done when:
    - Andrew approves the real composed floating and parked designs and the coefficient feel.
    - One setting changes placement without losing the selected source, open editor/popout, mapping gesture, or audition state. The coefficient scales each placement without overlap, clipping, uneven spacing, or changing the parked row's required height.
    - Hide/show animation leaves no permanent blank row, the tiny restore tab remains usable, and neither state blocks page content or the bottom navigation.
    - Scale, placement, and parked visibility survive relaunch as application preferences, do not change a saved patch's sound, and are covered at representative phone, desktop, and plugin sizes.
    Coordinator integration checkpoint (2026-08-27):
    - The reviewed implementation and two focused repairs are integrated from owner tips `d9ac2108`, `9286a573`, and `68c48b48`. Scale, explicit floating-left/floating-right/parked placement, parked visibility, source-page continuity, editor/popout continuity, and application-only persistence are present without entering sound, preset, URL, host, or modulation state.
    - The final 320 px matrix proves every parked source/tool target is at least 44 px wide and 40 px high with owned inset hit probes. Hiding/restoring preserves the same quick-sheet and full-MSEG DOM instances, while a live floating-to-parked change preserves a real drag through route creation.
    - Moving-master review caught and repaired an already-selected MSEG tap that silently moved the visible source page and prevented T66's MSEG 1 -> MSEG 2 Rate drag from starting. The exact composed T66 route/create/edit case and T60 live-placement drag case both pass after repair.
    - The complete desktop UI run was 272/274 before that focused repair: all seven T60 cases passed, the established React Grab `navigator.webdriver` assertion remained red, and the T66 composed regression was repaired and passed in isolated rerun as required by the integration protocol. Generated Developer Settings/Sites proof passes 1/1; source-map provenance and focused strict TypeScript are clean.
    - Fresh HMR was verified from coordinator worktree `74e4` at `http://127.0.0.1:5174` with `usesViteClient: true`, then stopped. A final compiled-mode build installed and strict-code-sign verified `/Users/winterfell/Library/Audio/Plug-Ins/VST3/CosimoDesktopNative.vst3`; installed executable SHA-256 is `59555f776e70712c51a0ce8a2a02338a68d0d948ac72629d2b7d48317e28c7a5`.
    - Andrew approved the repaired parked contract on 2026-08-28 and directed the coordinator to finish the queue. The trigger-note button is permanent right-side chrome immediately before the right paddle on every source/tools page, and the established 1.10 coefficient remains the shipped default within the adjustable 0.85-1.30 range.
    - The focused real-browser T60 matrix passes 8/8, including 393x852 phone proof of the permanent trigger-note position on source and tools pages; the composed T66 MSEG 1 -> open MSEG 2 Rate drag remains green. The final composed Cmajor rack gate passes 93/93. Moving-master browser fixtures exposed by the complete desktop/orphan runs were repaired and passed their focused full-file reruns; the only unrepaired desktop diagnostic remains the established unrelated React Grab `navigator.webdriver` assertion.
    - Fresh coordinator HMR launched the exact `74e4` standalone and the compiled phone render was reviewed. The final self-contained VST3 includes the pinned Cmajor runtime and is installed at `/Users/winterfell/Library/Audio/Plug-Ins/VST3/CosimoDesktopNative.vst3`; strict deep code-sign verification passes and its executable SHA-256 is `8a6754146b8c1a2b588798a2807d4c93a5f0edaf4996791783bd7cd2fdc198cd`. The replaced development-server bundle is recoverable from `/Users/winterfell/Desktop/CosimoDesktopNative-before-d1a9e51b.vst3`. Ableton remained open and was not disturbed, so no fresh host interaction/listening or physical iPhone/touch acceptance is claimed.

[ ] T61 — Package Enhancer Lite as a shippable free VST3 and iOS AUv3 product
    Existing starting point:
    - Cosimo Enhancer Lite is the one-band standalone spin-off, not the two-band Polish processor and not the eventual per-voice processor. Its DSP, direct-manipulation editor, analyzer, wordmark, state tests, signed universal macOS VST3, and pluginval proof start on `codex/t26-spectre-wrapper-prototype`. Treat that as source material, not as a finished public release.
    - Preserve Lite's cheaper 4x polyphase-IIR wrapper and truthful 3-sample latency. Do not replace it with Cosimo Enhancer's high-quality 60-sample FIR wrapper.
    - Two child branches must be composed rather than choosing one: `codex/t26-enhancer-lite-draggable-readouts` at `19a67d0c` adds draggable numeric readouts, while `codex/t26-enhancer-lite-shelves` at `0c5a73cd` adds the measured Low/Bell/High shapes, corpus proof, and Ableton audition evidence. Both descend independently from `2a652a40`; the shelf branch does not contain the readout work.
    Coordinator composition checkpoint (2026-08-27):
    - Coordinator commits `887757f3` and `47dadd75` semantically compose both child tips onto current master without importing their stale tracker copies. Low/Bell/High shapes, the 3-sample polyphase-IIR wrapper, Stereo/Mid-Side routing, separate Mid and Side amounts, Tube/Solid, Subtle/Medium, analyzer, and direct graph/readout manipulation all remain present; the shelf audition target stays excluded from production `all`.
    - Final automated qualification passes Cmajor 7/7, state/build 18/18, source/compiled browser 24/24, the complete accepted Lite audio comparison, and the complete 284-case shelf comparison. A fresh local Spectre capture reproduced the pinned report/measurement hashes, authenticated 20 inputs plus 284 outputs, and passed the copied-WAV tamper test.
    - The clean universal production VST3 installs at `/Users/winterfell/Library/Audio/Plug-Ins/VST3/CosimoEnhancerLite.vst3`, passes strict deep code-sign verification and pluginval strictness 5, and has executable SHA-256 `792534550f639a57b5cdc2d0cb2c4c0944edc3a58470203ccee7a1e70a3e8737`. An independent VST3 host reports the required 3-sample latency.
    - This checkpoint completes only sibling composition and automated macOS development-plugin qualification. No fresh composed Ableton listening/editor acceptance or physical iPhone/AUv3 acceptance is claimed, and T61 remains open for MIDI tracking, release packaging, iOS AUv3 delivery, branding/installation flow, and Andrew's acceptance.
    Required product:
    - Produce a clean free-product source boundary and reproducible release build for VST3 and iOS AUv3, with stable product/bundle identities, preset/state compatibility, packaged resources, versioning, licenses/credits, release notes, and install/validation instructions.
    - Add the approved MIDI pitch-tracking behavior rather than shipping a permanently fixed-frequency effect. Settle the standalone plugin's held-note/last-note behavior, pitch-bend handling, no-note latch, and continuous harmonic-ratio offset against T50's musical rules.
    - Preserve the current direct graph interaction, Stereo/Mid-Side routing, separate Mid and Side amounts, Tube/Solid character, Subtle/Medium intensity, fixed de-emphasis behavior, analyzer, Low/Bell/High shapes, and zero-effect dry behavior unless Andrew explicitly changes that product.
    - Validate VST3 in Ableton and the AUv3 in the real iPhone host/app. A browser preview, JIT loader, or locally installed development binary is not a shippable product.
    Done when:
    - Clean release artifacts for VST3 and iOS AUv3 build from documented commands, retain state across reload/update, pass signing and host validation, and are installable by someone without the development checkout.
    - Andrew approves the sound, MIDI tracking, interface, free-product branding, and installation flow.

[ ] T62 — Put a lightweight one-band Enhancer after the filter inside every synth voice
    Dependency:
    - Build on T39's shared played pitch and T50's approved `Key Track` state, offset, exact-entry, modulation, and last-held-note behavior. Do not create a third pitch-tracking formula specifically for this effect.
    Product decisions (2026-08-27):
    - The required Cosimo form is one Enhancer inside every active synth voice, immediately after that voice's filter and before its amplitude envelope/VCA and the final voice sum. Its purpose is early per-note tone shaping; a single processor after the voices are combined does not satisfy this requirement.
    - Add optional Key Track. With Key Track off, Frequency is an ordinary fixed frequency shared by the voices. With Key Track on, every active voice follows its own final played pitch and applies the same continuous harmonic-ratio offset, so a chord produces one correctly tracked Enhancer frequency per note. Preserve the non-tracked Frequency value while tracking is on and restore it when tracking is turned off, following T50.
    - Always process each voice in linked Mid/Side. Encode that voice's stereo filter output with the established Enhancer Lite half-sum/half-difference matrix, apply the same Frequency, Q, and Amount to Mid and Side through one fixed Tube-family curve, then decode back to stereo. This deliberately keeps the nonlinear Mid/Side sound rather than trying to reproduce ordinary linked-stereo processing.
    - A separate Enhancer Lite device may be added to the Effects Lane later, but it is optional additional work and cannot replace this required per-voice stage.
    - Keep this separate from T26's fixed two-band Polish Enhancer, which remains a single post-rack bus processor near the end of the complete synth signal path.
    Controls and placement decided (2026-08-27):
    - Expose exactly three adjustable values: Frequency when Key Track is off, Ratio when Key Track is on, Q, and Amount. Use the one fixed shaping curve internally; do not expose another sound-character control or persist hidden mode state.
    - Reuse the existing Voice filter area as a two-stage `FILTER` → `ENHANCER` surface instead of stacking another panel and stealing vertical space. Explicit taps switch the displayed stage; do not use a horizontal swipe because horizontal graph movement edits Frequency/Ratio. The Filter view remains unchanged.
    - In the Enhancer view, show the draggable bell in the existing graph footprint and use the existing three-cell bottom row for Frequency/Ratio, Q, and Amount. Horizontal bell drag edits Frequency/Ratio, vertical drag edits Amount, and the Q control remains directly editable; modifier-assisted Q dragging may remain on platforms with a keyboard.
    - Put the small `KEY TRACK` mode button inside a corner of the graph. Its state changes the first control's label and interpretation between `FREQ` and `RATIO`; it is not a fourth adjustable value.
    - This is one shared control surface for the settings used by every per-voice Enhancer instance. Do not put it on the Effects page, hide it in a generic settings popout, or show a separate panel for every sounding note.
    Required integration:
    - Reuse Enhancer Lite's one-band selection/filtering behavior, residue sound, and exact Mid/Side encode/decode convention behind a per-voice Cosimo adapter. Do not instantiate either Cosimo Enhancer's high-quality 4x FIR wrapper or standalone Lite's 4x IIR wrapper inside every voice. Use the dedicated first-order ADAA residue implementation in `ENHANCER_DESIGN.md`, with no oversampling wrapper or resampler. Give every allocated voice independent Mid and Side filter/shaper history and reset it through the synth's existing voice lifecycle.
    - Preserve complete sound save/restore, Init, URL sharing, modulation, exact entry, and host state. Keep the continuous tracked offset modulatable, and prove polyphonic notes, glide, pitch bend, Global Tune, note stealing, mono, and legato all follow the existing voice rules.
    Done when:
    - The approved three-value Voice interface fits in the existing Filter footprint without shrinking or covering either stage, and tests prove the Enhancer processes each voice after its filter but before voices are combined, fixed linked Mid/Side uses one Amount with no hidden mode state, optional pitch tracking works independently across simultaneous notes, zero Amount is bit-identical, state round trips are complete, retained-voice CPU is acceptable, and the fixed T26/T28 Polish Enhancer is unchanged.

[x] T63 — Discuss and build whole-Effects-chain Mix and Bypass
    Active owner (2026-08-27):
    - Codex task `T63 Build Effects Lane Mix and Bypass`, thread `01a04330-913e-7482-a486-ce792f10ee69`, branch `codex/t63-effects-lane-mix-bypass`, worktree `/Users/winterfell/.codex/worktrees/c0e8/cosimo-synth`, model SOL, reasoning max.
    - This is a coordinator-managed implementation task. Final review, rebase, merge, push, and any deployment belong to coordinator thread `01a042bf-abd2-7820-9d47-b10405b67c5d`.
    Product decisions (2026-08-27):
    - Add one control that blends the dry signal around the complete editable Effects Lane and one control that temporarily bypasses that lane. The later fixed Polish section remains outside both controls and still processes the chosen result. Per-effect Mix controls and effect bypasses remain unchanged inside the lane.
    - Do not consume permanent vertical space above the graph. Overlay the Bypass button in the currently unused top-left corner of the FX graph, and place Mix as a horizontal slider along the inside bottom edge of the graph. These controls must remain reachable without obscuring effects, connectors, group controls, tail add buttons, or drag destinations at phone, desktop, and plugin sizes.
    - Mix defaults to 100%. Mix and Bypass are saved with the sound and included in URL sharing, but neither is a modulation destination or a host-automatable parameter.
    - Tail spill and CPU saving are not product requirements. Use the simplest behavior supported by the existing rack architecture. Bypass must transition to dry without terrible clicks and releasing it must return to the previously set Mix value. Do not add a separate tail engine, special tail clearing, CPU suspension, or another rack-state mode merely to manufacture a particular tail behavior.
    - Mix at 0% deliberately stores a continuous blend value. Bypass is a temporary override that leaves the stored Mix untouched, allowing one tap to return to the exact previous blend.
    Product decision (2026-08-28):
    - Ship the whole-lane Mix and Bypass against the real creative rack, whose legal effects add no processing latency under ADR-008. Do not redesign the rack around a hypothetical delayed/lookahead effect, and do not retain a helper, test fixture, or product claim suggesting that future latency-producing lane effects are already aligned. If such an effect is proposed later, its host-reported latency, serial accumulation, parallel/split alignment, outer dry delay, and topology-transition timing require a separate architecture decision before that effect can enter the rack.
    Required implementation:
    - Keep bypass transitions click-free with the smallest suitable smoothing ramp. Whether the lane continues processing internally while bypassed is an implementation choice; document the chosen behavior without expanding scope.
    Done when:
    - The real composed FX page proves the overlaid controls consume no additional graph height and do not cover or block any graph content or interaction at representative sizes.
    - Tests cover empty, serial, split, parallel, feedback/tail, bypass-transition, preset, Init, and URL-share cases with click-free output; inventory tests prove neither control is modulatable or host-automatable. Tests must not manufacture a latency-producing child that the production rack cannot represent or report.
    Completion evidence (2026-08-28):
    - The canonical lane document now stores strict top-level `output:{mix,bypassed}` state. Mix retains continuous 0-100% state, Bypass temporarily overrides it, and restore sends output state before topology/device replay. Neither control enters host or modulation inventories.
    - One retargetable 3 ms outer ramp blends the real current zero-latency rack with aligned dry input after every editable layout and before fixed Polish. The rack keeps advancing while bypassed. The synthetic future-latency helper/test/claim was removed; any future delayed/lookahead effect still requires the separate architecture decision recorded above.
    - Bypass occupies the unused graph top-left and Mix stays inside the graph's bottom scroller with a real >=44 px touch target and no added graph height. Empty, serial, Parallel, Frequency Split, tail/feedback, zero-Mix, bypass, preset, Init, URL, and inventory cases pass. Final composed qualification includes Cmajor rack 93/93, sound/preset/share 30/30 across non-browser and browser paths, and focused green reruns for every moving-master browser/orphan fixture found by the broad gates; the only unrepaired desktop diagnostic is the established unrelated React Grab `navigator.webdriver` assertion.
    - Fresh coordinator HMR launched the exact `74e4` standalone. The final self-contained compiled VST3 includes the pinned Cmajor runtime and is installed at `/Users/winterfell/Library/Audio/Plug-Ins/VST3/CosimoDesktopNative.vst3`; strict deep code-sign verification passes and its executable SHA-256 is `8a6754146b8c1a2b588798a2807d4c93a5f0edaf4996791783bd7cd2fdc198cd`. Ableton remained open and was not disturbed, so final host use, subjective click/listening acceptance, host round-trip inspection, and physical iPhone touch/audio acceptance remain unperformed.

[ ] T64 — Toggle an effect's bypass from its selected graph icon without stealing scroll gestures
    Tap behavior:
    - Keep the current first tap: tapping an effect node selects it and expands its editor.
    - When that same effect is already selected, tapping specifically its effect icon toggles that effect between active and bypassed while leaving it selected and its editor open. The first icon tap on an unselected node only selects it; it must not also bypass it.
    - A repeated tap on the node's label or surrounding pill does not toggle bypass. Preserve the existing long-press menu and its actions.
    Gesture precedence:
    - An ordinary finger movement that begins on an effect node belongs to vertical graph scrolling. Do not capture it or promote it into reordering merely because it crossed the current movement threshold.
    - Reordering requires a deliberate hold-and-drag: a short hold lifts the node, then movement reorders it. Continuing to hold without moving reaches the existing longer-press context menu. Tune the two timings and movement tolerances so these three intentions are distinct.
    - Give exactly one light haptic bump when the short hold actually lifts the node into reorder mode. Do not vibrate on pointer-down, ordinary scrolling, the stationary context-menu hold, or every subsequent reorder movement. Use Cosimo's existing native haptic bridge in the iPhone/AUv3 wrapper; in an ordinary browser, use `navigator.vibrate` only when it exists and succeeds, with a silent no-haptic fallback everywhere else.
    - Once scrolling has won a gesture, it stays scrolling or hands off to the containing scroll area at a boundary; it must never turn into a reorder later in the same gesture. Once a node has visibly lifted for reordering, reorder owns the gesture until drop or cancellation.
    Done when:
    - Browser tests prove first tap selects/opens, second selected-icon tap bypasses, the next selected-icon tap re-enables, and neither action closes or changes the selected editor.
    - Real touch tests starting on trunk and branch nodes prove immediate vertical drags scroll without selecting, bypassing, reordering, or requesting haptics; hold-and-drag still reorders and requests exactly one bump when it lifts; stationary long-press still opens the menu without the reorder bump; and releasing any cancelled scroll/reorder gesture produces no stray tap.

[x] T66 — Make one MSEG's mapping to another MSEG's Rate editable
    Active owner (2026-08-27):
    - Codex task `Fix T66 MSEG rate mapping controls`, thread `01a0434d-6730-7340-a091-ff0d448199ff`, branch `codex/t66-mseg-rate-mapping`, worktree `/Users/winterfell/.codex/worktrees/4533/cosimo-synth`, model SOL, reasoning max.
    - This is a coordinator-managed implementation task. Its clean final handoff goes to `Coordinate Cosimo task integrations`, thread `01a042bf-abd2-7820-9d47-b10405b67c5d`; it must not merge, push master, or deploy itself.
    Integrated (2026-08-27):
    - Coordinator-rebased implementation commits preserve ordinary source-tap behavior while separating the open MSEG target from a transient dragged source. Drawer/full-editor Rate and MAPPINGS controls now edit the canonical route amount without moving the live base; endpoint-backed MSEG, ENV, Amp Envelope, and oscillator Level targets resolve their real bases, while the controlled genuinely base-less seam retains an amount-only drag/exact-entry control with complete cancellation cleanup.
    - Focused rebased browser/resolver/provenance checks pass, including T38 Amp Envelope composition. The complete desktop UI gate is 263/264 with every T66 case green and only the established unrelated React Grab `navigator.webdriver` assertion red.
    - A fresh coordinator HMR session reported the correct repo root and `usesViteClient: true`, then was stopped. The final compiled-mode VST3 is installed and strict-code-sign valid at `/Users/winterfell/Library/Audio/Plug-Ins/VST3/CosimoDesktopNative.vst3` (executable SHA-256 `15af4c6bd3bfb576d71ad28f31a4c92860a5ee1acc7db00a051ca5e3488c269f`). Physical iPhone and Ableton gesture acceptance remain unperformed.
    Confirmed failure (2026-08-27):
    - In the real phone-width interface, an MSEG 1 -> MSEG 2 Rate route can be created, but a vertical drag on MSEG 2 Rate leaves the route amount at exactly zero.
    - The Rate control in the open MSEG drawer currently looks for a route whose source is the MSEG being edited. When MSEG 2 is open, it therefore looks for MSEG 2 -> MSEG 2 even though the actually selected and dragged source is MSEG 1. The real MSEG 1 -> MSEG 2 route is present but cannot be adjusted there.
    - The Mod page's MAPPINGS table also recognizes and lists an MSEG Rate route, but resolves no live base contract for that target. It falls back to a plain amount label at zero with no slider, drag behavior, or exact-value menu, so the route is visible but cannot be adjusted there either.
    Required result:
    - During a source-to-target drag, keep the MSEG being edited and the transient dragged modulation source as independent identities. An open MSEG 2 editor remains on MSEG 2 while MSEG 1 is dragged from the Mod bar and dropped on its Rate control.
    - Do not invent or change ordinary tap/selection behavior in this task. What tapping MSEG 1 should do while MSEG 2 is open remains a separate product decision; preserve the current non-drag selection behavior.
    - After MSEG 1 is dropped on MSEG 2 Rate, the MSEG 2 Rate control must display that mapping and edit its amount through the normal vertical drag and exact-value entry behavior. Editing the amount must not change MSEG 2's underlying Rate value.
    - Apply the same source-versus-target rule anywhere the shared MSEG Rate control appears; do not repair only one drawer while leaving another MSEG editor unable to address the selected route.
    - Every MAPPINGS-table route must expose an editable amount control. MSEG Rate routes must bind their real Rate endpoint so the normal row can show the live base Rate separately from the route amount. Targets that genuinely have no editable base must still receive an amount-only slider/drag and exact-value control instead of dead text.
    Done when:
    - A real browser regression creates MSEG 1 -> MSEG 2 Rate through the interface, changes its amount away from zero, proves the stored route and running modulation program receive the new amount, and proves the base MSEG 2 Rate did not move.
    - A real MAPPINGS-table regression proves the same MSEG Rate route has a working amount control at zero, can be changed by drag/slider and exact entry, and never conflates the live base Rate with route depth. An amount-only target also remains editable.
    - The route survives save/reload and remains editable, self-mapping and another cross-MSEG pairing still work, cancelled gestures close cleanly, ordinary base-Rate editing remains unchanged when no mapped source is selected, and ordinary source taps retain their pre-task behavior.

[ ] T65 — Make every Cosimo haptic cross-platform and audit the complete interaction set
    One shared capability:
    - Replace scattered direct calls to `cmaj_triggerHaptic` and surface-specific callback plumbing with one shared haptic adapter used by the browser, desktop standalone/plugin, iPhone standalone, and AUv3 interfaces.
    - Prefer the native host bridge when it is present. Otherwise request the closest short `navigator.vibrate` pattern when the browser implements it and permits it. Never fire both for one event. Missing hardware, a rejected/false browser request, a hidden page, or an unsupported browser is a silent no-op and must never change the interaction result.
    - Give call sites semantic events such as detent, selection, lift, success, warning, and failure; translate those centrally to native styles and restrained browser vibration patterns instead of scattering platform-specific durations through product code.
    Audit and complete the product behavior:
    - Inventory every existing haptic request in `TODOS.txt`, every direct bridge call, and every callback-based haptic path. Preserve the already requested behavior for discrete oscillator detents, MSEG hold/lift editing, rack-parameter long press, Mod-source acquisition, mapping confirmation, duplicate/failure warnings, and T64's reorder lift.
    - Review the remaining touch interactions and add a bump wherever one clearly confirms a discrete physical transition that the person caused: acquiring or releasing a drag target, crossing a detent or snap point, lifting or dropping a movable object, completing an insertion or mapping, changing a discrete mode, or confirming a warning/error boundary.
    - Do not add vibration to continuous knob travel, ordinary scrolling, pointer-down before an action is recognized, every animation/reorder frame, automatic state restoration, telemetry, or background changes. One meaningful transition produces at most one bump unless a deliberately documented warning pattern requires otherwise.
    - Record the completed audit as an event-to-style table so future controls use the same vocabulary and do not reintroduce direct platform calls or duplicate bumps.
    Done when:
    - Tests prove native-bridge preference, browser fallback, unsupported/failed no-op behavior, semantic-style translation, and exactly-once delivery without duplicate native-plus-browser feedback.
    - Interaction tests cover every retained and newly added event, including negative assertions for scroll and continuous dragging. Real-device checks confirm native haptics in the iPhone standalone/AUv3 and best-effort vibration in a supporting browser; Safari and other unsupported browsers retain the complete interaction without tactile feedback.
    - Product UI code contains no direct native or browser haptic calls outside the shared adapter, and the audit table accounts for every haptic call site.

[ ] T67 — Coordinate review and integration of all delegated Codex tasks
    Active coordinator (2026-08-27):
    - Codex task `Coordinate Cosimo task integrations`, thread `01a042bf-abd2-7820-9d47-b10405b67c5d`, branch `codex/task-integration-coordinator`, worktree `/Users/winterfell/.codex/worktrees/74e4/cosimo-synth`, model SOL, reasoning max.
    Standing responsibility:
    - Receive clean committed handoffs from every implementation task; maintain the one authoritative queue of task, thread, branch, worktree, commit, scope, dependencies, review state, and remaining merge/push/deploy work.
    - Every delegated thread must be classified explicitly as either an Andrew-owned product/design conversation or a coordinator-managed implementation task. Review source and scoped diffs before broad suites or complete rebuilds. Return defects only to a thread explicitly classified as a coordinator-managed implementation task; use focused tests during repair loops, and run the complete relevant suite plus expensive build/install gates once only after rebased source is review-clean.
    - Rebase, review, and merge accepted branches one at a time against the moving latest master; push master and deploy affected Sites work. Preserve dirty worktrees and unrelated `TODOS.txt`/`PROGRESS.txt` history, and never move uncommitted files, generated output, or installed artifacts between tasks.
    - Parallelize independent reviews and focused checks while serializing master changes, fixed ports, shared generated output, installed plugins, and native builds. Keep automated qualification separate from Andrew's physical-device, Ableton, listening, and visual acceptance.
    Protected product discussion:
    - `T26 Build Fixed Two-Band Enhancer`, thread `01a03b9a-b1c0-7431-bc23-3e85da52c3d0`, is Andrew's ongoing product-development room. The coordinator may read Andrew-approved decisions after they are recorded on master and may later review commits originating there, but it must never message, interrupt, assign work or repairs to, wait on, or otherwise direct that thread.
    - If review finds a defect in code originating from that protected conversation, report the defect to Andrew. After Andrew approves repair work, create a separate implementation task and isolated worktree; do not turn the product conversation into the repair worker.
    Enhancer integration boundary:
    - Keep Cosimo Enhancer, Cosimo Enhancer Lite, and the per-voice Enhancer separate exactly as specified by T26, T61, T62, and `ENHANCER_DESIGN.md`. Per-voice simplifications never authorize deleting approved behavior from either standalone processor.
    - `codex/t26-spectre-wrapper-prototype` is the shared historical base. The readout and shelf branches are independent Lite descendants and must both survive composition. T61 packaging and MIDI tracking remain open after that composition; T62 remains separate and depends on T50.
    Initial handoff sources to audit:
    - T57/T58 effect-editor cleanup: thread `01a0426b-19ee-7db0-ba88-e21192770e6a`.
    - T27 Sausage Fattener lab: thread `01a03b9b-5ad4-7f11-aafb-53bc9db9ca6f`.
    - T26 fixed Enhancer source and initial Lite source: protected product thread `01a03b9a-b1c0-7431-bc23-3e85da52c3d0`; review its commits without directing the thread.
    - Enhancer Lite readouts: coordinator-managed implementation thread `01a0427c-0976-7561-949f-738fc8f47bca`.
    - Enhancer Lite shelves: coordinator-managed implementation thread `01a0426c-31f8-76a2-8b81-5e31af0d6c28`.
    Done when:
    - This is an ongoing coordination role. It remains open while delegated work exists; an empty queue is an idle state, not task completion.

INFRASTRUCTURE AND TOOLING

[x] T68 — Keep one live, Git-tracked task checklist across Codex worktrees
    Decision and implementation (2026-08-27):
    - `TODOS.txt` remains a normal tracked file in the primary checkout on master, so every change retains its ordinary repository history.
    - `.codex/environments/environment.toml` runs `scripts/link_master_todos.sh` whenever Codex creates a task worktree. The script replaces that worktree's clean checkout copy with a link to master's live file and hides only that expected local type change from Git.
    - The setup refuses to replace a checklist with staged changes, uncommitted changes, or a merge conflict. It also refuses a source checkout that is not this repository's primary master checkout, preventing an old branch from becoming the authority by accident.
    - `PROGRESS.txt` deliberately remains local to each branch because it records that task's implementation handoff rather than shared product decisions.
    - Existing active/recent task worktrees were reconciled by keeping the current master checklist and linking clean stale copies to it. Their former checklist snapshots remain in Git history; no rejected or superseded branch text was copied back onto master.
    Done when:
    - A newly created Codex worktree immediately reads the same `TODOS.txt` as master, edits made through either path are visible through the other, the task worktree stays clean, and unsafe replacement cases fail without losing the local file.

[x] T17 — Cut the desktop browser suite's wall clock without losing coverage or stringency
    Implementation evidence (2026-08-19, codex-implemented, independently verified):
    - Four balanced shard files (main 51 / articulation+mseg 39 / fx+modulation 40 / rail 39) sharing tests/helpers/desktop_patch_view_browser_suite.mjs, node --test concurrency 4, wired as the single test:desktop:ui command (TAP mirrored to build/t17-latest-sharded-suite.log).
    - Independently verified: fresh run 169/169 in 7m29s; the ok-name diff against the pre-shard baseline is EMPTY (169 = 169); assertion count 1258 -> 1259 (the +1 is one strengthened load-race wait); tsc clean. Honest speed claim: the true solo baseline was ~13.5 min, so the real win is ~1.8x (codex's reported 3.82x measured its baseline under its own concurrent load — corrected here). Codex's three consecutive sharded runs were green with zero boot flakes.
    - The unsound async waitForFunction patterns in tests/test_web_poc_browser.mjs were converted to sound polling with unchanged conditions.
    - Leftover for the user: codex left git stash@{0} ("pre-codex-master-merge-local-tracked-edits") holding OLD uncommitted transient/hardware-lit-moodboard edits it set aside; deliberately not dropped or popped — decide its fate when convenient.
    Delegation:
    - Implementation may be handed to codex at max reasoning effort. Fable writes the shard plan review and verifies the gates; codex must not modify what any test ASSERTS — only structure, waits, and orchestration.
    Measured baseline (2026-08-19, from a full 163-case run log):
    - ~13.3 minutes total. Per-test cost is dominated by app boot: every test calls openHarnessPage() (fresh isolated context, full bundle eval, waitForHarnessReady, first render) at ~3.5-4.5 s; median test 4.8 s, p90 5.6 s, assertions nearly free.
    - The whole suite lives in one file (tests/test_desktop_patch_view_browser.mjs), and node --test parallelizes across FILES, so 168 Chromium boots run strictly serially on a 16-core machine.
    - 66 static waitForTimeout sleeps total only ~13 s nominal — real but minor.
    - A legitimate ~2-minute tail (~15 tests, 6-18.5 s) exercises deceleration physics, momentum decay, animation midpoints, and nothing-happens-for-N-ms negative windows, where elapsed time IS the assertion.
    Build, in order of leverage:
    - Shard the one file into 4-6 domain files (rail/audition, articulations, MSEG, voice editor, FX rack, misc) that import the shared helpers from a new tests/helpers module instead of duplicating them. Every shard keeps its own before/after starting its own ephemeral-port servers (startStaticRepoServer/startDesktopHarnessServer already support this). Expected ~3-5x.
    - Cap file concurrency at ~4 (--test-concurrency) and raise the harness readiness timeout under concurrency: the observed "Desktop harness did not render. State: null" flake is a boot starved by concurrent machine load, and 16-wide concurrency would trade minutes for flakes.
    - Profile waitForHarnessReady (tests/helpers/desktop_harness_browser.mjs) and shrink the boot floor itself — what part is bundle eval vs catalog/audio-mock init vs first render; trim await chains. Even 4.5 s → 2 s saves ~7 minutes before sharding.
    - Replace fixed sleeps with condition waits that are strictly STRONGER than the sleep they replace (data-decelerating="false", data-settling-x="false", scrollTop stable across frames, midiInputEvents settled). Fold the ubiquitous post-boot 240 ms settle into the readiness condition itself.
    - Boot amortization (several sequential read-only cases sharing one page) is allowed only for cases that provably do not mutate stored state, localStorage, or runtime state consumed by a later case; never merge mutating cases.
    Do not:
    - Delete, skip, or weaken any test or assertion; do not shorten the time-semantics tail (deceleration/momentum/negative-window tests keep their real clocks); do not raise concurrency past the flake-free point just for speed.
    Done when:
    - Before/after runs on the same machine record the wall-clock win, and the after-run passes with EXACTLY the same set of test names as the before-run (diff the ok lines; count may only grow).
    - Ten consecutive full runs (or three on a loaded machine) show zero harness-boot flakes at the chosen concurrency.
    - tsc and the untouched suites (iPhone shell, web bundle, shared hooks) stay green; package.json test:desktop:ui runs the sharded set as one command.

CROSS-CUTTING EDITING FEATURES — REQUIRE PRODUCT AND ARCHITECTURE REVIEW

[-] T18 — Add synth-wide Undo history
    Current status (2026-08-26):
    - Deliberately deferred. Do not begin the remaining feasibility proof or any public implementation until Andrew explicitly reopens this work.
    Desired behavior:
    - Put Undo in the top-right Settings menu opened by the preset bar's three-dot button. Name the available action specifically, such as `Undo Delete Mapping`, rather than showing an unexplained generic command; show it disabled when no action is available.
    - Put Redo beside Undo in the same Settings menu. The architecture investigation found that it uses the same action records and atomic replay path, so it is part of the first public release rather than a separate engine feature.
    - Undo must recover destructive edits such as immediate mapping deletion; do not add confirmation dialogs or deletion toasts as substitutes.
    - Ideally, loading a preset is one undoable action that restores the entire sound that existed immediately before the load.
    - Cover every direct user action that changes the sound, including parameter adjustments, sound-changing switches, mapping edits, modulation-shape edits, source deletion, rack changes, and articulation edits.
    - Do not put played notes, page/tab navigation, manual auditioning, Auto-preview activity, DAW automation, or other incoming host changes into the synth's Undo history.
    - Keep Undo history for the lifetime of the running synth/plugin instance, including when its editor window is closed and reopened. Clear it when that instance is destroyed or the standalone app relaunches; never save Undo history inside a preset or project.
    - Treat one complete user action as one Undo step: an entire continuous gesture from touch-down to release, one committed exact-value entry, one button action, or one compound command such as deleting a source together with its mappings.
    - Keep the existing MSEG-editor Undo button and the temporary Articulation recovery button as contextual shortcuts to this same global history. They may appear or enable only when the newest reversible action is the relevant MSEG/Articulation action. They must never retain, expose, or apply a separate local history.
    - On desktop, while Cosimo's UI has keyboard focus, Command-Z belongs to Cosimo's synth-wide Undo even when Cosimo is running inside a DAW; it must not undo a host action. Redo uses the standard Shift-Command-Z shortcut under the same focus rule. Record the Windows/Linux equivalents during implementation rather than making the macOS wording the whole cross-platform contract.
    - While the user is actively typing in a number, name, or other text field, Command-Z first uses that field's normal text-editing Undo. Everywhere else in the focused Cosimo UI, Command-Z invokes synth-wide Undo. Neither case may leak the shortcut to the DAW.
    - Undo restores the affected sound state and visible controls without changing the user's current page, tab, open editor, or scroll position. For example, undoing a deleted mapping while viewing Voice restores that mapping in the background; it does not navigate to Mappings merely to show the restored row.
    - After a successful Undo or Redo, show one brief top-of-screen toast naming the completed action, such as `Undid Delete Mapping`. Repeated shortcuts replace the current Undo/Redo toast instead of stacking a queue. This confirms recovery only; the original immediate Delete still has no confirmation or deletion toast.
    - Use conventional Undo/Redo behavior for routine edge cases without returning them for product review: a new sound edit after Undo clears the Redo branch; cancelled, failed, and no-op edits add no history entry; Undo/Redo never records itself; unavailable commands are disabled; and shortcut key repeat may walk backward or forward through successive complete actions.
    - Applying an Undo or Redo action is atomic. If the complete prior/next state cannot be restored, apply none of it, leave the history position unchanged so the command can be retried, and replace the current status toast with a concise failure such as `Couldn't undo Delete Mapping`. Never expose a half-restored sound or controls.
    - Do not invent a fixed history length in the product ticket. Choose a safe capacity during the architecture investigation using measured worst-case memory cost; normal editing must not exhaust the available Undo history noticeably.
    Product-discussion status (2026-08-26): the user-visible Undo behavior is locked and the architecture investigation is complete, but the entire task is parked. Do not continue grilling the user about conventional Undo mechanics; use normal platform behavior only after implementation is separately approved.
    Architecture gate:
    - Satisfied by `docs/ADR-027-native-action-history-and-atomic-sound-replay.md`.
    - The accepted architecture still has a hard feasibility gate: a hidden production-equivalent spike must prove bounded all-or-none replay across the existing state owners on desktop and iOS before public implementation proceeds.
    - Preset/Init/effect-preset replacement is an explicit nonundoable history barrier in the first release. Undoable preset replacement is deferred until it can reuse the same atomic replay path.
    Current architecture findings (2026-08-19, read-only audit):
    - Ordinary knobs/readouts write scalar engine parameters through `usePatchParameter`. The existing user-edit stream reports which parameter changed and whether it changed, plus broad gesture start/end signals, but it does not retain the previous value, the final value, or a dependable identity for grouping every multi-control gesture into one reversible action.
    - The rest of the sound is split across independently persisted documents: `modulation.v6` owns routes and MSEG/source structure, `articulations.v4` owns the articulation bank, and `rack.v1` owns effect order and enabled state. Their mutation entry points are not currently one shared command path.
    - The preset system already observes the complete scalar parameter contract and can capture/apply modulation and articulation documents. That is useful prior art, not a ready Undo engine: rack state is not in the synth preset adapter list, preset application is a full load rather than an edit transaction, and complete rollback across a failed multi-domain apply is not currently guaranteed.
    - Two isolated recovery mechanisms exist and must not become competing histories: the MSEG editor holds one previous shape, while Articulations temporarily remembers one discarded edit. No global stack, Redo stack, or cross-domain compound action exists.
    - Desktop host-keyboard routing already provides the required ownership seam: the native CHOC layer temporarily retains the original key event, ordinary UI code handles a normal browser event, and `preventDefault()` tells the bridge that Cosimo claimed it; unclaimed musical-typing/transport keys can still be returned to the host. Implement Undo through that one existing route. Do not add a second native interception path, call host-forwarding APIs from product components, use `[NSApp currentEvent]`, or resign the WebView as first responder.
    - The investigation compared per-action before/after records, complete sound snapshots, and a hybrid approach. It rejected repeated whole-sound/document snapshots and selected owner-encoded logical fragments because they preserve the existing state authorities, avoid presentation-state capture, and give large structured edits a bounded representation.
    Chosen architecture (2026-08-20):
    - One transient native history belongs to the running synth/plugin instance, survives editor closure, and is never serialized. The existing parameter, table, rack, modulation, and articulation owners remain authoritative.
    - Each complete user action records only the accepted before/after fragments that changed. Explicit action tokens replace the current anonymous gesture grouping, so a rolling-axis drag remains one Undo step.
    - Undo and Redo stage every affected owner's candidate and publish the complete result together at one audio-block boundary or publish none. Routine Undo never invokes the voice/effect-resetting cold-restore path.
    - Host changes use owner generations to win or invalidate conflicting history predictably. Successful full-sound replacement starts a new epoch and clears both stacks; failed replacement preserves them.
    - History capacity is chosen from measured bytes after imposing a finite accepted MSEG payload bound. Capacity pressure may reject a new action before it starts, but an admitted valid edit may not become an untracked partial action.
    - Public Undo/Redo remains hidden until every enabled user sound-write path is correlated, source deletion is canonicalized or disabled, private MSEG/Articulation recovery ownership is removed, and the full acceptance suite passes.
    Done when:
    - The user-visible scope, grouping, and lifetime rules are explicit.
    - Undo restores both the sound and every affected visible control to the prior user-visible state.
    - Desktop integration tests prove that focused Cosimo handles its Undo shortcut exactly once and does not leak it to the DAW, text fields retain normal typing Undo, and unclaimed musical-typing and transport keys still reach the host with matching key-down/key-up behavior.

[x] T19 — Make exact-value entry understand parameter units consistently
    Implementation evidence (2026-08-20, architecture in docs/ADR-028-unit-aware-exact-value-entry.md from a full cited inventory; codex-implemented, Fable-reviewed):
    - ui/shared/parameter-value-entry.ts is the one parse/format authority: anchored trailing-unit matching (khz before k), explicit human-readable rejections (incompatible units are refused, never ignored), clamp-with-echo, log-percent Cutoff base (50% ≈ 632 Hz), octave-default Cutoff amounts with signed-Hz movement resolved against the live base into a preserved interval, per-target amount units with depth-percent (+100% = +6 oct), the displayed-default-unit rule for bare numbers, and one-commit Free/Sync division entry matched against the descriptor's own tables (division publishes before Sync so no stale flash). Specs derive from the existing descriptors/limit tables; the displayed unit freezes for the duration of an edit.
    - All seven surfaces migrated; the eleven duplicated parsers and the duplicate/dead formatters are DELETED (iOS's private formatters replaced by the shared ones; dead formatTargetValue removed). Every editing field now shows its default unit as a persistent token while typing.
    - The three live inventory bugs are gone by construction and regression-pinned: garbage into Unison Voices no longer commits NaN; `12khz` means 12 kHz on every surface; envelope bare numbers follow the VISIBLE unit instead of a hidden magnitude threshold.
    - Verified: pure suite 17/17 (khz sever kills 2 — discriminating), tsc clean, codex's three browser shards 154/154 in its run with failing-first evidence per test file; five test migrations preserve canonical-value assertions (displayed-unit drafts changed, stored values identical). Bonus retirement: tests/test_modulation_bridge.mjs's two PRE-EXISTING failures (stale cell literals from the filterMix domain append, verified failing at pristine HEAD in a worktree) now derive expectations from getModulationRuntimeCell.
    Product direction already stated:
    - Infer the expected unit when a parameter has one obvious meaning, while accepting an explicitly typed compatible unit where useful.
    - Define intentional behavior for ambiguous modulation amounts instead of treating every number as an unexplained raw scalar.
    - Every exact-value field visibly states its default unit. A bare number uses that displayed default; a compatible unit typed with the number may override it. For example, a Cutoff field whose default is Hz treats `2000` and `2 kHz` as the same value.
    - For the base filter Cutoff, accept Hz, kHz, and percentage. Percentage means the position through the existing logarithmic 20 Hz–20 kHz Cutoff range: 0% = 20 Hz, 50% is approximately 632 Hz, and 100% = 20 kHz; it does not mean a linear percentage of 20 kHz.
    - For filter Cutoff modulation amount, accept a signed Hz/kHz movement relative to the current base Cutoff. Convert the resulting full-modulation endpoint to the equivalent octave interval; if the base Cutoff later changes, preserve that interval rather than preserving the originally typed fixed-Hz difference.
    - For filter Cutoff modulation amount, use octaves as the displayed default unit. A typed percentage means percentage of the allowed signed modulation depth: +100% = +6 octaves and -50% = -3 octaves; it is not a percentage of the current Hz value.
    - For a parameter that has explicit Free and tempo-synced modes, exact entry may select the mode as part of the same commit. Typing an ordinary time/rate value such as `250 ms` for Delay or `2 Hz` for Phaser selects Free; typing a supported musical division such as `1/8`, `1/4T`, or a dotted value selects Sync and applies that division. The field must show the resulting mode and canonical value immediately.
    - Invalid text or an incompatible unit must not change the parameter. Keep the editor available and show a short explanation beside that field rather than a global toast.
    - A valid value outside the parameter's legal range clamps to the nearest legal value, and the field immediately shows the value that was actually applied.
    Current technical reality:
    - Unit-aware parsing already exists in separate places: Cutoff accepts Hz/kHz, envelope time accepts milliseconds/seconds, and rack controls accept their displayed units. The rules are duplicated and incomplete.
    - A route does not store one universal percentage. Its numeric `amount` is interpreted by its target: normalized targets use a -1..+1 offset displayed as percentage, pitch uses semitones, level uses dB, envelope/MSEG timing uses seconds, and logarithmic targets such as Cutoff, Delay Time, and Phaser Rate use octaves. A UI percentage such as "50% depth" is derived from that target's allowed amount; it is not the persisted route value.
    - Exact entry must translate compatible typed units into that existing target-owned amount. It must not replace the canonical route amount with a second percentage representation. For example, entering a Delay endpoint change from 250 ms to 500 ms resolves to the existing +1-octave amount and therefore remains a 2x interval if the base time later changes.
    Architecture gate:
    - This ticket specifies input behavior, not the parser/storage design. Before implementation, inventory the existing per-surface rules, discuss the appropriate shared architecture, and record that design separately.
    - As part of that inventory, assign each parameter family its obvious displayed default and common compatible typed units. Use ordinary domain meaning without requiring product review for every conversion; return only genuinely ambiguous cases for a product decision.
    Product-discussion status (2026-08-19): locked and queued. Do not reopen the existing target-specific route-amount contract or ask the user to adjudicate routine compatible-unit conversions. Begin with the architecture inventory above; return only if the repository exposes a genuinely ambiguous musical meaning that cannot be resolved by the displayed unit and existing target contract. No implementation was authorized during this clarification pass.
    Done when:
    - The same text means the same value everywhere that parameter can be edited, explicit units round-trip correctly, and no exact-value field silently guesses between genuinely different meanings.

FUTURE DSP EXPLORATIONS — VALIDATE BEFORE PRODUCT INTEGRATION

[ ] T23 — Explore a detail-preserving clipper as a Cosimo effect
    Origin and scope (Andrew, 2026-08-22):
    - Reference reel: https://www.instagram.com/reel/DZEVKuITTaJ/?igsi=YXE4OHZ2dzJpbHpw
    - Determine whether the demonstrated detail-preserving clipping idea can add useful peak control and aggressive loudness/character without conventional clipping masking the patch's upper-frequency detail and transient edge.
    - This ticket authorizes research and a throwaway DSP comparison only. It does not authorize a production effect, UI, preset contract, Effects Lane integration, or replacement of the current Distortion effect.
    Evidence status and first gate:
    - The supplied reel has not yet been captured in a browser session. Watch it first and record the author, exact signal graph, controls, claims, before/after material, and any caveats; do not infer those details from the phrase "detail-preserving clipper."
    - Related public prior art is not evidence that the reel uses the same design. Au5 describes a high-passed clipping-delta/foldback path; Newfangled Saturate describes frequency-separated spectral treatment. Preserve them as different hypotheses, not interchangeable implementations:
      - https://www.patreon.com/Au5Music/posts/detail-clipper-156412018
      - https://www.newfangledaudio.com/saturate
    Product hypothesis (provisional):
    - For a sound designer driving dense or transient-rich Cosimo patches into clipping, a detail-preserving mode/effect may retain more audible snap, brightness, and separation at the same controlled peak level than the existing conventional distortion/clip baseline.
    - Unknown until audition: whether this is broadly useful enough to be a lane device, only an extreme sound-design mode, or redundant with the current 4x-oversampled DistortionBus and its band-shaped wet path.
    Investigation order:
    1. Capture the reel and translate only its demonstrated behavior into an annotated signal-flow reference.
    2. Freeze a small audio corpus that can expose both the proposed benefit and its failure modes: low-heavy material with quiet high-frequency detail, sharp transients, harmonically dense synth patches, and deliberately adversarial near-Nyquist content.
    3. Freeze the comparison protocol before judging sound: identical source and drive condition, matched output ceiling/peak reduction, loudness-matched audition, and retained originals/renders/measurements.
    4. Build an isolated Cmajor lab, leaving production DistortionBus untouched, with the current clip/distortion behavior as baseline and at least two genuine design forks:
       - a high-passed clipping-delta/foldback detail path derived independently from the public Au5 explanation;
       - an independently designed frequency-separated/detail-path alternative that does not copy Newfangled code, coefficients, UI, or proprietary implementation.
    5. Sweep detail amount/frequency, clipping drive/shape, oversampling or other anti-alias treatment, and output trim. Check stereo coherence, DC, NaN/Inf containment, output ceiling, intersample overs, foldback harshness, and bypass/zero-detail identity.
    6. Measure retained upper-band/transient behavior, alias products, peak reduction, latency, and CPU on the product-relevant backends. Keep sound quality and current-effect parity explicit; do not substitute a generic efficiency objective.
    Decision gate — freeze numeric thresholds after the reference capture and baseline measurement, before tuning either fork:
    - Advance only if one fork wins a level-matched user audition on more than one corpus item, the corresponding frozen measurements distinguish it from the conventional baseline, safety invariants pass, and its measured cost fits an explicitly named product placement.
    - If no fork produces a repeatable advantage, close the ticket as a useful negative result. If the advantage exists only for extreme coloration, recommend that narrower scope rather than marketing it as transparent peak control.
    - Any production proposal must separately decide lane device vs Distortion mode vs output-stage tool, define the minimal controls, and review naming/licensing/provenance. This exploration must not silently make that product decision.
    Done when:
    - The reel evidence, signal-flow hypotheses, frozen corpus/protocol, renders, metrics, and source commit are retained together.
    - The result ends with one explicit recommendation: build a narrowly named product feature, run one identified follow-up experiment that would choose between real alternatives, defer, or do not build.

DESIGN-BACKED DISTORTION AND OUTPUT POLISH

Source note (2026-08-25):
- The five documents for T25–T29 were imported at their original paths from branch `claude/cosimo-distortion-quality-x0rx9c` at commit `63dd431b15f7ebfd27e9f54406892391a42fd6d7`.
- The checked-in root documents are the implementation authority on master. The branch and pinned commit remain their provenance; no unrelated branch code was imported.

[x] T25 — Rebuild the rack Distortion as one selectable, level-matched nonlinear algorithm
    Design authority: `DISTORTION_QUALITY_DESIGN.md`.
    Superseding product decisions (Andrew, 2026-08-25 and 2026-08-26):
    - The design authority was replaced after direct product discussion. Its new single-algorithm/type-selector design supersedes the imported moving-bias, pre/de-emphasis, post-filter, Drive-macro, cascaded-shaper, no-new-controls, and listening-approval instructions below their original provenance.
    User-visible bug to close:
    - The dry signal and driven wet signal reaching the current Classic Mix control are not level matched. Moving Mix therefore causes a large, erratic loudness change instead of revealing a controlled blend between the two sounds.
    Locked work:
    - Keep all six existing parameter identities/ranges and add one saved, discrete, non-modulatable `distortionType`: Symmetric, fixed-static-bias Asymmetric, or Wavefold. T41 owns the new/reset Distortion Wet default of 50%.
    - Run exactly one selected nonlinear algorithm. Do not cascade clippers or add pre/post-fold saturation. Knee stays softer-to-sharper curvature inside the selected algorithm; in Wavefold it controls reflection strength across the folded portion.
    - Keep both wet filters before Drive/distortion. Keep the existing DC removal. Do not add dynamic bias, pre/de-emphasis, post-shaper filtering, or a hidden voiced-Drive macro.
    - Andrew's physical-phone low-level test supersedes the fixed-table instruction: at -36 dBFS the old table made 50% Mix 12.47 dB louder than 1%. A matched-level filtered saw and pink noise required correction within 0.85 dB of each other, proving input level—not fixture choice—was the dominant missing variable.
    - Match the actual completed wet signal to dry with one bounded, stereo-linked running dry/wet energy ratio. Apply correction only to wet; never raise dry to hide the mismatch. Use the same running cross-energy for intermediate Classic Mix normalization, with exact 0% and 100% endpoints.
    - Give the oversampled core a unity output and subtract shaped-round-trip minus unity-round-trip so Harmonics residue is aligned by construction. Apply makeup after subtraction, never to only one subtraction operand.
    - Clear filter, DC, interpolation, and oversampling state on pool reset. Preview output must reflect the final compensated Classic wet.
    - Keep 4x oversampling, zero declared latency, allocation-free/pool-safe processing, and no ADAA, feedback, physical circuit, hysteresis, or neural detour in T25.
    Preserve:
    - Wet at zero remains the existing dry Classic path exactly. Existing Mode/Drive/Knee/Wet/HP/LP IDs, ranges, and continuous modulation targets remain intact; only Type is appended.
    Minimal execution fence:
    - Use one existing production test seam plus at most one narrow calibration entry point. Do not build a generic audio-analysis framework, add dependencies, broadly refactor the rack, or create another plan.
    Done when:
    - The retained regression fails against the old path, then proves default-filter pink noise and Andrew's 110 Hz saw through a 350 Hz 12 dB/octave low-pass remain within 1 dB from -36 through -18 dBFS at 100% Wet and across the Mix sweep.
    - The automated sweep remains within 1 dB on fixed drums, bass, and bright-poly holdouts and across the tested Type x Drive x Knee grid. Adversarial near-Nyquist material remains finite/bounded; Symmetric, Asymmetric, and Wavefold have the specified distinct harmonic/transfer behavior.
    - Harmonics residue is effectively silent at zero Drive; Classic Wet zero is bit-identical dry for every Type; latency stays zero; Type state/UI, safety, smoothing, and pool reset pass.
    - The current desktop UI is built, a fresh HMR standalone is launched, and the compiled VST3 is installed for Ableton. Andrew is not a manual gain-calibration or completion gate.
    Prior completion claim superseded 2026-08-26:
    - The -18 dBFS static table passed its narrow test but failed Andrew's quiet physical-phone signal by 12.47 dB. T25 is reopened until the live level matcher, reviewer repairs, rebuilt phone surface, standalone, and VST3 are all requalified.
    Completion evidence (2026-08-26; graph claim corrected 2026-08-26):
    - Replaced the invalid static compensation table with the bounded stereo-linked live dry/wet energy and correlation matcher. Andrew's filtered-saw fixture now measures Mix 50%-vs-1% at -0.121..-0.093 dB from -36 through -18 dBFS; representative grid worst is 0.324 dB and musical-holdout worst is 0.873 dB.
    - Independently reproduced and repaired the reviewers' audio/runtime defects: Drive-zero discontinuity, reset burst, off-grid/sample-rate drift, endpoint lag, missing Harmonics/DC/reset/Type-transition proof, and strict current-contract fixtures. The visualizer repair claim is withdrawn: T25 suppressed the full transfer curve whenever telemetry existed and substituted low-level post-match samples as the only active geometry, collapsing the real graph into a thin line. T25A owns the restoration below. Intentional ADR-021 old-state rejection and the lack of an alias-energy claim are documented rather than misrepresented as fixes.
    - Distortion 6/6 and full rack 79/79 Cmajor tests, 723/723 shared/unit tests, focused parameter/state/browser gates, Chromium/WebKit offline rendering, and the five-case current-contract Speedrun pipeline pass. Those gates support the accepted sound and state work; they did not establish a correct or useful graph. Final web/iPhone/desktop bundles were rebuilt; this worktree's HMR standalone is running on 127.0.0.1:5176; the compiled VST3 is installed at `~/Library/Audio/Plug-Ins/VST3/CosimoDesktopNative.vst3` and passes strict codesign verification.

[x] T25A — Restore the pre-T25 Drive graphic with current-algorithm transfer curves
    Active owner (2026-08-26):
    - Completed by Codex task `Fix T25 Drive graph regression`, thread `01a03b99-e818-7ec2-8d0d-1ab5ba6b7d0f`, branch `codex/t25-drive-graph-regression`, worktree `/Users/winterfell/.codex/worktrees/6195ed10-94d0-4866-a3af-aec686aff452/cosimo-synth`.
    Locked visual behavior:
    - Restore the exact pre-T25 renderer, scale, plot geometry, layering, and presentation. Do not replace it with a new visualization or adaptive scale.
    - Keep the full transfer function visible during live telemetry. Its curve must truthfully represent the selected current Symmetric, Asymmetric, or Wavefold algorithm and respond to the controls that define that algorithm.
    - Draw the live signal overlay in the same input/output coordinate system as the transfer function so users see the exercised waveform meet and clip where the curve flattens or folds.
    - Preserve real telemetry as the activity source. Render clipped and unclipped portions in clearly different, high-contrast colors; do not fabricate movement.
    - Do not change the accepted distortion DSP, gain matching, Mix, filters, oversampling, or sound.
    Done when:
    - A red-capable real composed-editor regression fails when telemetry hides the transfer curve, when the live overlay diverges from its curve, or when clipped/unclipped regions are not materially distinguishable.
    - Current-algorithm curve samples are checked against the production DSP equations for all three Types, and the real editor is visually compared with the pre-T25 reference at phone, desktop, and plugin sizes.
    - The required fresh desktop HMR app and installed VST3 are delivered.
    Completion evidence (2026-08-26):
    - Restored the fixed pre-T25 compact canvas, plot geometry, layering, and always-visible curve. Symmetric, Asymmetric, and Wavefold now use the current production-core equations, including Drive's 0–6 dB engagement ramp.
    - The live overlay remains driven by real input telemetry, but its geometry is projected through that same core transfer instead of using the post-level-match preview output. The background waveform and curve therefore share one amplitude scale and clipping/folding boundary without fabricated activity. Unclipped silver and clipped coral regions now have materially stronger color and opacity contrast.
    - The composed-editor regression covers quiet and normal signals for all three Types, exact restored compact geometry, curve/history boundary alignment, and contrast. Shared/UI/browser suites pass 124/124; deterministic before/after captures at 393×852 phone, 1440×900 desktop, and 1120×680 plugin dimensions retain identical Type/Drive/Knee/Mix/filter settings and editor geometry.
    - No distortion DSP or audio behavior changed. A fresh HMR standalone is running from this worktree at `127.0.0.1:5178`; the compiled VST3 is installed at `~/Library/Audio/Plug-Ins/VST3/CosimoDesktopNative.vst3` and passes strict codesign verification.

[x] T26 — Finish the high-quality two-band Enhancer for the Polish section
    Protected product discussion and source (2026-08-27):
    - Codex task `T26 Build Fixed Two-Band Enhancer`, thread `01a03b9a-b1c0-7431-bc23-3e85da52c3d0`, branch `codex/t26-spectre-wrapper-prototype`, worktree `/Users/winterfell/.codex/worktrees/479c3700-c222-4730-88a3-1f9ff584b411/cosimo-synth`, model SOL, reasoning max.
    - This thread is Andrew's product-development conversation, not a coordinator-managed implementation worker. Do not send it repair requests or other assignments. Review its committed source; if repair is needed, ask Andrew to authorize a separate task.
    Design authority: `ENHANCER_DESIGN.md`.
    Locked processor:
    - Preserve the accepted `CosimoEnhancer` processor: two parametric bell bands, each with Frequency, Q, Stereo/Mid-Side routing, separate Mid and Side amounts, and Tube/Solid character; plus global Subtle/Medium intensity and the saved de-emphasis amount used during listening. These fourteen static settings are smoothed, preset-persisted, and unavailable to modulation and the Effects Lane.
    - Preserve the high-quality measured 4x FIR wrapper and its truthful 60-sample declared latency. Do not replace it with Enhancer Lite's cheaper 3-sample IIR wrapper or the per-voice ADAA design.
    - Generate the accepted shaped-minus-selected contribution, condition it, and add it back to the delayed original signal. With every amount at zero, the nonlinear contribution must be exactly zero and output must match the retained JUCE FIR neutral impulse within `1e-7`, with exactly 60 samples reported to the host. Do not restore the obsolete same-frame bit-exact or zero-latency implementation.
    - Put the processor after Safe Bass and before the compressor/clipper in T28.
    Scope boundary:
    - T61 owns packaging and finishing the separate one-band Enhancer Lite product. T62 owns the still-required lightweight per-voice form. Neither is part of T26 completion, and neither may be used to simplify this processor.
    Done when:
    - Zero amount produces no nonlinear contribution, matches the retained FIR neutral impulse within `1e-7`, and reports exactly 60 samples; mono stays mono; side-only behavior, safety, reset, state, and declared latency pass; and level-matched listening preserves the approved sound.
    - The high-quality two-band processor is cleanly available for T28 without silently deleting or replacing any accepted sound, routing, state, or wrapper behavior.
    Integration evidence (2026-08-27):
    - Coordinator source commit `71f106b9` semantically replays protected checkpoint `2a652a40` onto current master without importing its stale tracker or design copies. The current 14-setting contract, separate Lite/T62 boundaries, Polish lab build target, and unrelated package history are preserved.
    - Final qualification passes Cmajor 14/14, state/exposure 7/7, source/compiled editor 7/7, and the complete retained audio evidence. The zero-Amount neutral FIR, exact 60-sample host seam, mono/Side behavior, reset, sample-rate safety, and approved measured laws are covered.
    - The clean universal `CosimoEnhancer.vst3` build installs at `/Users/winterfell/Library/Audio/Plug-Ins/VST3/CosimoEnhancer.vst3`, passes strict deep code-sign verification, and has executable SHA-256 `a0f356fd56f612c816e0dc7b011c50d4380efb5abeb1d5ee0b1837200b419040`, exactly matching the previously pluginval/Ableton-qualified binary. The prior observed Ableton listening sign-off is retained; no new listening or physical-device acceptance is claimed.
    - This completes only the high-quality two-band T26 processor. Lite readout/shelf composition, T61 release/MIDI work, T62 per-voice work, and T28 Polish-chain integration remain open.

[x] T27 — Reproduce the Sausage Fattener reference before using it to voice Cosimo's compressor and clipper
    Completed owner (2026-08-27):
    - Codex task `Complete T27 — isolated polish comp/clip reference lab`, thread `01a03b9b-5ad4-7f11-aafb-53bc9db9ca6f`, branch `codex/t27-isolated-reference-lab`, worktree `/Users/winterfell/.codex/worktrees/a68e3ac6-eb15-442d-9a2e-af21dae745ad/cosimo-synth`, model SOL, reasoning max.
    Replacement direction after sound-design review (2026-08-27):
    - The first decoded knot/tension/Drive editor was rejected rather than retained as the product surface. Curve Lab exposes only compressor Threshold, Ratio, Knee, Attack, Release, and Makeup plus one waveshaper Morph control; decoded values remain evidence in the isolated reference package, not dormant live controls.
    - The compressor graph uses its real threshold/knee/ratio/makeup equation, operating point, and gain-reduction trace. The replacement waveshaper is one full bipolar graph with independent positive/negative ceilings, up to seven two-dimensional points per side, direct segment bends, exact entry, and one assigned point moving linearly between visible Morph A/B positions.
    - The active waveshaper bend is a documented signed output-space midpoint offset. Graph/DSP parity, raw host-state cancellation, pointer ownership, telemetry hit testing, add-point continuity, saved-state replay, and the compiled real-control surface must all be proven before integration.
    Reference authority: `SAUSAGE_FATTENER_ANALYSIS.md`.
    Work:
    - Preserve the provenance distinction between the closed-source original, Polarity's reported null-tested recreation, the decoded Bitwig preset values, and our own inferences.
    - Build an isolated reference lab for the decoded compressor settings, clipper transfer curve, input/output gains, and Fatness macro wiring. Retain the parser or extraction evidence, numeric fixtures, audio corpus, renders, and measurements.
    - Use this as one comparison target for T28, not as permission to copy a product, interface, name, or closed implementation.
    Done when:
    - Automated fixtures pin the decoded values and transfer-curve points, the reference lab reproduces them deterministically, and a level-matched comparison bundle is available before T28's compressor/clipper is voiced.
    Completion evidence (2026-08-27):
    - `reference_labs/polish_comp_clip/` remains an isolated evidence package with no production import path in either direction. It pins Polarity's source commit/file digest, exact record offsets and IEEE-754 values, the decoded compressor/macro/curve fixtures, three 48 kHz inputs, and 18 raw/level-matched renders. The final reference gate passes 7/7 and authenticates/regenerates all 21 WAVs with worst RMS match `1.742e-7 dB`.
    - `fx/polish_lab/` is the independently named, production-disconnected Curve Lab. The final composed lab gate passes 16/16 Node/browser checks and Cmajor passes 5/5. It proves compressor and bipolar-waveshaper graph/DSP parity, folds and flat/descending bends, independent sides, exact split continuity, raw-state cancellation, owning-pointer lifecycle, telemetry passthrough, reset, and complete host-state reconstruction.
    - The coordinator preserved current `POLISH_CHAIN_DESIGN.md` authority and imported no stale branch tracker/design prose. `npm run fx:jit:install -- polish` associated the final runtime with the strict-code-sign-valid `/Users/winterfell/Library/Audio/Plug-Ins/VST3/CmajPlugin.vst3`. Association SHA-256 is `431e5121272bc637f5f4fe2cb6a1fe85edd1817bccdae6263cd811f7f5cf1881`; compiled DSP/UI SHA-256 values are `3a15cf7dbbff41c978206ba3fd08fb653e128937c2a1ed20072800a33e2824f5` and `f0bcbb51fbc221a12415c99317f95cefa4e91a61290a891ee8bd59756b5ea0b7`.
    - Fresh integration-time pluginval 1.0.4 strictness 5 with seed `0x27a8` succeeds, including cold/warm editor, editor-while-processing, automation, state, stereo buses, and 44.1/48 kHz at 64/512 frames. Earlier branch evidence records a successful Ableton processor load; no fresh integration-time Ableton custom-editor gesture/listening pass or physical touch acceptance is claimed.

[ ] T28 — Build the fixed end-of-chain Polish section
    Design authority: `POLISH_CHAIN_DESIGN.md`.
    Locked signal chain:
    - This is one fixed, non-reorderable, non-modulatable end-of-chain section: Safe Bass into the T26 Enhancer, then medium compression into an intentional soft clipper, followed by Output Trim.
    - Up to 4 ms of constant lookahead is permitted if the host is told the exact delay. No audible gain change may complete faster than one 60 Hz cycle (about 16.7 ms). FAT, Width, Tilt, and Punch are not part of this version.
    User controls and tuning:
    - T26's fourteen settings remain the internal high-quality Enhancer contract. T28 decides which values are baked and how the public Polish controls drive them; a compact final interface is not permission for the integrator to delete T26 controls or replace its DSP while merging the source.
    - Expose a dedicated `Enhancer Amount` knob. Frequency, Q, routing, character, intensity, de-emphasis, and the relative per-band Mid/Side balance remain baked internal settings. The knob scales the accepted per-band Enhancer amounts together from neutral to the tuned Polish setting; tune its response during development.
    - Expose a separate `Compression/Clip Amount` knob for the compressor and soft-clip combination. Do not expose separate Compression and Clip controls. Tune that macro mapping and the final medium-compression values during development using level-matched listening and the T27 reference bundle.
    - Safe Bass is always fixed and has no user control. Tune its cutoff and side-only high-pass slope during development, then bake the accepted values into the design.
    - Expose Output Trim after the intentional Polish soft clipper.
    - Init and every brand-new patch start with `Enhancer Amount` at zero, `Compression/Clip Amount` at zero, and Output Trim at 0 dB. Polish starts off; do not ship an audible default.
    - When both Amount knobs are zero, the complete Polish section must be neutral apart from its declared constant latency: Safe Bass must not change the signal merely because it has no visible control. Derive its internal activation from the two Amount knobs and transition click-free; do not add a public or saved Polish bypass state. Once either Amount is raised, Safe Bass uses its one baked cutoff and slope.
    Interface placement:
    - Show one permanent `POLISH` node at the end of the FX graph, after the complete editable Effects Lane. It represents the fixed end-of-chain section and is not another lane effect.
    - Tapping `POLISH` opens Enhancer Amount, Compression/Clip Amount, and Output Trim in the existing effect-editor area. The node does not create another permanent panel or consume extra page height outside the graph.
    - The `POLISH` node cannot be moved, deleted, replaced, reordered, or bypassed. It has no effect context menu or drag behavior. The Effects Lane tail `+` remains immediately before it and continues to insert a normal effect at the editable lane's tail.
    - Keep one fixed-width post-trim meter capsule inside the existing compact preset-bar row. It adds no row or height and uses tabular fixed-width digits so live values cannot shift the preset name or menu. The approved position is on the left side while the preset name remains centered and the menu remains on the right.
    - The capsule contains one small light plus two compact readouts: `P` shows ordinary post-trim sample peak/headroom in dBFS relative to 0 dB and `L` shows 400 ms momentary loudness. The light pulses with momentary loudness and changes color with peak headroom, reaching red at digital full scale. A new higher peak writes immediately, holds for one second, then falls smoothly toward silence; a later higher peak restarts that hold. Do not add true-peak oversampling, a selectable meter mode, a permanent latch, tap-to-clear behavior, saved meter state, or an expanded mastering panel. Andrew chose ordinary sample peak because it is the simplest implementation.
    - On detail screens, show the universal Back glyph immediately before the meter as one compact left-side cluster; the meter remains visible. Keep the preset name independently centered and truncate it before collision, while the menu stays fixed on the right. Real composed proof at 320 px must show that neither Back nor the meter disappears, adds height, covers another control, or moves the centered name.
    - The whole-Effects-Lane Mix and Bypass controls remain upstream and do not affect Polish.
    Saved-sound cutoff:
    - Do not migrate or preserve presets made before Polish exists. Adding Polish hard-cuts the complete saved-sound format to a new version. Presets, automatic browser state, shared URLs, and host state from an older complete-sound version are ignored or rejected as a whole; never infer missing Polish values and never create a hidden legacy `Polish off` state.
    - Inventory the factory presets shipped in this repository. Rebuild any factory sound that is intentionally retained in the new complete format and delete obsolete old-format copies; do not retain a compatibility reader merely to keep them loadable.
    Final output behavior:
    - Delete the separate final `RackOutputStage` waveshaper. It is a safety clipper in practice, not a limiter, and must not remain after the Effects Lane.
    - Do not replace it with an always-on final limiter or any other hidden output processor. Internal safety shaping inside an individual effect or feedback loop remains part of that effect and is outside this removal.
    - The compact preset-bar capsule reports the post-trim output and never alters it. Ordinary sample peak/headroom drives its color and `P` readout; 400 ms momentary loudness drives its pulse and `L` readout. Reaching or exceeding 0 dBFS turns it red.
    Architecture proof before sound work:
    - Decide the compressor's exact lookahead within the approved 0-4 ms range, add it to T26's fixed 60-sample Enhancer latency, then prove that the desktop plugin, AUv3, standalone app, and browser path report and compensate that exact constant total. At `Compression/Clip Amount` zero, that stage must remain a delayed identity rather than a timing-mismatched bypass. At `Enhancer Amount` zero, retain T26's fixed latency while its nonlinear contribution is neutral.
    - Update `POLISH_CHAIN_DESIGN.md` to match these settled choices before implementing the sound.
    Done when:
    - The implemented order is Safe Bass → Enhancer → medium compressor → intentional soft clipper → Output Trim, with only Enhancer Amount, Compression/Clip Amount, and Output Trim exposed to the user.
    - The real FX graph always shows `POLISH` after the editable lane, opens its controls in the normal editor area, and proves that add, drag, swap, delete, context-menu, per-effect bypass, and whole-lane bypass actions cannot move or alter that fixed boundary.
    - Tests prove Init/new-patch neutrality, the fixed Safe Bass values when Polish is active, independent behavior and neutral points for both Amount knobs, a click-free transition out of the all-zero state, the 60 Hz gain-change floor, smoothing, preset recall, exact reported lookahead, Output Trim, and correct peak/loudness metering. Real composed-layout coverage at 320 px and wider proves the fixed-height capsule, Back control, centered preset name, and menu coexist without overlap or digit-driven movement.
    - State tests prove every current-format Polish value round-trips and every older complete-sound version is ignored atomically without partially changing the running sound. No migration, missing-field fallback, or hidden compatibility flag exists.
    - The separate `RackOutputStage` is gone, there is no replacement final limiter or hidden final clipper, below-ceiling output is not changed after Output Trim, and overloads are reported without being silently reshaped.
    - Andrew approves the fixed Safe Bass tuning, both Amount responses, compression/clip character, and level-matched listening against the T27 reference bundle.

[?] T29 — Use the distortion field notes to choose one genuinely new sound experiment
    Knowledge source: `DISTORTION_FIELD_NOTES.md`.
    Work:
    - Treat Height, Speed, Time, Change, and Memory as ways to distinguish distortion behavior, not as five promised products.
    - Map the notes against T23, T25, T26, and T28 so the next experiment does not merely duplicate the saturator, enhancer, clipper, or output chain already planned.
    - Select at most one next experiment whose audible claim can be tested with a frozen corpus, a named baseline, a level-matched comparison, measurable safety and CPU checks, and a result that would change a build/defer/reject decision.
    - Keep physical tape, hysteresis, and other grade-3 models out of production scope unless Andrew explicitly chooses one after the experiment.
    Done when:
    - The notes have produced one prioritized, testable experiment or a recorded decision that none is currently worth building. Any successful experiment still requires a separate product-placement and control-design decision.

IMPLEMENTED BASELINES TO CONFIRM, NOT REBUILD BLINDLY

[x] B01 — Finger-clear modulation dragging exists at commit 3b769b21
    - Touch drag uses amplified preview movement, target capture/hysteresis, haptic acquisition, rail retreat, and preview-owned drop targeting.
    - Product follow-up: physically confirm that it now feels right; reopen only a specific failed behavior.

[x] B02 — Discrete oscillator controls have detents and best-effort haptics
    - Octave and Semitone are discrete; Fine and Level remain continuous.

[x] B03 — Mobile Mod Matrix list/detail and compact MSEG editor exist
    - These are implementation baselines, not automatic approval of the newer inline-edit direction in T14/T15.

[x] B04 — FX parameter-state truth correction and exact-value menus exist
    - Current baseline uses neutral base framing and source color for the armed source/current-target relationship.
    - T07 remains open because the spoken workstream questions that color contract and reports too much white.

[x] B05 — Global Mod rail and collision-aware modulation HUD exist
    - T09 should inventory missing controls and reuse this language rather than creating another HUD system.

COMMON DEFINITION OF DONE FOR CODE TASKS

- Reproduce or capture the current reachable product behavior before editing.
- Preserve unrelated dirty work and record the exact source/commit used for validation.
- Add the narrowest outer-seam regression that proves the user-visible behavior, not only component internals.
- For mobile interaction work, verify representative short and tall phone layouts and use real touch where the result depends on feel.
- For completed desktop React synth work, finish with a fresh HMR standalone launch from this repo and a rebuilt/installed Ableton VST3, unless explicitly waived.
- Update this file with status and evidence; update PROGRESS.txt only with durable decisions, blockers, or hard-won setup knowledge.

PLANNING DECISIONS RECORDED HERE

D01 — Finish the accepted Voice handoff first, then replace the tab shell before Filter/Mod layout work.
Reason: ADR-024 deliberately keeps the accordion so the focused Voice cutover can ship independently. The later tab shell determines the real vertical space and navigation model inherited by Filter and Mod; building those deeper editors before it would make their geometry provisional.

D02 — Existing related implementations are baselines to audit, not proof that the spoken concerns are resolved.
Reason: the repository shows completed work for haptics, matrix/detail, parameter states, HUD, and drag occlusion, while the spoken inventory still reports dissatisfaction or uncertainty. Product acceptance must resolve that mismatch.

D03 — Effects Lane is parked behind cross-platform qualification, not merely phone availability.
Reason: the newest evidence has both an unexecuted physical campaign and a browser no-go. A phone run cannot by itself authorize production integration.

D04 — Wavetable graph X = Warp and Y = Index is provisional.
Reason: rolling one-axis-at-a-time Warp/Index shaping is the current starting behavior, while physical use may show that horizontal table switching is more valuable. The current cutover keeps explicit table selection and isolates X behind one graph-axis seam so that later review does not disturb Y/Index, state ownership, or the retained renderer.

D05 — The patch_gui single-sourcing port (T02B) runs immediately after T02, before the tab shell.
Reason: T02's in-flight work edits the hand-written renderer and its declaration shims directly, and a 2026-08-18 audit proved two of the three CanvasWavetableDisplay declarations are dead (invalid relative ambient modules masked by skipLibCheck) while the live one advertises a setFrames(null) call the implementation has always thrown on. Every later renderer-adjacent task (D04 axis review, T04 filter graphics) would otherwise keep paying the hand-sync tax; porting on a clean tree right after T02 closes the gap at its cheapest point. The committed patch_gui directory and the Path B single-owner rule for render math are deliberate and stay; only the source of truth moves to typed ui/shared modules emitted by the existing pipeline.
